## Scope override (from .context/137.md)
verdict=current — no override. The only refinement is AC1's mechanics: `graph-specs check` does NOT support multiple `--code` args, so the two-pass fallback is the shipping form. Two valid implementations satisfy the acceptance criteria:

- Option A: disjoint spec subtrees — `specs/concepts/` (crates) + `specs/tools/` (tools) + two CI passes.
- Option B: disjoint spec subtrees under one root — `specs/concepts/crates/` + `specs/concepts/tools/` + two CI passes.
- Option C: a single `--code .` pass — REJECTED because it picks up `spikes/qa5-utc-now/` (confirmed: three types leak through) and the issue forbids that.

Prescription picks between A and B.

## Forbidden moves (from .context/137.md)

- Do NOT restore the spec file `specs/concepts/qa5-utc-now-spike.md` that commit `15f7b7a` added and `ff00b4a` reverted. `spikes/qa5-utc-now/` is legitimately experimental and stays excluded.
- Do NOT add a `cfdb-concepts` dependency to `tools/check-prelude-triggers/Cargo.toml`. The `LoadError` duplication is intentional (Forbidden move #8 in the tool's own prescription).
- Do NOT merge, alias, or cross-reference the `check-prelude-triggers::TriggerId` and `cfdb_cli::TriggerId` concepts. They are a deliberate DDD homonym across bounded contexts.
- Do NOT collapse `check-prelude-triggers::toml_io::LoadError` with `cfdb_concepts::LoadError` in the spec set. They are independent concepts (separate change vectors, separate owners).
- Do NOT use `--code .` as the shipping CI invocation. It picks up `spikes/qa5-utc-now/` types (`FileStats`, `Subclass`, `Totals`) and would re-introduce the boy-scout trap that ff00b4a rolled back.
- Do NOT onboard additional `tools/` entries in this PR. Only `check-prelude-triggers` is in scope; future tools need their own follow-up.
- Do NOT alter the fact-graph / wire format / schema version. This is CI + docs only; no `cfdb-core::SchemaVersion` touch.
- Do NOT introduce a baseline/ceiling/allowlist file for violations. Zero-tolerance; any pre-existing spikes/ leakage must be excluded by structure, not waived by a baseline.
- Do NOT run BDD/TDD gates for this issue — it is pure CI + docs (project CLAUDE.md §1 routing). The proof is the dogfood run, not a RED→GREEN test cycle.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DISCOVERY ARTIFACT — Issue #137 (agency:yg/cfdb)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue: feat(ci): extend graph-specs anti-drift gate to tools/ — check-prelude-triggers is a first-class deliverable
Date: 2026-04-21
Branch: work/137-tools-anti-drift-gate
cfdb_keyspace_sha: 4864b37df6ac

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DOCTRINAL CONTEXT — Issue #137
Source: .context/137.md Doctrinal layer (refreshed_sha=4864b37)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- `docs/RFC-030-anti-drift-gate.md:141` — the RFC's canonical invocation is `graph-specs check --specs specs/concepts/ --code crates/`. Implication: the narrow `--code crates/` path is what the RFC originally ratified; onboarding `tools/` is a scoped amendment the PR body must explain.
- `docs/RFC-030-anti-drift-gate.md:532` — G2 gate: "graph-specs check ... exits 0 on the main branch". Implication: the widened invocation must still satisfy G2 on develop — zero violations across both passes, no ratchet file.
- `CLAUDE.md` (project) §3 — "Adding a new ban rule is an RFC-gated change." Implication: this issue is NOT adding a new ban rule; it is extending the SCOPE of the existing rule. Covered by the issue's own archaeology; no new RFC required.
- `CLAUDE.md` (project) §1 — "Docs, CI config, chore | Issue → direct PR → dogfood-gated PR". Implication: this issue is CI config + spec docs; the normal full-gate pipeline is NOT required. Discovery + prescription + ship are sufficient; BDD/TDD/reality gates do not apply (no Rust code changes).
- `CLAUDE.md` (project) §5 — "the recall crate holds cfdb to the rustdoc ground truth. Any RFC that adds a new fact type MUST extend the recall corpus." Implication: N/A — this issue adds no fact types.
- `tools/check-prelude-triggers/src/lib.rs:18-21` — "Homonym note: the C* IDs here are RFC-034 mechanical pre-council triggers and are distinct from cfdb's internal Cypher-query triggers T1/T3 (cfdb issues #100/#101)." Implication: `check-prelude-triggers::TriggerId` and `cfdb-cli::TriggerId` are homonyms BY DESIGN. The new spec file must describe only the `C*` enum; it must NOT merge or cross-reference the `T*` enum in `cfdb-cli.md`.
- `tools/check-prelude-triggers/src/toml_io.rs:3-4` — "Pattern shape cloned from cfdb-concepts/src/lib.rs:165-220 (REUSE-of-shape per prescription D3; NO cfdb-concepts dependency per Forbidden move #8)." Implication: `check-prelude-triggers::toml_io::LoadError` and `cfdb-concepts::LoadError` are a deliberate duplication. The new spec must describe the tool's `LoadError` on its own terms; it must NOT collapse the two into a single concept.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONCEPT INVENTORY — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

graph-specs anti-drift gate (CI step)
  ENTRY:       .gitea/workflows/ci.yml:108-110  — "Graph-specs check (RFC-030 §3.1 anti-drift gate)" step; body `graph-specs check --specs specs/concepts/ --code crates/`
  CI-SIDE:     .gitea/workflows/ci.yml:86-104   — install step `cargo install --git https://agency.lab:3000/yg/graph-specs-rust --branch develop --root /cache/cargo --bin graph-specs --force application`
  DOCTRINAL:   docs/RFC-030-anti-drift-gate.md:141  — reference invocation
  DOCTRINAL:   docs/RFC-030-anti-drift-gate.md:532  — G2 gate criterion

Graph-specs binary scope (--code argument)
  VERIFIED:    `graph-specs --help` / `graph-specs check --help` at workspace-local install shows a SINGLE `--code <CODE>` arg (no multi-value). Output captured during freshness.
  VERIFIED:    `graph-specs check --specs <dir> --code <dir>` walks `--specs` recursively across subdirs (verified with a `nested/test.md` fixture during freshness — a type defined in the nested spec triggered "missing in code" as expected).

specs/concepts/ spec tree (current state)
  LAYOUT:      specs/concepts/cfdb-cli.md (23 LOC)
               specs/concepts/cfdb-concepts.md (27 LOC)
               specs/concepts/cfdb-core.md (171 LOC)
               specs/concepts/cfdb-extractor.md (9 LOC)
               specs/concepts/cfdb-hir-extractor.md (19 LOC)
               specs/concepts/cfdb-hir-petgraph-adapter.md (9 LOC)
               specs/concepts/cfdb-petgraph.md (11 LOC)
               specs/concepts/cfdb-query.md (51 LOC)
               specs/concepts/cfdb-recall.md (19 LOC)
  GATE STATE:  `graph-specs check --specs specs/concepts/ --code crates/` → "0 violations." (exit 0) at `refreshed_sha=4864b37`.
  GATE STATE:  `graph-specs check --specs specs/concepts/ --code tools/` → 80+ violations of the form "missing in code" (every crate spec's types resolve nowhere in `tools/`).

specs/README.md
  NOT FOUND — no `specs/README.md` exists on `origin/develop` @ 4864b37.
  Searches exhausted: `ls specs/README.md`; `ls specs/` shows only the single subdir `concepts/`; `rg -l specs/README\.md .` (not tried — absence is confirmed by direct `ls`).

Makefile target for graph-specs
  NOT FOUND — `Makefile` contains only the no-op `test-integ-up` / `test-integ-down` targets (cfdb is a pure-library workspace per `CLAUDE.md` §3). No target references `graph-specs`.
  Searches exhausted: `grep -n "graph-specs" Makefile`; `find . -maxdepth 3 -name "Makefile*"` finds a single `./Makefile`; no hidden `.make/` targets.

check-prelude-triggers tool (pub surface)
  LIB:         tools/check-prelude-triggers/src/lib.rs:20-26  — `pub mod report; pub mod toml_io; pub mod trigger_id; pub mod triggers; pub use report::PreludeTriggerReport; pub use toml_io::LoadError; pub use trigger_id::TriggerId;`
  BIN:         tools/check-prelude-triggers/src/main.rs:15-19  — imports `report::PreludeTriggerReport`, `triggers::{c1..c9, TriggerOutcome}`.
  HOMONYM:     tools/check-prelude-triggers/src/lib.rs:18-21  — explicit doc note: "the C* IDs here are RFC-034 mechanical pre-council triggers and are distinct from cfdb's internal Cypher-query triggers T1/T3".

PreludeTriggerReport (TOOL-PUBLIC struct)
  DEF:         tools/check-prelude-triggers/src/report.rs:32  — `pub struct PreludeTriggerReport { schema_version, from_ref, to_ref, triggers_fired: Vec<TriggerId>, evidence: BTreeMap<String, serde_json::Value> }`
  RE-EXPORT:   tools/check-prelude-triggers/src/lib.rs:25  — `pub use report::PreludeTriggerReport;`
  USE:         tools/check-prelude-triggers/src/main.rs:108  — `let mut report = PreludeTriggerReport::new(cli.from_ref, cli.to_ref);`
  CONSUMER:    .context/ + RFC-034 §4.2 (skill-level consumption described in `/freshness` Step 2g). The struct's `Serialize` output is the frozen JSON envelope written to `.triggers/<issue>.json`.

SCHEMA_VERSION (TOOL-PUBLIC const)
  DEF:         tools/check-prelude-triggers/src/report.rs:28  — `pub const SCHEMA_VERSION: &str = "v1";`
  USE:         tools/check-prelude-triggers/src/report.rs:45  — set on `PreludeTriggerReport::new`.
  TEST:        tools/check-prelude-triggers/src/report.rs:69-94 — unit tests assert envelope shape at SCHEMA_VERSION "v1".

TriggerId (TOOL-PUBLIC enum — HOMONYM with cfdb-cli::TriggerId)
  DEF:         tools/check-prelude-triggers/src/trigger_id.rs:11  — `pub enum TriggerId { C1, C3, C7, C8, C9 }` with `#[serde(rename = "C1")]` etc.
  AS_STR:      tools/check-prelude-triggers/src/trigger_id.rs:26-37  — `pub const fn as_str(self) -> &'static str` pattern-matches each variant.
  RE-EXPORT:   tools/check-prelude-triggers/src/lib.rs:27  — `pub use trigger_id::TriggerId;`
  NO FromStr:  no `impl FromStr for TriggerId` exists in the tool — envelope is output-only. (The cfdb-cli homonym HAS `impl FromStr` with a paired `UnknownTriggerId` parse-error type.)

UnknownTriggerId
  NOT FOUND — tool's `TriggerId` is output-only (Serialize only; no FromStr), so no parse-error type exists. The cfdb-cli homonym has one at `crates/cfdb-cli/src/check.rs:139`.
  Searches exhausted: `rg "UnknownTriggerId|FromStr" tools/check-prelude-triggers/src` → zero matches.

TriggerOutcome (TOOL-PUBLIC struct)
  DEF:         tools/check-prelude-triggers/src/triggers/mod.rs:16-22  — `pub struct TriggerOutcome { pub fired: bool, pub evidence: serde_json::Value }`
  CONSTRUCTED: all 5 trigger handlers at c1/c3/c7/c8/c9_*.rs `evaluate*` functions.
  USE:         tools/check-prelude-triggers/src/main.rs:132  — `fn run_command(cmd: &Command) -> Result<(TriggerId, TriggerOutcome), LoadError>`.

LoadError (TOOL-PUBLIC enum — DUPLICATE-BY-DESIGN with cfdb_concepts::LoadError)
  DEF:         tools/check-prelude-triggers/src/toml_io.rs:11-25  — `pub enum LoadError { Io { path, source }, Toml { path, source } }` via `thiserror::Error`.
  RE-EXPORT:   tools/check-prelude-triggers/src/lib.rs:26  — `pub use toml_io::LoadError;`
  RATIONALE:   tools/check-prelude-triggers/src/toml_io.rs:3-4  — inline comment: "Pattern shape cloned from cfdb-concepts/src/lib.rs:165-220 (REUSE-of-shape per prescription D3; NO cfdb-concepts dependency per Forbidden move #8)."
  USE:         every trigger module at `use crate::toml_io::{..., LoadError};`
  USE:         tools/check-prelude-triggers/src/main.rs:21  — re-imported via `use check_prelude_triggers::{..., LoadError, ...}`.

read_toml / read_changed_paths (TOOL-PUBLIC fns)
  DEF:         tools/check-prelude-triggers/src/toml_io.rs:33  — `pub fn read_toml(path: &Path) -> Result<toml::Value, LoadError>`
  DEF:         tools/check-prelude-triggers/src/toml_io.rs:47  — `pub fn read_changed_paths(path: &Path) -> Result<Vec<String>, LoadError>`
  USE:         all 5 trigger modules import these helpers.

Per-trigger handler fns (TOOL-PUBLIC, not types)
  DEF:         tools/check-prelude-triggers/src/triggers/c1_cross_context.rs:22  — `pub fn run(context_map, changed_paths) -> Result<TriggerOutcome, LoadError>`; `:30 pub fn evaluate`
  DEF:         tools/check-prelude-triggers/src/triggers/c3_port_signature.rs:29  — `pub fn run`, `:36 pub fn evaluate`
  DEF:         tools/check-prelude-triggers/src/triggers/c7_financial_precision.rs:19  — `pub fn run`, `:27 pub fn evaluate`
  DEF:         tools/check-prelude-triggers/src/triggers/c8_pipeline_stage.rs:19  — `pub fn run`, `:27 pub fn evaluate`
  DEF:         tools/check-prelude-triggers/src/triggers/c9_workspace_cardinality.rs:28  — `pub fn run`, `:45 pub fn evaluate_absent`, `:59 pub fn evaluate_present`
  NOTE:        graph-specs' concept model (observed in existing specs under specs/concepts/) extracts TYPES (struct/enum) from code, not free fns. Free fns would not be flagged as "missing in specs" by the anti-drift gate.

cfdb-cli::TriggerId (HOMONYM — ALREADY SPEC'D)
  DEF:         crates/cfdb-cli/src/check.rs:90  — `pub enum TriggerId { T1, T3 }`
  FromStr:     crates/cfdb-cli/src/check.rs:123  — paired with `UnknownTriggerId` at `:139`
  SPEC:        specs/concepts/cfdb-cli.md (file contents show `## TriggerId` + `## UnknownTriggerId` sections) — confirmed in recent commit `e41365e docs(specs): add TriggerId + UnknownTriggerId spec entries for #101`.
  CO-EXISTENCE: the tool's TriggerId enum and the cfdb-cli TriggerId enum live at different file paths in different crates; graph-specs extracts types by lexical name, so both needing spec coverage means TWO sections titled `## TriggerId` — one in `specs/concepts/cfdb-cli.md` (T1/T3) and one in the new tools spec (C1/C3/C7/C8/C9). The session handoff `a0-session:2026-04-21-cfdb-triggers-t1-t3-shipped` confirms the DDD homonym is intentional; `audit-split-brain` flags the duplicate type name but `Forbidden moves` of .context/137.md explicitly permit this co-existence.

cfdb_concepts::LoadError (DUPLICATE-BY-DESIGN — ALREADY SPEC'D)
  DEF:         crates/cfdb-concepts/src/lib.rs:216  — `pub enum LoadError`
  SPEC:        specs/concepts/cfdb-concepts.md has a `## LoadError` section (shown in freshness cat).
  DUPLICATE:   audit-split-brain's `duplicate_type_defs` entry identifies `LoadError` as defined in both `cfdb-concepts` and `check-prelude-triggers` crates. The tool's crate marks the duplication as intentional per the source-file comment.

spikes/qa5-utc-now (MUST REMAIN EXCLUDED)
  DEF:         spikes/qa5-utc-now/src/main.rs:50  — `pub struct FileStats`
  DEF:         spikes/qa5-utc-now/src/main.rs:72  — `pub struct Totals`
  DEF:         spikes/qa5-utc-now/src/classify.rs:20  — `pub enum Subclass`
  DEF:         spikes/qa5-utc-now/src/classify.rs:10,28 — `pub fn is_test_path`, `pub fn classify_line`
  DEF:         spikes/qa5-utc-now/src/artifacts.rs:14,40 — `pub fn write_tsv`, `pub fn write_markdown`
  REVERT:      commit `ff00b4a fix(specs): revert boy-scout check-prelude-triggers + qa5-utc-now specs (#48)` — message: "CI invokes graph-specs with --code crates/ (see .gitea/workflows/ci.yml:110), which EXCLUDES tools/ and spikes/ deliberately — those aren't the library surface, so they're not expected to have specs."
  IMPLICATION: the spike dir is a STATED scope boundary (not a bug). The issue (#137) AC3 reaffirms this. Any strategy that pulls spike types into the gate is a regression.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GLOBAL RESOLUTION CENSUS — parseable concepts in scope
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

check_prelude_triggers::TriggerId (enum C1/C3/C7/C8/C9)
  Resolution points found: 0 (output-only; no FromStr) — OK (no split risk; the tool emits, never parses).
  Serialization points: `Serialize` via serde rename + `as_str(self)` (both in `tools/check-prelude-triggers/src/trigger_id.rs`).

cfdb_cli::TriggerId (enum T1/T3)
  Resolution points found: 1 (canonical) — OK.
    1. crates/cfdb-cli/src/check.rs:123 — `impl FromStr for TriggerId` (CANONICAL)
  audit-split-brain `fromstr_bypasses` flagged the clap wrapper at `crates/cfdb-cli/src/main.rs:72` as `delegates: true` (legitimate delegation, NOT a split).

check_prelude_triggers::toml_io::LoadError
  Resolution points found: 0 (error enum, not parseable) — OK.
  Definitions: audit-split-brain `duplicate_type_defs` entry shows the enum is also defined in `cfdb-concepts`. Inline rationale comment marks the duplication as intentional.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONST TABLE CENSUS — parseable concepts in scope
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SCHEMA_VERSION (TOOL-PUBLIC)
  Definitions found: 1 — OK.
    1. tools/check-prelude-triggers/src/report.rs:28 — `pub const SCHEMA_VERSION: &str = "v1";` (CANONICAL)
  No duplicates elsewhere in the workspace.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXISTING RESOLUTION POINTS — Issue #137 (audit-split-brain --full-workspace, scope-filtered)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Concept: TriggerId
  Definition A: crates/cfdb-cli/src/check.rs:90 — enum { T1, T3 } (editorial-drift bounded context)
  Definition B: tools/check-prelude-triggers/src/trigger_id.rs:11 — enum { C1, C3, C7, C8, C9 } (RFC-034 mechanical bounded context)
  Ownership A: cfdb-cli (consumer of cfdb keyspace; editorial-drift queries)
  Ownership B: check-prelude-triggers (Tier-1 binary; RFC-034 §4.2 envelope)
  Homonym state: INTENTIONAL per tool's lib.rs docstring (lines 18-21) and confirmed by session handoff `2026-04-21-cfdb-triggers-t1-t3-shipped`.

Concept: LoadError
  Definition A: crates/cfdb-concepts/src/lib.rs:216 — enum (concept overrides + published-language-crates loader errors)
  Definition B: tools/check-prelude-triggers/src/toml_io.rs:11 — enum (trigger-binary TOML loader errors)
  Ownership A: cfdb-concepts (concept-map + published-language crate loader)
  Ownership B: check-prelude-triggers (RFC-034 S0 TOML loaders: context-map, pipeline-stages, financial-precision-crates)
  Duplication state: INTENTIONAL per tool's toml_io.rs:3-4 comment (cloned pattern, forbidden dep).

Concept: TriggerOutcome
  Single resolution point — no split-brain risk ✅ (only defined at tools/check-prelude-triggers/src/triggers/mod.rs:16).

Concept: PreludeTriggerReport
  Single resolution point — no split-brain risk ✅ (only defined at tools/check-prelude-triggers/src/report.rs:32).

Concept: Visibility (FromStr bypass flagged — NOT in scope for this issue)
  audit-split-brain flagged `crates/cfdb-extractor/src/item_visitor.rs:674 parse_syn_visibility` as `delegates: true`. Outside #137's touch scope.

Source: audit-split-brain --full-workspace (filtered to issue scope)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MCP/CLI BYPASS RISKS — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No MCP/CLI handlers exist in the cfdb workspace — cfdb has no MCP server and no qbot-mcp crate. The only CLI is `cfdb-cli`. The tool `check-prelude-triggers` is itself a CLI, invoked by `/freshness` Step 2g, not by another handler. MCP/CLI bypass scan N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PARAM CENSUS — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No resolver/factory/consumer files in scope — Param Census N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALL CHAINS — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Chain 1: CI runner invokes the anti-drift gate
  ENTRY  → .gitea/workflows/ci.yml:108           "Graph-specs check (RFC-030 §3.1 anti-drift gate)" step declaration
  SHELL  → .gitea/workflows/ci.yml:110           `graph-specs check --specs specs/concepts/ --code crates/`
  BIN    → (external) `graph-specs` binary (installed from `yg/graph-specs-rust` develop branch at ci.yml:86-104)
  WALK   → `specs/concepts/` recursively for `*.md`; `crates/` recursively for `*.rs`
  OUTPUT → "N violations." → exit 0 (N=0) or exit 1 (N>0)

Chain 2: `/freshness` Step 2g invokes the tool binary
  ENTRY  → skills/freshness Step 2g               consumer-repo-gated (`.cfdb/` present)
  SHELL  → `check-prelude-triggers --from-ref <sha> --to-ref <sha> c1-cross-context ...` (one invocation per trigger)
  BIN    → tools/check-prelude-triggers/src/main.rs:1..               clap dispatch
  FN     → tools/check-prelude-triggers/src/main.rs:132 run_command() → TriggerId + TriggerOutcome
  IO     → `toml_io::read_toml` + `read_changed_paths`
  OUT    → `PreludeTriggerReport` serialized to stdout (merged per-trigger envelopes into `.triggers/<issue>.json` by the caller)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DATA FLOW TRACE — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No param/config concepts — data flow trace N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALLER CENSUS — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No resolution functions or option/config structs touched — Caller Census N/A. (The tool's `pub fn run` / `pub fn evaluate` handlers are called only by `main.rs`; each has exactly 1 caller in the binary.)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECORATOR CHAINS — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No trait-based decorator chains in scope. The issue is CI YAML + spec markdown + a single binary's pub surface. `check-prelude-triggers` has no traits.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNERSHIP MAP — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Spec authoring responsibility for the pub types in scope:
  PreludeTriggerReport — owner: `check-prelude-triggers` crate (defined at report.rs:32). No spec anywhere on develop.
  SCHEMA_VERSION        — owner: `check-prelude-triggers` crate (defined at report.rs:28). No spec anywhere on develop.
  TriggerId (C*)        — owner: `check-prelude-triggers` crate (defined at trigger_id.rs:11). No spec anywhere on develop (cfdb-cli's T* homonym is spec'd at specs/concepts/cfdb-cli.md).
  TriggerOutcome        — owner: `check-prelude-triggers` crate (defined at triggers/mod.rs:16). No spec anywhere on develop.
  LoadError             — owner: `check-prelude-triggers` crate (defined at toml_io.rs:11). No spec anywhere on develop (cfdb-concepts's homonym is spec'd at specs/concepts/cfdb-concepts.md; lib.rs docstring notes them as "reused vocabulary, not a new variant").

CI gate invocation ownership:
  Owner: `.gitea/workflows/ci.yml:108-110` — the single authoritative invocation site for the anti-drift gate. No sibling script, no Makefile target, no CI re-use file.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCOPE CLASSIFICATION — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Existing files touched by the issue's domain concepts (from call chain traces):
  CI:          .gitea/workflows/ci.yml:108-110      — invokes graph-specs check
  SPEC:        specs/concepts/*.md                   — authoritative spec surface consumed by the gate
  TOOL:        tools/check-prelude-triggers/src/*    — the library+binary whose pub types are the object of this issue

Layers containing existing code relevant to the issue: CI-CONFIG + SPEC (markdown) + TOOL (library surface, already shipped in PR #127)

Gate sequence (derived from layers — NOT from intuition):
  Gate 1 (BDD):       NOT REQUIRED — no user-facing behavior change. The library surface of `check-prelude-triggers` is unchanged.
  Gate 2 (Contract):  NOT REQUIRED — no new trait, no signature change.
  Gate 3 (Domain):    NOT REQUIRED — no Rust code touched; no business rules in scope.
  Gate 4 (Reality):   NOT REQUIRED — no adapter/infra change; no integration-test path exists.
  Dogfood gate:       REQUIRED — `graph-specs check --specs <new-layout-for-crates> --code crates/` AND `graph-specs check --specs <new-layout-for-tools> --code tools/` both must exit 0 on the PR branch.
  Negative test:      REQUIRED — the issue's AC §Tests names a negative test: rename one pub type in `tools/check-prelude-triggers/src/trigger_id.rs` without touching the spec → CI must fail. Capture the failed-run link in the PR body, then revert.
  Determinism / schema: N/A — no wire-format change.
  Cross dogfood:      UNCHANGED — this PR doesn't touch the graph-specs binary itself, only its invocation and the cfdb-side spec set.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXPERT VERDICTS — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

None — no Step 6 ambiguity trigger fires. The issue is CI + docs with a confirmed pub-type surface and one mechanical ambiguity (multi `--code`) resolved at freshness time.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECISION ARCHAEOLOGY — Issue #137
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Concept: graph-specs anti-drift gate scope (`--code crates/` vs `--code tools/`)
  Issue AC claim: "change the gate line to scan both paths ... If graph-specs check supports multiple --code args, use --code crates/ --code tools/. If not, run the check twice and || exit 1."
  Current state:  `.gitea/workflows/ci.yml:110` runs `graph-specs check --specs specs/concepts/ --code crates/` (single pass, crates only). `graph-specs --help` on the installed binary (graph-specs 0.x, workspace-pinned) confirms `--code <CODE>` is a SINGLE-VALUE arg.
  Introduced by:  `629eb5d ci: wire RFC-030 dual-control gates + fix spec drift (#53)` — wired the RFC-030 gate; subsequently unchanged by `ci: fix graph-specs install — package is application, bin is graph-specs` and `ci: add --force to graph-specs install — track-develop semantics`.
  Rationale:      commit `ff00b4a fix(specs): revert boy-scout check-prelude-triggers + qa5-utc-now specs (#48)` spelled out the mechanics explicitly: "CI invokes graph-specs with --code crates/ (see .gitea/workflows/ci.yml:110), which EXCLUDES tools/ and spikes/ deliberately — those aren't the library surface, so they're not expected to have specs. My local boy-scout run used --code . and saw 6 violations that CI does not enforce; adding specs for those concepts then triggered the reverse direction ('missing in code') because CI's narrower --code scan can't find the types described."
  RFC support:    `docs/RFC-030-anti-drift-gate.md:141` reinforces the narrow form (`--code crates/`). The RFC does NOT explicitly forbid tools/ — it simply hadn't envisioned a Tier-1 binary living under tools/.
  Addendum:       N/A.
  Parent briefing: YES — the context package's Doctrinal layer cites both RFC-030 §141 and §532, and the Forbidden moves explicitly address `--code .` and the ff00b4a revert.
  Divergence verdict (fact, not prescription):
    - Issue AC and current code agree on the DIRECTION of change (extend to tools/) but disagree on MECHANICS (multi `--code` does not exist in the installed graph-specs binary).
    - The ff00b4a revert is consistent with the current narrow `--code crates/` invocation; the revert did not express an opinion about whether `tools/` should be onboarded, only that the spec additions were filed WITHOUT a matching CI change and thus broke the "reverse direction" check.
    - Issue #137 was authored AFTER ff00b4a with full awareness of the mechanics (the issue body cites ff00b4a verbatim).
    - Prescription has enough facts to decide the CI mechanics (two passes over disjoint spec subtrees) without re-reading the issue body in isolation.

Concept: PreludeTriggerReport / TriggerOutcome spec drift
  Issue AC claim: "Renaming any pub type in tools/check-prelude-triggers/src/*.rs fails the CI gate until the matching specs/concepts/check-prelude-triggers.md section is updated."
  Current state:  no `specs/concepts/check-prelude-triggers.md` exists on `origin/develop` @ 4864b37. The types `PreludeTriggerReport`, `TriggerId` (C*), `TriggerOutcome`, `LoadError`, `SCHEMA_VERSION` live in `tools/` — unreachable by the current narrow gate invocation. The tool has been on develop since `666a400 feat(#127): W2.A — check-prelude-triggers Tier-1 binary (5 C-triggers)`, so this is a ~5-day drift gap.
  Introduced by:  `666a400` (tool landed) and `ff00b4a` (spec removed again).
  Rationale:      ff00b4a's message documents the cause — the spec files were added by a boy-scout pass that assumed `--code .` semantics; reverted when CI's narrower `--code crates/` tripped the reverse check. The tool has remained uncoverage-by-design pending the CI widening this issue delivers.
  RFC support:    `docs/RFC-030-anti-drift-gate.md:532` (G2 gate) requires zero violations on main. Because the current gate doesn't look at `tools/`, it reports zero violations with or without spec coverage for tool types — a structural blind spot described in the issue body ("Classifier signal" paragraph).
  Addendum:       N/A.
  Parent briefing: YES — the context package's Forbidden moves explicitly preserve the ff00b4a revert and prohibit restoring the spike spec; the current issue carries the complementary positive prescription (add the tool spec + widen the gate).
  Divergence verdict (fact, not prescription):
    - Issue AC and current code disagree on whether the tool's pub types are spec-gated.
    - Current code is consistent with the ff00b4a revert and the current `--code crates/` narrow invocation — internally consistent but structurally blind.
    - Issue AC was authored AFTER both `666a400` (tool) and `ff00b4a` (revert) and explicitly cites them in its Refs section.
    - Prescription has enough facts to decide CREATE (new tools-scoped spec directory + new spec file) vs REUSE (the reverted 15f7b7a content was subset-only — `PreludeTriggerReport` / `TriggerId` / `TriggerOutcome`; does not cover `LoadError` or `SCHEMA_VERSION`). The issue body enumerates a broader set than the reverted commit had.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
