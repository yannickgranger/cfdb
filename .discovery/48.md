━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DISCOVERY ARTIFACT — Issue #48 (agency:yg/cfdb)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue: feat(cfdb): :Finding classifier Cypher + 6-class taxonomy wiring (NO fix_skill field — DIP)
Date: 2026-04-20
Branch: work/48-finding-classifier
cfdb_keyspace_sha: 8547d85d1519

## Scope override (from .context/48.md)

verdict=current — no override

## Forbidden moves (from .context/48.md)

- DO NOT add `fix_skill` / `recommended_skill` / any skill-mapping column to `:Finding` — pinned by `crates/cfdb-query/tests/finding_no_skill_field.rs`.
- DO NOT route `ContextHomonym` to `/sweep-epic` anywhere.
- DO NOT bump `cfdb-core::SchemaVersion`.
- DO NOT re-implement `compute_bounded_context` / `WELL_KNOWN_PREFIXES` / `.cfdb/concepts/*.toml` loading — canonical lives in `cfdb-concepts` crate.
- DO NOT hide empty buckets — per-class warning naming the missing input is required.
- DO NOT reintroduce `class_v01_unavailable_note` in its v0.1 form.
- DO NOT add new ban rules under `.cfdb/queries/*.cypher` in this PR.
- DO NOT parallelize `/sweep-epic` for `RandomScattering` (route `/boy-scout`).
- DO NOT `git push` or create PRs outside `/ship`.
- DO NOT implement the RFC-§A2.2 rich `:Finding` schema (id / evidence[] / confidence / qnames[]) — scope is the existing Finding struct.
- DO NOT edit `.proofs/` files by hand.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DOCTRINAL CONTEXT — Issue #48
Source: .context/48.md Doctrinal layer (refreshed_sha=8547d85)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.1 (SECOND-PASS COUNCIL GREEN 2026-04-14) — 6-class taxonomy; fix-strategy column is abstract action, not Claude-skill name; `SkillRoutingTable` owns concrete mapping (council BLOCK-1 / solid-architect DIP).
- `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.2 (council BLOCK-1 clean-arch) — two-stage pipeline: enrichment passes (shipped via #43 sub-slices) THEN a Cypher query joining the enriched graph. Per-class `.cypher` rules are RFC-sanctioned because the v0.1 Cypher parser has no `UNION`/`CASE WHEN`; single-rule consolidation is v0.3.
- `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.2 Invariant I6 — classifier MUST NOT deploy until v0.2-9 accuracy gate met. Gate cleared at 100%/100% by PR #119.
- `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.3 guardrails — ContextHomonym NEVER `/sweep-epic`; UnfinishedRefactor → `/sweep-epic --mode=port`; RandomScattering → `/boy-scout`; Unwired → `/boy-scout` delete.
- `CLAUDE.md` §2.5 Tests template — Unit / Self dogfood / Cross dogfood / Target dogfood required.
- `CLAUDE.md` §3 Dogfood — classifier rules go under `examples/queries/`, NOT `.cfdb/queries/` (ban rules). No `SchemaVersion` bump (query-only addition).

No expansions beyond package needed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONCEPT INVENTORY — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`DebtClass`  (enum, 6 variants — §A2.1 canonical)
  DOMAIN:    crates/cfdb-query/src/inventory.rs:23 — `pub enum DebtClass` with `#[serde(rename_all = "snake_case")]`; variants Duplicated/Context/Unfinished/Random/Canonical/Unwired.
  DOMAIN:    crates/cfdb-query/src/inventory.rs:34 — `fn variants()` stable order.
  DOMAIN:    crates/cfdb-query/src/inventory.rs:47 — `fn as_str()` snake_case spelling ("duplicated_feature", …).
  DOMAIN:    crates/cfdb-query/src/inventory.rs:71 — `impl FromStr` + `UnknownDebtClass` error.

`Finding`  (structural row, v0.1 column set)
  DOMAIN:    crates/cfdb-query/src/inventory.rs:110 — `struct Finding { qname, name, kind, crate_name (#[serde(rename="crate")]), file, line, bounded_context }`.
  ARCH TEST: crates/cfdb-query/tests/finding_no_skill_field.rs:21 — `FORBIDDEN_FIELDS` = [fix_skill, skill, skill_name, routing, council_required, mode, concrete_skill] (uncommitted prior-session work); pins DIP invariant + exact 7-column set.

`ScopeInventory`
  DOMAIN:    crates/cfdb-query/src/inventory.rs:156 — `struct ScopeInventory { context, keyspace_sha, findings_by_class: BTreeMap<DebtClass, Vec<Finding>>, canonical_candidates, reachability_map: Option<_>, loc_per_crate, warnings }`.
  CTOR:      crates/cfdb-query/src/inventory.rs:171 — `ScopeInventory::new(context, ks_sha)` pre-seeds every `DebtClass` with empty `Vec<Finding>` (deterministic iteration).

`CanonicalCandidate` / `ReachabilityEntry`
  DOMAIN:    crates/cfdb-query/src/inventory.rs:126 / :139 — siblings of Finding; already populated or reserved-for-HIR.

`SkillRoutingTable` / `SkillRoute` / `SkillRoutingLoadError`
  DOMAIN:    crates/cfdb-query/src/skill_routing.rs:58 — `struct SkillRoutingTable { schema_version: u32, classes: BTreeMap<String, SkillRoute> }` (uncommitted prior-session work).
  DOMAIN:    crates/cfdb-query/src/skill_routing.rs:82 — `struct SkillRoute { skill: String, council_required: bool, mode: Option<String>, notes: Option<String> }`.
  ERRORS:    crates/cfdb-query/src/skill_routing.rs:94 — `enum SkillRoutingLoadError { Io, Toml }`. **Distinct name from the pre-existing `LoadError` in `cfdb-concepts/src/lib.rs:210`** — no split-brain surfaced.
  RE-EXPORT: crates/cfdb-query/src/lib.rs:29 — `pub use skill_routing::{SkillRoute, SkillRoutingLoadError, SkillRoutingTable}`.
  CONSUMER (docs only today): `/operate-module`, `/boy-scout --from-inventory` per `docs/cfdb-classifier.md`; none yet in-tree.

`.cfdb/skill-routing.toml`  (external policy file)
  SEED:      `.cfdb/skill-routing.toml` (uncommitted prior-session work) — 6 class rows; `schema_version = 1`; all council guardrails pre-baked (ContextHomonym → operate-module council_required=true; UnfinishedRefactor → sweep-epic mode=port; RandomScattering → boy-scout; CanonicalBypass → sweep-epic; Unwired → boy-scout; DuplicatedFeature → sweep-epic).

Classifier rules  (per-class .cypher files, §A2.2 per-rule decomposition sanctioned by parser gap)
  RULE 1:    examples/queries/classifier-duplicated-feature.cypher (uncommitted) — struct/enum/trait, same name, same bounded_context, is_test=false. Parameters: $context.
  RULE 2:    examples/queries/classifier-context-homonym.cypher (uncommitted) — fn/method, last_segment match, a.bounded_context=$context vs b.bounded_context<>$context, `signature_divergent(a.signature, b.signature) = true`.
  RULE 3:    examples/queries/classifier-unfinished-refactor.cypher (uncommitted — not read but referenced at scope.rs:33).
  RULE 4:    examples/queries/classifier-random-scattering.cypher (uncommitted) — fn/fn reachable_from_entry=true, shared name prefix via regexp_extract '^(\w+)_(?:from|to|for|as)_', divergent suffix, both bounded_context=$context, a.qname<b.qname dedup.
  RULE 5:    examples/queries/classifier-canonical-bypass.cypher (uncommitted) — `(i:Item)-[:CANONICAL_FOR]->(c:Concept)`, `i.reachable_from_entry=false`, i.bounded_context=$context. Surfaces CANONICAL_UNREACHABLE (generic, parameterless; per-concept BYPASS_REACHABLE/DEAD rules in canonical-bypass-{reachable,dead}.cypher are separate).
  RULE 6:    examples/queries/classifier-unwired.cypher (uncommitted — not read but referenced at scope.rs:41).

Classifier orchestrator
  ENTRY:     crates/cfdb-cli/src/main.rs:553 — `Command::Scope { db, context, workspace, format, output, keyspace } => scope(...)`.
  APP:       crates/cfdb-cli/src/scope.rs:67 — `pub fn scope(...)` → validate_context → build_scope_inventory → emit.
  APP:       crates/cfdb-cli/src/scope.rs:122 — `fn build_scope_inventory` — canonical_candidates (hsb-by-name) + `populate_findings_by_class` + attach_scope_warnings.
  APP:       crates/cfdb-cli/src/scope.rs:151 — `fn populate_findings_by_class` iterates `classifier_rules()`, writes `inventory.findings_by_class[class]` sorted + deduped (uncommitted).
  APP:       crates/cfdb-cli/src/scope.rs:171 — `fn classifier_rules() -> [(DebtClass, &'static str); 6]` static array; order matches `DebtClass::variants` (uncommitted).
  APP:       crates/cfdb-cli/src/scope.rs:196 — `fn run_classifier_rule` parses the embedded Cypher, binds `$context = PropValue::Str(context)`, runs `store.execute(ks, &parsed)`; treats store-level Err as "classifier rule cannot run against this keyspace" → `Ok(Vec::new())` (uncommitted).
  APP:       crates/cfdb-cli/src/scope.rs:489 — `fn finding_from_row(row) -> Option<Finding>` projector (v0.1 column set).
  WARNINGS:  crates/cfdb-cli/src/scope.rs:286 — `fn attach_scope_warnings` filters to classes with empty bucket, emits `Warning { kind: EmptyResult }` via `class_empty_bucket_note` (uncommitted rename from `class_v01_unavailable_note`).
  WARNINGS:  crates/cfdb-cli/src/scope.rs:546 — `fn class_empty_bucket_note(class) -> Option<String>` returns per-class guidance naming the likely missing input (HIR / enrich-concepts / enrich-reachability) (uncommitted).

Classifier-taxonomy test fixture + integration test
  INTEG:     crates/cfdb-cli/tests/classifier_taxonomy.rs:1 (uncommitted) — `#![cfg(feature = "hir")]`; drives full pipeline (`extract --hir`, `enrich-concepts`, `enrich-reachability`, `scope`) against a tempdir-copied fixture; 7 `#[test]` fns — one per class + composite-all-six.
  FIXTURE:   examples/queries/fixtures/classifier-taxonomy/ (uncommitted) — Cargo workspace with 4 crates: `trading_domain_a`, `trading_domain_b`, `trading_cli` (context=trading), `portfolio_domain` (context=portfolio). README documents which shape yields which class.

Producers of classifier inputs (all on develop — shipped)
  ADAPTER:   crates/cfdb-petgraph/src/enrich/bounded_context.rs — `:Item.bounded_context` (#108 / PR #119; v0.2-9 gate 100%/100%).
  ADAPTER:   crates/cfdb-petgraph/src/enrich/concepts.rs — `:Concept` nodes + LABELED_AS + CANONICAL_FOR edges (#109 / PR #121).
  ADAPTER:   crates/cfdb-petgraph/src/enrich/reachability.rs — `:Item.reachable_from_entry` BFS from `:EntryPoint` (#110 / PR #122).
  ADAPTER:   crates/cfdb-petgraph/src/enrich/git_history.rs — `:Item.git_last_commit_unix_ts` (#105 / PR #117; not used by v0.1 classifier rules).
  ADAPTER:   crates/cfdb-petgraph/src/enrich/rfc_docs.rs — `:RfcDoc` + REFERENCED_BY (#107 / PR #118; not used by v0.1 classifier rules).
  EXTRACTOR: crates/cfdb-extractor/src/attrs.rs — `:Item.is_deprecated` (#106 / PR #113).

UDFs used by classifier rules
  UDF:       crates/cfdb-petgraph/src/eval/predicate.rs:201 — `signature_divergent(a_sig, b_sig) -> bool` (#47 / PR #135). Consumed by classifier-context-homonym.cypher.
  UDF:       `last_segment(qname)` — path-tail helper. Consumed by classifier-context-homonym.cypher.
  UDF:       `regexp_extract(str, regex)` — consumed by classifier-random-scattering.cypher.

Ban-rule sibling (NOT a classifier)
  RULE:      examples/queries/hsb-by-name.cypher — Pattern A horizontal split-brain candidate rule; already seeds `canonical_candidates` in `scope.rs:263` on develop.

Pattern B / Pattern C rule siblings
  RULE:      examples/queries/vertical-split-brain.cypher — Pattern B (#44 / PR #133); not embedded in scope.rs today.
  RULE:      examples/queries/canonical-bypass-caller.cypher, canonical-bypass-reachable.cypher, canonical-bypass-dead.cypher, canonical-unreachable.cypher — Pattern C verdicts (#45 / PR #134).

Schema labels referenced by classifier rules
  LABEL:     crates/cfdb-core/src/schema/labels.rs:43 — `ENTRY_POINT = "EntryPoint"`.
  LABEL:     crates/cfdb-core/src/schema/labels.rs:104 — `LABELED_AS = "LABELED_AS"`.
  LABEL:     crates/cfdb-core/src/schema/labels.rs:105 — `CANONICAL_FOR = "CANONICAL_FOR"`.
  ATTR:      crates/cfdb-core/src/schema/describe.rs:184–185 — `reachable_entry_count` / `reachable_from_entry` (written by `enrich_reachability`).

### GLOBAL RESOLUTION CENSUS — DebtClass (FromStr)

- Resolution points found: 1 [OK]
  1. crates/cfdb-query/src/inventory.rs:72 — `impl FromStr for DebtClass` (CANONICAL; the only one).
- No CLI / MCP bypass — DebtClass is not parsed from user input in any CLI handler today (classifier rules bind the class via code, not user string). N/A for #48.

### GLOBAL RESOLUTION CENSUS — Finding (construction sites)

- Construction sites found: 3
  1. crates/cfdb-cli/src/scope.rs:490 — `finding_from_row(row)` — CANONICAL row → Finding projector.
  2. crates/cfdb-query/tests/finding_no_skill_field.rs:32 — `sample_finding()` — test helper.
  3. (no others) — all classifier rules route through `finding_from_row` via `run_classifier_rule` / `query_findings_in_context`.
- Single canonical construction site → OK.

### CONST TABLE CENSUS

- `HSB_BY_NAME_CYPHER` — 1 definition at scope.rs:22 (embedded via `include_str!`). Sibling `CLASSIFIER_*_CYPHER` constants at scope.rs:28–42 each defined once, all via `include_str!` of one file each — no duplication.
- `FORBIDDEN_FIELDS` (test-only) — 1 definition at finding_no_skill_field.rs:21. Test-only, not a split-brain.
- `.cfdb/skill-routing.toml` — 1 seed file. Test fixture `FULL_TABLE` in skill_routing.rs:154 is a test-only constant, distinct content, not a split.

### Split-brain pre-scan (scope-filtered)

Ran `audit-split-brain --workspace . --full-workspace --format json` on the worktree (including prior session's uncommitted classifier implementation):

- `const_table_overlaps`: empty
- `split_resolutions`: empty
- `alias_map_overlaps`: empty
- `match_arm_overlaps`: empty
- `fromstr_bypasses`: 1 entry — `Visibility::parse_syn_visibility` at `crates/cfdb-extractor/src/item_visitor.rs:674` (`delegates: true`) — pre-existing, out of #48 scope, per global CLAUDE.md §11 known-FP.
- `duplicate_type_defs`: 1 entry — `LoadError` at `crates/cfdb-concepts/src/lib.rs:210` + `tools/check-prelude-triggers/src/toml_io.rs:11` — pre-existing, out of #48 scope. The prior-session `SkillRoutingLoadError` uses a distinct name and does NOT compound this split.

### MCP/CLI BYPASS RISKS

No domain types with `impl FromStr` are touched by #48. The classifier rules are Cypher text consumed by `cfdb-query::parser::parse`; no MCP tool and no CLI command (other than `cfdb scope`) parses `DebtClass` from user input. N/A.

### PARAM CENSUS

No resolver/factory/consumer files in scope — `scope.rs` uses `$context` as a Cypher parameter but never calls `.get("...")` with dynamic param keys. N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALL CHAINS — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Chain 1: user runs `cfdb scope --db <d> --keyspace <ks> --context <c>` — intended new classifier path (prior-session uncommitted wiring)
  ENTRY  → crates/cfdb-cli/src/main.rs:553            Command::Scope dispatch
  APP    → crates/cfdb-cli/src/scope.rs:67             scope(db, context, …)
  APP    → crates/cfdb-cli/src/scope.rs:102            validate_context — queries `:Context` nodes
  APP    → crates/cfdb-cli/src/scope.rs:122            build_scope_inventory
  APP    → crates/cfdb-cli/src/scope.rs:263            query_canonical_candidates — runs embedded hsb-by-name
  APP    → crates/cfdb-cli/src/scope.rs:151            populate_findings_by_class — iterates classifier_rules()
  APP    → crates/cfdb-cli/src/scope.rs:196            run_classifier_rule — parse + bind $context + store.execute
  PORT   → crates/cfdb-query/src/parser.rs             parse(cypher) — v0.1 Cypher subset parser
  ADAPT  → crates/cfdb-petgraph/src/…                  PetgraphStore::execute(ks, &parsed)
  UDF    → crates/cfdb-petgraph/src/eval/predicate.rs:201  call_signature_divergent  (for Context Homonym rule)
  APP    → crates/cfdb-cli/src/scope.rs:489            finding_from_row — row → Finding
  APP    → crates/cfdb-cli/src/scope.rs:286            attach_scope_warnings — empty buckets get `class_empty_bucket_note`
  APP    → emit_scope_output                           JSON serialize → stdout/file

Chain 2: existing pre-#48 scope path on develop (pre-uncommitted-wiring)
  ENTRY  → crates/cfdb-cli/src/main.rs:553            Command::Scope dispatch
  APP    → crates/cfdb-cli/src/scope.rs:67             scope(…)
  APP    → crates/cfdb-cli/src/scope.rs:122            build_scope_inventory — canonical_candidates populated; findings_by_class left empty; attach_scope_warnings emits 6× `class_v01_unavailable_note`.
  (Classifier rules NOT embedded, populate_findings_by_class does NOT exist on develop.)

No decorator chains. No fan-out beyond the classifier orchestrator — `run_classifier_rule` is the single caller of `store.execute` in the classifier path.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DATA FLOW TRACE — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`$context` (CLI string → Cypher parameter)
  ENTRY:     crates/cfdb-cli/src/main.rs:553 — CLI arg `--context <name>`
  VALIDATE:  crates/cfdb-cli/src/scope.rs:102 — validate_context checks against `:Context` nodes in the keyspace
  [CONVERGE] crates/cfdb-cli/src/scope.rs:196 — `run_classifier_rule` binds $context into every rule's `parsed.params` via `Param::Scalar(PropValue::Str(context.to_string()))`
  ENGINE:    crates/cfdb-petgraph/src/… — PetgraphStore evaluator resolves `$context` predicate binding

Single resolution point — no fork, no diverge. OK.

Classifier rule source (embedded Cypher strings → parsed AST → executed query)
  ENTRY:     crates/cfdb-cli/src/scope.rs:28–42 — `include_str!` of `examples/queries/classifier-*.cypher` (6 files)
  [CONVERGE] crates/cfdb-cli/src/scope.rs:196 — `parse(cypher)` → `parsed: Query`
  ENGINE:    `store.execute(ks, &parsed)` — returns `Result<Vec<Row>>`
  [CONVERGE] crates/cfdb-cli/src/scope.rs:489 — `finding_from_row(row) -> Option<Finding>` → `Vec<Finding>`
  [CONVERGE] crates/cfdb-cli/src/scope.rs:163 — extend + sort + dedup bucket

Single construction pipeline for each rule — no fork, no diverge. OK.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALLER CENSUS — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`finding_from_row` — 2 production callers:
  1. crates/cfdb-cli/src/scope.rs:225 — `run_classifier_rule` (NEW in this PR, uncommitted)
  2. crates/cfdb-cli/src/scope.rs:247 — `query_findings_in_context` (pre-existing on develop, for `canonical_candidates` derivation)

`Finding` struct — 1 canonical construction site (`finding_from_row`) plus 1 test helper (`sample_finding`).

`SkillRoutingTable::from_path` — 0 production callers today (the TOML is shipped but consumers are `/operate-module` and `/boy-scout --from-inventory`, neither yet in-tree). Documented consumer surface in `docs/cfdb-classifier.md`.

`classifier_rules` — 1 caller (`populate_findings_by_class`) — OK.

No split-brain. No multi-site construction of `Finding` outside the canonical projector.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECORATOR CHAINS — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No decorator chains in this codebase. cfdb is a library with a CLI, not a trading system with composed executors. Classifier rules are embedded text constants, not wrapped trait impls.

Trait: `cfdb_core::StoreBackend` — one production impl (`PetgraphStore`). No decorators.
Trait: `cfdb_core::EnrichBackend` — one production impl (`PetgraphStore`). No decorators.

N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNERSHIP MAP — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`ScopeInventory::findings_by_class[class]` bucket
  Writer (intended, uncommitted): crates/cfdb-cli/src/scope.rs:151 `populate_findings_by_class` — canonical owner.
  Writer (pre-existing on develop): none — buckets stay empty, 6× warnings emitted instead.
  Fan-out: 1 (expected: 1) — OK.

`.cfdb/skill-routing.toml` parsing
  Canonical parser: `SkillRoutingTable::from_path` / `SkillRoutingTable::from_toml_str` in `crates/cfdb-query/src/skill_routing.rs:118` (uncommitted).
  No competing parser. Fan-out: 0 today; 1 intended (consumer skills). OK.

`:Finding` schema drift gate
  Owner: `crates/cfdb-query/tests/finding_no_skill_field.rs` (uncommitted).
  Pins: FORBIDDEN_FIELDS (7 names) + exact 7-key set via `serde_json` round-trip.
  Fan-out: 1 arch test protecting 1 struct definition. OK.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCOPE CLASSIFICATION — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Existing files touched by the issue's domain concepts (from call chain traces):
  DOMAIN/QUERY: crates/cfdb-query/src/inventory.rs:156 — ScopeInventory / Finding / DebtClass (already on develop).
  DOMAIN/QUERY: crates/cfdb-query/src/skill_routing.rs — SkillRoutingTable (new module, uncommitted).
  DOMAIN/QUERY: crates/cfdb-query/src/lib.rs:15–29 — re-exports (uncommitted diff adds `skill_routing` module + 3 re-exports).
  APPLICATION:  crates/cfdb-cli/src/scope.rs — classifier orchestrator + warnings rename (uncommitted +208/-31).
  TESTS (ARCH): crates/cfdb-query/tests/finding_no_skill_field.rs — DIP invariant (uncommitted, new file).
  TESTS (INTG): crates/cfdb-cli/tests/classifier_taxonomy.rs — classifier end-to-end (uncommitted, new file, behind `--features hir`).
  RULES:        examples/queries/classifier-*.cypher — 6 new files (uncommitted).
  POLICY:       .cfdb/skill-routing.toml — 1 new file (uncommitted).
  DOCS:         docs/cfdb-classifier.md — narrative (uncommitted, new file).
  FIXTURE:      examples/queries/fixtures/classifier-taxonomy/ — 4-crate synthetic workspace (uncommitted).

Layers containing existing code relevant to the issue: DOMAIN/QUERY + APPLICATION (CLI orchestrator) + TESTS + RULES (Cypher) + POLICY (TOML) + FIXTURE.

Gate sequence (derived from layers — NOT from intuition):
  Gate 1 (BDD / Intent):      N/A for cfdb — pure library + CLI, no Podman / HTTP. The classifier-taxonomy integration test in `crates/cfdb-cli/tests/classifier_taxonomy.rs` is the equivalent intent surface (drives the full pipeline via `assert_cmd`).
  Gate 2 (Contract / Semantic Wall): CONDITIONAL — `SkillRoutingTable` / `SkillRoute` / `SkillRoutingLoadError` are new public types in `cfdb-query`; the architecture test in `finding_no_skill_field.rs` is the contract invariant. No new port trait is introduced.
  Gate 3 (Domain / Proof):    REQUIRED — `SkillRoutingTable` parse + route + missing_classes logic; DebtClass iteration order invariant; Finding column-set invariant. Unit tests already written in `crates/cfdb-query/src/skill_routing.rs` tests module + `finding_no_skill_field.rs`.
  Gate 4 (Reality):           REQUIRED — self-dogfood: `cfdb scope --context cfdb` on this tree must exit 0 with a non-error JSON envelope; at least DuplicatedFeature and UnfinishedRefactor buckets exercise non-HIR inputs. Integration test `classifier_taxonomy.rs` covers all 6 buckets on the synthetic fixture (HIR required).

cfdb CLAUDE.md test-matrix mapping:
  Unit:           DebtClass/Finding arch tests + SkillRoutingTable tests (unit).
  Self dogfood:   `cfdb scope --db .cfdb/db --keyspace cfdb --context cfdb` on cfdb's own tree.
  Cross dogfood:  `ci/cross-dogfood.sh` — query-only addition, no SchemaVersion bump, should remain zero-findings green against graph-specs-rust pinned SHA.
  Target dogfood: bucket sizes on qbot-core — report in PR body (not CI).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECISION ARCHAEOLOGY — Issue #48
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Concept: Rich `:Finding` schema (id / class / context / qnames[] / evidence / confidence)
  Issue AC claim: "`:Finding {id, class, context, qnames[], evidence, confidence}` populator wired up against the existing Finding struct"
  Current state:  `crates/cfdb-query/src/inventory.rs:110` — `Finding` carries 7 columns (qname, name, kind, crate_name, file, line, bounded_context). Fields `id`, `class`, `evidence`, `confidence` are NOT on the struct. The issue body's inline text explicitly says "the data shapes already exist in v0.1 … this issue WIRES UP the classifiers; the data shapes already exist in v0.1".
  Introduced by:  inventory.rs struct shape lands on develop via the `cfdb scope` keystone work (pre-#48; authored when the `ScopeInventory` JSON envelope was defined).
  Rationale (issue body, AC section intro): "the **data shapes already exist** in v0.1 … this issue WIRES UP the classifiers". The richer schema is an aspirational description of the taxonomy's *ideal* final form (RFC §A2.2) — NOT a directive to redesign the struct in #48.
  RFC support:    `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.2 shows a "§A2.2 `:Finding` schema envisions richer rows" pattern — but the gate I6 text + the issue body's explicit scoping fall back on the existing struct.
  Addendum:       §A2.2 does not bump the schema to demand the richer form in v0.2; the addendum §A2 body talks about enrichment passes + query shape, not about Finding schema evolution.
  Parent briefing: Yes — context package Forbidden moves list pins "DO NOT implement the RFC-§A2.2 rich `:Finding` schema (id / evidence[] / confidence / qnames[]) in this PR" + cites prior-session `docs/cfdb-classifier.md` §"Follow-ups deferred to v0.3+".
  Divergence verdict (fact, not prescription):
    - Issue title's column list is the RFC-§A2.2 ideal shape.
    - Issue body's AC-2 text refers to "the EXISTING Finding struct" — present-tense structural constraint.
    - Current struct on develop is the v0.1 7-column shape.
    - The title and AC-2 text do NOT strictly agree; the body's qualifier "populator wired up against the EXISTING Finding struct" is the load-bearing phrase that resolves the ambiguity toward the shipped struct.
    - Prior-session `docs/cfdb-classifier.md` line 166–169 defers rich fields to v0.3, consistent with the body's "existing struct" reading.

Concept: Per-class .cypher files vs single `classifier.cypher`
  Issue AC claim: "`examples/queries/classifier.cypher` (or per-class queries — decide at discovery)"
  Current state:  prior-session uncommitted work chose six per-class files (`classifier-{duplicated-feature,context-homonym,unfinished-refactor,random-scattering,canonical-bypass,unwired}.cypher`).
  Introduced by:  prior session on 2026-04-20 (uncommitted).
  Rationale (issue body + RFC §A2.2): RFC explicitly permits per-class or single-rule form; single-rule form is v0.3 contingent on `UNION` / `CASE WHEN` parser support, which cfdb-query's v0.1 parser does not ship (confirmed by absence of such productions in `crates/cfdb-query/src/parser.rs` — not grepped here but constant-table census of Cypher rules in this repo shows no existing UNION usage).
  RFC support:    §A2.2 — "This classifier is a **two-stage pipeline**: enrichment passes materialize signals into the graph as new edges/attributes, THEN a Cypher query joins on the enriched graph." Per-class decomposition is the parser-gap-respecting execution of this design.
  Addendum:       §A2.2 Invariant I6 — gate cleared.
  Parent briefing: Yes — context package says "decision permitted by issue body".
  Divergence verdict (fact, not prescription):
    - No divergence. The issue body sanctions either form and defers the choice to discovery.
    - The prior session's six-file form is consistent with the RFC text and the parser constraint.

Concept: `class_v01_unavailable_note` rename → `class_empty_bucket_note`
  Issue AC claim: (implicit in "replaces per-class warnings with real classifier results").
  Current state (develop): `class_v01_unavailable_note` emits "classifier unavailable for class X" for all 6 classes.
  Current state (worktree uncommitted): `class_empty_bucket_note` emits per-class "bucket is empty because …" naming the missing enrichment input when the classifier rule returned zero rows.
  Introduced by:  prior session on 2026-04-20 (uncommitted).
  Rationale:  context package Forbidden move #6 explicitly prohibits reintroducing the v0.1 warning form; §A2.2 requires empty-bucket provenance.
  RFC support: §A2.2 degradation semantics — "each classifier rule projects empty rows — not errors — when its required inputs are absent; the CLI orchestrator surfaces per-class warnings on empty buckets that name the likely missing input".
  Divergence verdict: no divergence. The rename is RFC-aligned.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## Final self-check

Re-read `.context/48.md` Forbidden moves list. Diffed against this artifact:

- `fix_skill` on Finding — artifact confirms it is absent on develop AND that the prior-session arch test pins it.
- ContextHomonym → /sweep-epic — artifact confirms prior-session skill-routing.toml seed routes to `operate-module` with `council_required = true`.
- SchemaVersion bump — artifact confirms no new labels/edges/attrs are introduced.
- Re-implementing `compute_bounded_context` — artifact confirms the classifier consumes `:Item.bounded_context` via Cypher, not via TOML parsing.
- Silent empty-bucket skip — artifact confirms `class_empty_bucket_note` fires on every empty bucket.
- v0.1 `class_v01_unavailable_note` form — artifact notes the rename.
- New `.cfdb/queries/*.cypher` ban rules — artifact confirms all new rules land under `examples/queries/classifier-*.cypher`.
- Parallelized sweep-epic for RandomScattering — artifact confirms TOML seed routes to `boy-scout`.
- Rich Finding schema — artifact's Decision Archaeology flags the issue-title-vs-body divergence and cites both the body's "existing struct" qualifier and the prior-session v0.3 deferral.
- `.proofs/` hand edits — N/A in this phase.
- `git push` outside `/ship` — N/A in this phase.

No contradictions. Artifact is cleared for emission.
