━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DISCOVERY ARTIFACT — Issue #227 (yg/cfdb)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue: HIR fn_name_and_qname should include impl target for methods
Date: 2026-04-24
Branch: work/227-hir-impl-qname
cfdb_keyspace_sha: 508b16463c4a

## Scope override (from .context/227.md)
verdict=current — no override.

## Forbidden moves (from .context/227.md)
- **Do not re-implement `normalize_impl_target`.** Canonical version at `crates/cfdb-core/src/qname.rs:145`. Call it; do not copy.
- **Do not re-implement `method_qname` formula.** Canonical at `crates/cfdb-core/src/qname.rs:61`. Route through it or recreate the exact same output (prefer routing).
- **Do not bump SchemaVersion.** Producer bug fix, not wire contract change. No graph-specs lockstep.
- **Do not delete the known-limitation doc block silently.** Remove it as part of this PR with a note that the limitation is closed.
- **Do not widen scope to the clap `#[derive(Parser)]` or `Subcommand` paths.** Those paths are separate emitters at `entry_point_emitter.rs:395-408` (struct/enum) that already use their type name correctly — they are NOT impl methods. This issue is narrowly about `fn_name_and_qname` for fns inside `impl` blocks.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DOCTRINAL CONTEXT — Issue #227
Source: .context/227.md Doctrinal layer (refreshed_sha=508b164)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- `docs/RFC-037-schema-producer-alignment.md` §3.1 — crate-ownership table for REGISTERS_PARAM. MCP `#[tool]` fn row is HIR-owned (entrypoint detector + REGISTERS_PARAM emitter live together in cfdb-hir-extractor); :Param node emission stays syn-side. Implication: HIR and syn MUST construct qname bit-identically or the cross-extractor `param_node_id(fn_qname, i)` target dangles.
- `crates/cfdb-hir-extractor/src/entry_point_emitter.rs:337-346` — inline "Known limitation (follow-up)" doc block naming this exact issue. The fix includes deleting that block.
- `CLAUDE.md` §2.5 — tests mandatory; issue body prescribes 4-row Tests: template.
- `CLAUDE.md` §5 — no SchemaVersion bump (wire format unchanged).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONCEPT INVENTORY — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`fn_name_and_qname` (HIR-side fn qname builder — the bug site)
  DOMAIN:  crates/cfdb-hir-extractor/src/entry_point_emitter.rs:410-418 — builds qname from module path + fn name only; omits impl target.

`function_qname` (HIR-side fn qname builder — canonical, handles impl+trait containers)
  DOMAIN:  crates/cfdb-hir-extractor/src/call_site_emitter.rs:257-301 — pub(crate) helper. Takes `hir::Function`, routes impl container through `normalize_impl_target` + `method_qname`; trait container through `trait_def.name()` + `method_qname`; free fns through `item_qname`.

`method_qname` (qname formula — canonical)
  DOMAIN:  crates/cfdb-core/src/qname.rs:61-68 — pure: `<module_qpath>::<impl_target>::<method_name>`.

`normalize_impl_target` (strip generics from impl target — canonical)
  DOMAIN:  crates/cfdb-core/src/qname.rs:145-158 — pure: strips `<...>` by depth tracking; trims trailing whitespace.

`item_qname` (fallback for non-method items)
  DOMAIN:  crates/cfdb-core/src/qname.rs:44-51 — pure: `<module_qpath>::<item_name>`.

`build_item_qname` (module-stack builder on HIR Module)
  DOMAIN:  crates/cfdb-hir-extractor/src/entry_point_emitter.rs:467-503 — helper shared by struct / enum / fn qname builders; does NOT understand impl containers (operates on `hir::Module`, not `hir::Function`).

`build_module_stack` (module-stack from HIR Function)
  DOMAIN:  crates/cfdb-hir-extractor/src/call_site_emitter.rs:307 — builds stack from `Function::module`. Used inside `function_qname`. File-private (`fn` not `pub(crate)`).

`emit_mcp_registers_param` (REGISTERS_PARAM edge emitter for MCP tools)
  DOMAIN:  crates/cfdb-hir-extractor/src/entry_point_emitter.rs:346-365 — consumer of `fn_qname`; constructs `param_node_id(fn_qname, index)` as dst.

Syn-side canonical impl-method qname (reference for "what HIR must match")
  DOMAIN:  crates/cfdb-extractor/src/item_visitor/visits.rs:140 — `let qname = method_qname(&self.module_stack, &target, &method);` where `target` came from `normalize_impl_target(render_type_string(&node.self_ty))` at line 113.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GLOBAL RESOLUTION CENSUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

fn-qname resolution points in HIR extractor: 2 [⚠️ SPLIT RESOLUTION]
  1. crates/cfdb-hir-extractor/src/call_site_emitter.rs:257 — `function_qname(sema, Function)` — CANONICAL (impl-aware, trait-aware).
  2. crates/cfdb-hir-extractor/src/entry_point_emitter.rs:410 — `fn_name_and_qname(sema, ast::Fn)` — BUGGY (module + fn name only).

This is the EXACT split-brain the issue describes. The two resolution points disagree on impl-method qnames.

method_qname resolution points across workspace: 2 [OK — both canonical]
  1. crates/cfdb-extractor/src/item_visitor/visits.rs:140 — syn-side canonical.
  2. crates/cfdb-hir-extractor/src/call_site_emitter.rs:288,295 — HIR-side canonical (CALLS edges).

normalize_impl_target resolution points: 1 [OK]
  1. crates/cfdb-core/src/qname.rs:145 — single canonical definition.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONST TABLE CENSUS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No const tables in scope — N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXISTING RESOLUTION POINTS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

audit-split-brain --full-workspace filtered to `entry_point_emitter|qname.rs`: zero findings. The split-brain census above (call_site_emitter vs entry_point_emitter for fn-qname) is not flagged by the tool because the two points have different function names (`function_qname` vs `fn_name_and_qname`) and do not share a concept literal the scanner inspects.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MCP/CLI BYPASS RISKS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No domain enum / FromStr changes — N/A. This is a qname-construction fix, not a parse-path fix.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PARAM CENSUS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No resolver/factory/consumer files in scope — Param Census N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALL CHAINS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Chain 1: HIR extractor visits an MCP `#[tool]` fn during entry-point scan
  ENTRY: crates/cfdb-hir-extractor/src/entry_point_emitter.rs:183  `SyntaxKind::FN` match arm
    → BRANCH: entry_point_emitter.rs:185 `has_tool_attr(&fn_ast)` guard (true for MCP tool fn)
      → CALL: entry_point_emitter.rs:186 `fn_name_and_qname(sema, &fn_ast)` ⚠️ BUG SITE
        → CALL: entry_point_emitter.rs:415 `sema.to_def(fn_ast)` returns `hir::Function`
        → CALL: entry_point_emitter.rs:416 `build_item_qname(sema, def.module(...), def.krate(...), &name)` — builds module-only qname
        → RETURN: `(name, "mod::method")` — missing impl target when fn is inside an impl block
      → CALL: entry_point_emitter.rs:187 `emit(...)` — emits `:EntryPoint` with `handler_qname = "mod::method"`
      → CALL: entry_point_emitter.rs:202 `emit_mcp_registers_param(&qname, &fn_ast, edges)`
        → CALL: entry_point_emitter.rs:360 `param_node_id(fn_qname, syn_index)` — constructs `"param:mod::method#i"`
        → PUSH: edges.push(Edge { src: "entrypoint:mcp_tool:mod::method", dst: "param:mod::method#i", ... })

Chain 2 (reference): Syn extractor visits the same fn inside `impl Foo { fn method }`
  ENTRY: crates/cfdb-extractor/src/item_visitor/visits.rs:113 — captures `target = normalize_impl_target(render_type_string(&node.self_ty))`
    → CALL: visits.rs:140 `method_qname(&self.module_stack, &target, &method)` → `"mod::Foo::method"`
    → CALL: emit::emit_param — emits `:Param` nodes with id `param:mod::Foo::method#i` (matches `param_node_id(method_qname_result, i)`)

⚠️ Divergence: Chain 1 constructs `"param:mod::method#i"`; Chain 2 emits `"param:mod::Foo::method#i"`. On ingest, the HIR REGISTERS_PARAM edge's `dst` does not match any `:Param` node id and is silently dropped.

Chain 3 (canonical HIR pattern already in the repo): `call_site_emitter::function_qname` handles this correctly
  ENTRY: crates/cfdb-hir-extractor/src/call_site_emitter.rs:257 `function_qname(sema, Function)`
    → CALL: build_module_stack(db, func) → stack
    → CALL: line 273 `AsAssocItem::as_assoc_item(func, db)` → `Option<AssocItem>`
      → BRANCH: AssocItemContainer::Impl(impl_block) @ line 276
        → CALL: impl_block.self_ty(db).display(db, display_target).to_string() → raw rendering
        → CALL: normalize_impl_target(&rendered) → target
        → RETURN: method_qname(&stack, &target, &fn_name)
      → BRANCH: AssocItemContainer::Trait(trait_def) @ line 290
        → CALL: trait_def.name(db).display_no_db(...).to_string() → target
        → RETURN: method_qname(&stack, &target, &fn_name)
    → ELSE (no assoc item — free fn): item_qname(&stack, &fn_name)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DATA FLOW TRACE — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No param/config concepts — data flow trace N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALLER CENSUS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

fn_name_and_qname (file-private `fn` in entry_point_emitter.rs) — 2 production callers:
  1. crates/cfdb-hir-extractor/src/entry_point_emitter.rs:186 — MCP tool fn detection branch (BUG path for impl methods).
  2. crates/cfdb-hir-extractor/src/entry_point_emitter.rs:461 — called from `enclosing_fn_name_and_qname` (ancestor-walk helper used by cron_job + websocket close-over-enclosing-fn fallback).

  ⚠️ Caller #2 also inherits the bug: if a cron_job or websocket handler closure lives inside an `impl Foo { fn handler() { ... JobScheduler::add(Job::new_async(...)) ... } }`, the enclosing-fn qname will also omit the impl target. This is out-of-scope for the narrow fix per the Forbidden moves list ("do not widen scope"), BUT fixing `fn_name_and_qname` at its single definition site fixes both callers simultaneously — a point the prescription agent should note.

function_qname (pub(crate) canonical HIR fn qname) — 3 production callers:
  1. crates/cfdb-hir-extractor/src/call_site_emitter.rs:179 — :CallSite emission (callee qname).
  2. crates/cfdb-hir-extractor/src/call_site_emitter.rs:244 — resolved-dispatch qname.
  3. crates/cfdb-hir-extractor/src/entry_point_emitter/http_route.rs:254 — HTTP route handler resolution via `Semantics::resolve_path`.

method_qname (public helper in cfdb-core) — 4 callers (confirmed earlier by grep):
  1. crates/cfdb-extractor/src/item_visitor/visits.rs:140
  2. crates/cfdb-hir-extractor/src/call_site_emitter.rs:288
  3. crates/cfdb-hir-extractor/src/call_site_emitter.rs:295
  4. test assertions in qname.rs only.

normalize_impl_target — 3 callers:
  1. crates/cfdb-extractor/src/item_visitor/visits.rs:113
  2. crates/cfdb-hir-extractor/src/call_site_emitter.rs:287
  3. test assertions only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECORATOR CHAINS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No trait-based decorator chains in scope — both resolution functions are free functions on file-private types.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNERSHIP MAP — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

qname-from-Function (HIR side) — canonical owner: `call_site_emitter::function_qname`.
  Called from: call_site_emitter.rs:179, 244; http_route.rs:254.
  Fan-out: 3 (all acting on `hir::Function` — the canonical input type).

qname-from-ast::Fn (HIR side) — current owner: `entry_point_emitter::fn_name_and_qname`.
  Called from: entry_point_emitter.rs:186, 461.
  Fan-out: 2 (both acting on `ast::Fn` — the ast-node input type).
  ⚠️ Parallel abstraction to `function_qname` with the same semantic goal. The canonical owner exists but is unreachable from this call site because the caller has `ast::Fn`, not `hir::Function`. Bridge: `sema.to_def(ast::Fn)` returns `Option<hir::Function>` — this is the bridge the bug fix can use to route through the canonical owner.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCOPE CLASSIFICATION — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Existing files touched by the issue's domain concepts (from call chain traces):
  DOMAIN (pure formula): crates/cfdb-core/src/qname.rs:44-158 — `item_qname`, `method_qname`, `normalize_impl_target`. Pure functions; no change needed.
  ADAPTER (producer): crates/cfdb-hir-extractor/src/entry_point_emitter.rs:410-418 — bug site.
  ADAPTER (canonical reference): crates/cfdb-hir-extractor/src/call_site_emitter.rs:257-301 — canonical qname builder (pub(crate) already — reachable).
  TEST FIXTURE: crates/cfdb-hir-extractor/tests/fixtures/entry_points/mcp_fx/src/lib.rs — currently has only free-fn MCP tools; needs impl-method fixture.
  TEST: crates/cfdb-hir-extractor/tests/entry_point.rs:100+ (existing MCP test) + :100-160 ranges; needs new test asserting impl-method qname.

Layers containing existing code relevant to the issue: ADAPTER (cfdb-hir-extractor) only. Pure qname helpers in cfdb-core are already correct and will not be edited.

Gate sequence (derived from layers):
  Gate 1 (BDD):      N/A — cfdb is a pure library, no BDD infra; work-issue-lib skips Gate 1.
  Gate 2 (Contract):  N/A — no port/interface signature change.
  Gate 3 (Domain):    REQUIRED — unit test asserting qname shape on impl-method fixture.
  Gate 4 (Reality):   REQUIRED — dogfood asserting REGISTERS_PARAM count on qbot-core is non-zero post-fix.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXPERT VERDICTS — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No expert sub-agents spawned. The ambiguity bar for spawning (fan-out > 2 on a port, business rules in adapter, split backtest/live) is not triggered: the bug and the canonical pattern are both present in the same file-neighborhood; the fix is mechanical bridging.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECISION ARCHAEOLOGY — Issue #227
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Concept: Impl target inclusion in HIR fn qname
  Issue AC claim: "Extend `fn_name_and_qname` to detect when the fn's HIR container is an impl and include the normalized impl target."
  Current state:  crates/cfdb-hir-extractor/src/entry_point_emitter.rs:410-418 builds qname via `build_item_qname(module, krate, name)`, no impl lookup.
  Introduced by:  commit `ccac5fb feat(rfc-037): REGISTERS_PARAM 3-paths + TYPE_OF producer (#219 + #220)` (merged 2026-04-23) shipped the REGISTERS_PARAM producer AND the inline "Known limitation" doc block at entry_point_emitter.rs:337-346 acknowledging this gap.
  Rationale:      The MCP row of RFC-037 §3.1's crate-ownership table assigned the REGISTERS_PARAM emitter to HIR. The shipper documented the qname divergence as a known limitation rather than blocking the PR on it. Issue #227 is the follow-up.
  RFC support:    docs/RFC-037-schema-producer-alignment.md §3.1 — no textual directive on impl-method qname shape; the requirement is implicit in the single-producer discipline ("cross-extractor `HAS_PARAM` / `REGISTERS_PARAM` edges target the same node id" per cfdb-core/src/qname.rs:85).
  Parent briefing: yes — context package forbidden moves enumerate normalization routing and warn against widening scope.
  Divergence verdict (fact):
    - Issue AC and current code disagree on including the impl target in the HIR-built fn qname.
    - The canonical pattern for the correction ALREADY EXISTS in the same crate at `call_site_emitter::function_qname` (lines 257-301). It accepts `hir::Function` and handles Impl + Trait containers correctly via `AsAssocItem::as_assoc_item`.
    - The bridge from `ast::Fn` (what `fn_name_and_qname` has) to `hir::Function` (what `function_qname` needs) is `sema.to_def(fn_ast)` — already called on entry_point_emitter.rs:415.
    - No commit has closed the gap between the two parallel qname builders; this issue is the explicit trigger.
    - Prescription has enough facts to decide REUSE (route through `function_qname`) vs EXTEND (copy the impl-aware branch into `fn_name_and_qname`) without reading the issue body in isolation.

Concept: `function_qname` visibility (pub(crate))
  Current state:  call_site_emitter.rs:257 `pub(crate) fn function_qname<DB>(sema: &Semantics<'_, DB>, func: Function) -> String`
  Rationale:      Comment at line 252 notes "pub(crate) so `crate::entry_point_emitter` can reuse the same formula when resolving `http_route` handler paths (Issue #124)." The helper was already intentionally shared with entry_point_emitter for HTTP route handler resolution (line 254 of http_route.rs).
  Divergence verdict (fact):
    - `function_qname` is ALREADY reachable from `entry_point_emitter`. No visibility change required.
    - The helper's existing comment explicitly welcomes cross-module reuse within the crate.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
