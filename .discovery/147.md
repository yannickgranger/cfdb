## Scope override (from .context/147.md)
verdict=current — no override

## Forbidden moves (from .context/147.md)

- Do NOT leave `#![allow(dead_code)]` in `crates/cfdb-cli/src/param_resolver.rs`. The allow's comment at lines 31-36 explicitly states removal is part of THIS slice's diff.
- Do NOT re-implement the CLI-arg parsing logic. Call the Slice-1 `resolve_params` directly.
- Do NOT re-implement the parse/execute plumbing. `parse_and_execute()` at `commands.rs:636` is the shared entry point.
- Do NOT introduce a UDF; do NOT modify `cfdb-petgraph/src/eval/`; do NOT add `cfdb-concepts` as a dep of `cfdb-query`; do NOT add template composition; do NOT add a `--baseline`/`--allowlist`/`--ceiling` flag.
- Do NOT use the `cfdb violations` verb path for predicate dispatch (different contract).
- Do NOT name a new `LoadError` variant on `CfdbCliError`; map via `Usage(String)` or an existing `From` impl.
- Do NOT touch drift god-files (#151's scope).
- Do NOT bump `SchemaVersion`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DISCOVERY ARTIFACT — Issue #147 (agency:yg/cfdb)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue: feat(cfdb-cli): check-predicate verb — dispatch named predicate with resolved params (RFC-034 Slice 3)
Date: 2026-04-21
Branch: work/147-check-predicate
cfdb_keyspace_sha: fa3be5352df3

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DOCTRINAL CONTEXT — Issue #147
Source: .context/147.md Doctrinal layer (refreshed_sha=fa3be53)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

(verbatim copy of .context/147.md §Doctrinal layer — see that file for full quotes)

Load-bearing anchors used in Step 5 scope classification:
- `docs/RFC-034-query-dsl.md` §3.1 — locks `check_predicate` / `PredicateRunReport` / `PredicateRow` signatures with `pub` visibility
- `docs/RFC-034-query-dsl.md` §7 Slice 3 — "`main_dispatch.rs` — new dispatch arm into **`dispatch_typed`**" (RFC-choice, overrides any intuition that would put it in dispatch_core alongside Check/Violations)
- `council/49/RATIFIED.md` §9 — verb owner = cfdb-cli; `pub` re-exports at crate root
- `crates/cfdb-cli/src/param_resolver.rs:31-36` — inline comment mandates this slice removes `#![allow(dead_code)]`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONCEPT INVENTORY — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`check_predicate` fn (ENTRY — to be created by this slice)
  NOT FOUND — `rg "fn check_predicate" crates/ --glob '*.rs'` returns zero production hits. Only references are in `docs/RFC-034-query-dsl.md`, `council/49/*.md`, `.context/147.md`, and the `param_resolver.rs:1` module-doc first line ("`--param <name>:<form>:<value>` CLI-arg resolver for `cfdb check-predicate`.").
  Searches exhausted: `rg "check_predicate"`, `rg "CheckPredicate"`, `rg "pub fn check_predicate"`.

`PredicateRunReport` struct (ENTRY — to be created)
  NOT FOUND — zero production hits.
  Searches exhausted: `rg "PredicateRunReport"`, `rg "struct.*Report.*predicate"`, `rg "RunReport"`.

`PredicateRow` struct (ENTRY — to be created)
  NOT FOUND — zero production hits.
  Searches exhausted: `rg "PredicateRow"`, `rg "struct.*Row.*predicate"`, `rg "pub struct Row"` (matched unrelated ones only).

`Command::CheckPredicate` variant (ENTRY — to be created)
  NOT FOUND — `grep "CheckPredicate" crates/cfdb-cli/src/main_command.rs` returns zero. The existing `enum Command` at `main_command.rs:16-409` has variants: Extract, Query, Violations, Check (#101 trigger verb, NOT the new `check-predicate`), Dump, Export, ListKeyspaces, FindCanonical, ListCallers, ListBypasses, ListItemsMatching, Scope, Snapshots, Diff, Drop, Version, SchemaDescribe, and enrich-* subcommands via `Command::Enrich { verb: EnrichVerb }`. No `CheckPredicate`.

`resolve_params` fn (REUSE — shipped in Slice 1 / #145)
  LIB (pub(crate)):  crates/cfdb-cli/src/param_resolver.rs:117 — `pub(crate) fn resolve_params(workspace_root: &Path, cli_args: &[String]) -> Result<BTreeMap<String, Param>, ParamResolveError>`
  LIB (pub(crate)):  crates/cfdb-cli/src/param_resolver.rs:89  — `pub(crate) fn resolve_param(workspace_root: &Path, cli_arg: &str) -> Result<(String, Param), ParamResolveError>` (single-arg variant)
  CALLERS (production): zero today — the `#![allow(dead_code)]` allow at `param_resolver.rs:36` exists BECAUSE this slice is its first production consumer.
  Slice 3 consumer path: `use crate::param_resolver::resolve_params;` inside the new `check_predicate.rs`.

`ParamResolveError` enum (REUSE — shipped in Slice 1)
  LIB (pub(crate)):  crates/cfdb-cli/src/param_resolver.rs:47 — `pub(crate) enum ParamResolveError` variants `UnknownForm`, `UnknownContext`, `Io`, `Toml`.
  From impl:  crates/cfdb-cli/src/param_resolver.rs:69 — `impl From<cfdb_concepts::LoadError> for ParamResolveError`.
  No `From<ParamResolveError> for CfdbCliError` today — verified via `rg "From<ParamResolveError>" crates/cfdb-cli/src/error.rs` → zero hits. Callers must map via `.to_string().into()` (hits `CfdbCliError::Usage` through the existing `From<String>` at `error.rs:56`).

`parse_and_execute` fn (REUSE — canonical cypher-rule plumbing)
  LIB (pub(crate)):  crates/cfdb-cli/src/commands.rs:636 — `pub(crate) fn parse_and_execute(db, keyspace, cypher, rule_tag) -> Result<cfdb_core::result::QueryResult, CfdbCliError>`
  Behavior: `parse(cypher) → shape_lint (logs warnings to stderr) → compose::load_store → store.execute(&ks, &parsed) → return QueryResult`. Used by `run_cypher_rule` (file-based violations) and by `check()` (#101 trigger dispatcher).
  Caveat: `parse_and_execute` takes a `cypher: &str` and does NOT pre-populate `Query::params` — consumers that need params MUST parse themselves + call `store.execute(&ks, &parsed)` directly (the pattern used by `list_callers` at `commands.rs:552-556`).

`run_cypher_rule` fn (similar but not directly reusable)
  LIB (pub(crate)):  crates/cfdb-cli/src/commands.rs:605 — handles parse+execute+3-column-text-print+count_only for the existing `violations` verb.
  Caveat: same — does NOT bind params. Since `check-predicate` needs param binding, it must replicate the outer loop (parse → insert params → execute → print) rather than call `run_cypher_rule` directly.

`list_callers` fn (REUSE SHAPE — templated-verb precedent)
  LIB (pub):  crates/cfdb-cli/src/commands.rs:536 — `pub fn list_callers(db, keyspace, qname) -> Result<(), CfdbCliError>`. Shape: `parse(LIST_CALLERS_CYPHER)` → `parsed.params.insert("qname", Param::Scalar(PropValue::Str(qname)))` → `compose::load_store` → `store.execute(&ks, &parsed)` → `serde_json::to_string_pretty`.
  This is the STRUCTURAL precedent for `check_predicate` (same 4-step pipeline with different template source: CLI `--name` → `.cfdb/predicates/<name>.cypher` → `fs::read_to_string` — not `include_str!`).

`Query::params` (REUSE — AST binding site)
  DOMAIN:  crates/cfdb-core/src/query/ast.rs:41 — `pub params: BTreeMap<String, Param>`
  Binding precedent:  crates/cfdb-cli/src/commands.rs:509-517 (`bind_single_param`) + commands.rs:555 (`list_callers`) — both use `parsed.params.insert(k.to_string(), Param::Scalar(...))`.

`Command::Violations` variant (REUSE SHAPE — CLI-signature precedent)
  DEF:  crates/cfdb-cli/src/main_command.rs:342 — clap-derive variant with `db: PathBuf`, `keyspace: String`, `rule: PathBuf`, `no_fail: bool`, `count_only: bool`.
  Dispatch:  crates/cfdb-cli/src/main_dispatch.rs:33-44 — `let rows_found = violations(db, keyspace, rule, count_only)?; if rows_found > 0 && !no_fail { std::process::exit(1); } Ok(())`
  This is the EXIT-CODE precedent. Note: Violations lives in `dispatch_core`, not `dispatch_typed` (see ambiguity note below).

`Command::Check` variant (REUSE SHAPE — #101 trigger verb, peer of CheckPredicate)
  DEF:  crates/cfdb-cli/src/main_command.rs:380 — `Check { db, keyspace, trigger: TriggerId, no_fail: bool }`.
  Dispatch:  crates/cfdb-cli/src/main_dispatch.rs:46-56 — same exit-code pattern as Violations.
  Lives in `dispatch_core`.

`CfdbCliError::Usage` (REUSE — error escape hatch)
  DEF:  crates/cfdb-cli/src/error.rs:53 — `Usage(String)`; `From<String>` at error.rs:56 routes any `.into()` on a `String` into this variant.
  Used for ParamResolveError boundary: `resolve_params(..).map_err(|e| e.to_string().into())?` maps into `CfdbCliError::Usage`.

`.cfdb/predicates/<name>.cypher` (REUSE — shipped in Slice 2 / #146)
  DATA:  `.cfdb/predicates/` directory on develop @ `fa3be53` contains: `README.md`, `context-homonym-crate-in-multiple-contexts.cypher`, `fn-returns-type-in-crate-set.cypher`, `path-regex.cypher`.
  Predicate file path resolution: `<workspace_root>/.cfdb/predicates/<name>.cypher`. Verified via `ls` on the worktree.

`#![allow(dead_code)]` in param_resolver.rs (to be REMOVED by this slice)
  LINE:  crates/cfdb-cli/src/param_resolver.rs:36 — `#![allow(dead_code)]` inner attribute with a 6-line inline comment at lines 31-35 explicitly naming Slice 3 (#147) as the remover.

`cfdb-cli lib.rs re-export line` (EXTEND — add `pub use check_predicate::...`)
  DEF:  crates/cfdb-cli/src/lib.rs (22-32 range per §128 post-split) already re-exports `check, commands::*, enrich, error::CfdbCliError, hir (cfg-gated), scope, stubs::*`. Pattern: `pub use <module>::{<symbol>, ...};`. The new module re-exports the RFC-locked `pub` symbols `check_predicate`, `PredicateRunReport`, `PredicateRow`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GLOBAL RESOLUTION CENSUS — parseable concepts in scope
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`PredicateRunReport`
  Resolution points: 0 (no FromStr — it's a report struct, not parseable). Construction site: single — `check_predicate.rs` only. No split risk ✅.

`PredicateRow`
  Resolution points: 0. Construction site: single — produced by the sort+map step inside `check_predicate.rs`. No split risk ✅.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONST TABLE CENSUS — parseable concepts in scope
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No new const arrays. This slice does not add a registry; the four param forms are owned by the Slice-1 resolver's match arms.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXISTING RESOLUTION POINTS — Issue #147 (audit-split-brain --full-workspace, scope-filtered)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scope-filtered hits (crates/cfdb-cli/* + crates/cfdb-concepts/*):
- `TriggerId` FromStr bypass (cfdb-cli::check::TriggerId T1/T3 canonical at `check.rs:123`; delegation wrapper at `main_parse.rs:29` marked `delegates: true`). Out of scope for #147.
- `LoadError` duplicate type def (cfdb-concepts::LoadError at `lib.rs:216`; check-prelude-triggers::toml_io::LoadError at `toml_io.rs:11`). Out of scope — this slice does NOT name a LoadError.
- `TriggerId` duplicate type def (cfdb-cli T1/T3 vs check-prelude-triggers C1/C3/C7/C8/C9). Documented DDD homonym; irrelevant.

No NEW split risks introduced by this slice.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MCP/CLI BYPASS RISKS — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No domain-enum variants added or changed. The new `--param` CLI flag is already bound by the Slice-1 resolver (single canonical parser). No MCP server in cfdb. Sole CLI is `cfdb-cli`. N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PARAM CENSUS — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Files in scope: `crates/cfdb-cli/src/check_predicate.rs` (new) + `main_command.rs` + `main_dispatch.rs` + `lib.rs`. None match `resolver|factory|consumer|engine` or use `.get("key")` param-registry idioms. Param Census N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALL CHAINS — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Existing chains (reuse targets):

Chain A — existing `cfdb violations --rule <path>` (file-based cypher rule)
  ENTRY  → crates/cfdb-cli/src/main.rs → Cli::parse
  DISP   → crates/cfdb-cli/src/main_dispatch.rs:33 Command::Violations match arm
  VERB   → crates/cfdb-cli/src/commands.rs:580 violations(db, keyspace, rule, count_only)
  COMMON → crates/cfdb-cli/src/commands.rs:605 run_cypher_rule (parse+execute+print+count)
  COMMON → crates/cfdb-cli/src/commands.rs:636 parse_and_execute (parse+shape_lint+load_store+execute)
  ENGINE → crates/cfdb-petgraph (StoreBackend::execute) — unchanged by this slice

Chain B — existing `cfdb list-callers --qname <regex>` (templated verb with inline param binding)
  ENTRY  → main.rs → dispatch_typed
  DISP   → crates/cfdb-cli/src/main_dispatch.rs:82 Command::ListCallers match arm
  VERB   → crates/cfdb-cli/src/commands.rs:536 list_callers(db, keyspace, qname)
  BIND   → commands.rs:552 `parse(LIST_CALLERS_CYPHER)` (include_str! embedded template)
  BIND   → commands.rs:555 `parsed.params.insert("qname", Param::Scalar(PropValue::Str(qname)))`
  LOAD   → commands.rs:558 `compose::load_store(&db, &keyspace)`
  EXEC   → commands.rs:559 `store.execute(&ks, &parsed)`
  PRINT  → commands.rs:561 `serde_json::to_string_pretty(&result)`

Chain C (to be built by this slice) — `cfdb check-predicate --name X [--param ...]`
  ENTRY  → main.rs → dispatch_typed
  DISP   → (NEW) main_dispatch.rs dispatch_typed arm → Command::CheckPredicate
  VERB   → (NEW) check_predicate.rs::check_predicate — composes:
             1. fs::read_to_string(<workspace_root>/.cfdb/predicates/<name>.cypher)
             2. parse(cypher) (via cfdb_query::parse)
             3. parsed.params = resolve_params(workspace_root, &cli_params)? (Slice-1 resolver)
             4. compose::load_store(&db, &keyspace)
             5. store.execute(&ks, &parsed)
             6. sort rows by (qname, line) ascending for determinism
             7. format output: text (3-column) or JSON (PredicateRunReport)
             8. return row_count for exit-code contract

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DATA FLOW TRACE — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`--name <predicate-name>`
  ENTRY:     CLI via clap `--name <String>` on Command::CheckPredicate
  PARSE:     `<workspace_root>/.cfdb/predicates/<name>.cypher` → fs::read_to_string
  [CONVERGE] single read point in the new `check_predicate` fn
  ENGINE:    cfdb_query::parse → Query AST

`--param <name>:<form>:<value>` (vec of strings; repeatable)
  ENTRY:     CLI via clap `--param <String>` (repeatable; `num_args = 0..` or `Vec<String>`)
  PARSE:     `resolve_params(workspace_root, &args)` (Slice 1 resolver, pub(crate))
  [CONVERGE] single resolution point — RATIFIED §9 canonical authority
  ENGINE:    `parsed.params: BTreeMap<String, Param>` → evaluator consumes via `Expr::Param(name)` lookup

`--format text|json`
  ENTRY:     CLI via clap `--format <String>` (or custom enum)
  PARSE:     match on the string inside check_predicate; default to `text` if None
  OUTPUT:    text → 3-column (qname/line/reason) + stderr summary; json → PredicateRunReport via serde_json::to_string_pretty

No [FORK] / [DIVERGE] — single-path flow.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALLER CENSUS — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`resolve_params` — 1 production caller post-slice (`check_predicate.rs`). Zero before. No split risk.
`parse_and_execute` — 2 production callers today (`run_cypher_rule` and `check` for trigger T1). NOT called by #147 directly — `check_predicate` uses the lower-level sequence (`parse` → `params.insert` → `store.execute`) because `parse_and_execute` does NOT support pre-execute param binding. No split risk; parallel sequence, not redundant.
`list_callers` — 1 production caller (dispatch_typed). Shape precedent for check_predicate; not called by the new verb.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECORATOR CHAINS — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No trait-based decorator chains in scope. `StoreBackend` is a port (unchanged by this slice); `check_predicate` is a free function.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNERSHIP MAP — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`check-predicate` verb — canonical site: `crates/cfdb-cli/src/check_predicate.rs` (new) + re-export at `crates/cfdb-cli/src/lib.rs`. Fan-out: 1 dispatch arm (`main_dispatch.rs::dispatch_typed`). Forbidden: a second verb in qbot-core or another crate.

`.cfdb/predicates/*.cypher` read authority — canonical reader: `check_predicate.rs` + `cfdb_query::parse`. Consumers may NOT write their own `fs::read` + `cfdb_query::parse` loop; if future callers emerge, they reuse `check_predicate(...)` or its inner helper.

`--param` resolution — canonical resolver: `crates/cfdb-cli/src/param_resolver.rs::resolve_params` (REUSE). No other resolver exists; none may be created.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCOPE CLASSIFICATION — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Existing files touched by this slice's concepts (from call chain traces + inventory):
  ENTRY (cfdb-cli):   crates/cfdb-cli/src/main_command.rs:342,380 — existing Violations/Check variants as shape templates; new CheckPredicate variant adjacent
  ENTRY (cfdb-cli):   crates/cfdb-cli/src/main_dispatch.rs:70-118 — existing dispatch_typed; new arm adjacent to ListCallers/Scope
  ENTRY (cfdb-cli):   crates/cfdb-cli/src/lib.rs — existing pub use … ; new line for check_predicate module
  ENTRY (cfdb-cli):   crates/cfdb-cli/src/param_resolver.rs:36 — existing #![allow(dead_code)]; removed by this slice
  LIB (cfdb-cli):     crates/cfdb-cli/src/commands.rs:536 (list_callers shape), :636 (parse_and_execute plumbing — NOT called by this slice but serves as precedent)
  LIB (cfdb-core):    crates/cfdb-core/src/query/ast.rs:41 (Query::params binding site) + fact.rs (PropValue) — UNCHANGED
  LIB (cfdb-petgraph): crates/cfdb-petgraph/src/* — StoreBackend::execute consumed unchanged
  LIB (cfdb-concepts): UNCHANGED
  DATA:               .cfdb/predicates/*.cypher — shipped in #146; read-only consumer by this slice
  NEW (cfdb-cli):     crates/cfdb-cli/src/check_predicate.rs — new module file
  NEW (cfdb-cli):     crates/cfdb-cli/tests/check_predicate_dogfood.rs — new integration test for self-dogfood (path-regex predicate against real workspace)

Layers containing existing code relevant to this issue: ENTRY (cfdb-cli) + LIB (cfdb-cli commands.rs helpers as precedent only)

Gate sequence (derived from layers):
  Gate 1 (BDD):      NOT REQUIRED — no new user-facing journey-level behavior beyond the CLI verb; #148 Slice 4 adds the CI-level dogfood wiring.
  Gate 2 (Contract): NOT REQUIRED — no new port, no trait.
  Gate 3 (TDD):      REQUIRED — `check_predicate` is a pure-ish orchestrator (values in, values out, one fs read, one store.execute). Unit coverage: PredicateRunReport row-ordering (qname asc, line asc); JSON serialization shape; missing predicate file → structured Err; invalid cypher in a fixture predicate → parse-error Err.
  Gate 4 (Reality):  REQUIRED — the "self dogfood" integration test prescribed by RFC §7 Slice 3. Running `check_predicate(..., "path-regex", ["pat:literal:cfdb-query/.*\\.rs"])` against cfdb's own keyspace returns Param::List with ≥10 File rows (loose lower bound).
  Dogfood gate:      REQUIRED — Gate 4 IS the dogfood proof for this slice.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXPERT VERDICTS — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

None spawned. One ambiguity flagged (documented below in Decision Archaeology); no architect sub-agent needed because the RFC text is explicit.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECISION ARCHAEOLOGY — Issue #147
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Concept: dispatch_typed vs dispatch_core placement of Command::CheckPredicate
  Issue AC claim: `.prescriptions/147.md` (yet to be written) will follow RFC §7 which says: "`main_dispatch.rs` — new dispatch arm into **`dispatch_typed`**".
  Current state: `Command::Violations` (rule-runner, exit-code contract) + `Command::Check` (trigger runner, exit-code contract) both live in **`dispatch_core`** (main_dispatch.rs:33 + :46). `dispatch_typed` currently contains FindCanonical, ListCallers, ListBypasses, ListItemsMatching, Scope — "composer-over-Cypher shortcuts" per the comment at main_dispatch.rs:69. None of `dispatch_typed`'s existing arms use `std::process::exit(1)` for the rule-violation exit-code contract.
  Introduced by: `docs/RFC-034-query-dsl.md` §7 Slice 3 (PR #150, merged 2026-04-21) — explicit "dispatch_typed" choice ratified by council-49-query-dsl.
  Rationale: RFC-034 §7 Slice 3 verbatim: "`main_dispatch.rs` — new dispatch arm into `dispatch_typed`". The RFC author classified `check-predicate` as a typed convenience (named predicate + resolved params — conceptually adjacent to `list-callers` which is also dispatch_typed).
  RFC support: Explicit. No addendum contradicts.
  Parent briefing: YES — `.context/147.md` §Doctrinal layer cites this specifically.
  Divergence verdict (fact, not prescription):
    - The RFC's dispatch_typed choice is in tension with the in-codebase precedent (peer rule-runner verbs Violations/Check live in dispatch_core).
    - No commit reconciles the tension; RFC-034 is the later decision.
    - `dispatch_typed` return type is `Result<(), CfdbCliError>` — same as `dispatch_core` — so `std::process::exit(1)` inside a dispatch_typed arm is architecturally identical to inside dispatch_core. No correctness issue; only an idiom drift.
    - Prescription has enough facts to follow the RFC (dispatch_typed) and note the drift inline if desired.

Concept: `pub` vs `pub(crate)` surface for `check_predicate` / `PredicateRunReport` / `PredicateRow`
  Issue AC claim: issue-body Scope: "`pub fn check_predicate`" + "`pub struct PredicateRunReport`" + "`pub struct PredicateRow`" + "Re-export check_predicate via cfdb-cli lib".
  Current state: Slice 1 (#145) established `pub(crate)` surface for `resolve_params` + `ParamResolveError` per RATIFIED §9 "single module, `pub(crate)` surface"; the `param_resolver.rs:36 #![allow(dead_code)]` exists because that module has no cross-crate consumer on develop today.
  Introduced by: `council/49/RATIFIED.md` §9 (ratified 2026-04-21). RFC-034 §3.1 shows `pub fn check_predicate(...)` in the Rust code block.
  RFC support: RFC §3.1 code block explicitly `pub` — NOT `pub(crate)` — for check_predicate + PredicateRunReport + PredicateRow. This IS consistent with the issue body. In contrast, the Slice 1 resolver is `pub(crate)` because it is an internal helper; Slice 3's verb handler is the external API surface for integration tests + future skill consumers.
  Parent briefing: YES — `.context/147.md` §Doctrinal layer quotes RFC §3.1 verbatim.
  Divergence verdict (fact, not prescription):
    - Slice 1 and Slice 3 differ DELIBERATELY on visibility — Slice 1's module is internal plumbing (pub(crate)); Slice 3's verb handler is the external surface (pub + lib.rs re-export).
    - No precedent conflict: `list_callers` at commands.rs:536 is `pub` and is re-exported at lib.rs:23 via `pub use commands::{..., list_callers, ...};`. Same pattern for check_predicate.
    - Prescription follows RFC §3.1 verbatim (pub) and adds the lib.rs re-export line.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
