━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DISCOVERY ARTIFACT — Issue #101 (agency:yg/cfdb)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue: feat(cfdb): Trigger T1 — concept-declared-in-toml-but-missing-in-code query + CLI surface
Date: 2026-04-21
Branch: work/101-trigger-t1
cfdb_keyspace_sha: fe8b8901a373

## Scope override (from .context/101.md)

### IN (actionable this PR)
- AC-1: Land `examples/queries/t1-concept-unwired.cypher` implementing the three sub-verdicts (`CONCEPT_UNWIRED`, `MISSING_CANONICAL_CRATE`, `STALE_RFC_REFERENCE`) against the enriched graph (`:Concept` + `:Item` + `:Crate`).
- AC-3: Exit code = 1 iff findings, 0 when clean (consistent with `cfdb violations` pattern).
- AC-4: Synthetic fixture + integration test asserting exactly 2 findings against a crafted `.cfdb/concepts/<ctx>.toml`.
- AC-5: Self-dogfood proof — run T1 against cfdb's own tree; paste result in PR body (expect zero findings).
- **New CLI surface** `cfdb check --trigger T1` — thin composition over the existing `violations` plumbing, but with a dedicated verb so prescription/consumer skills can bind to `--trigger <ID>` as a stable contract rather than a cypher file path.

### OUT (already shipped differently — DO NOT re-implement)
- AC-2 "emit `:Finding` nodes in the configured store" — no graph-node emission path exists; queries return rows.
- Generic cypher-rule dispatch + exit-code wiring — already in `cfdb violations`.

### DEFERRED (route to another issue)
- AC-6 `/operate-module` consumption — gated on #50 merging.
- AC-7 `DebtClass` extension — frozen taxonomy.

## Forbidden moves (from .context/101.md)

- Do NOT add a 7th `DebtClass` variant.
- Do NOT add a `:Finding` graph node label or any query-execution-writes-to-store mechanism.
- Do NOT duplicate the `cfdb violations` cypher dispatch + exit-code plumbing.
- Do NOT bypass `cfdb-concepts::ConceptOverrides::load`.
- Do NOT bake `/operate-module` routing into the cypher or CLI.
- Do NOT skip the determinism `ORDER BY` clause in the new cypher.
- Do NOT add trigger-specific fields to `ScopeInventory`.
- Do NOT file follow-up issues for ambient debt surfaced during this work.
- Cross-dogfood must stay green; if the new cypher is added under `.cfdb/queries/` vs `examples/queries/`, prescription decides.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DOCTRINAL CONTEXT — Issue #101
Source: .context/101.md Doctrinal layer (refreshed_sha=fe8b8901a373)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.1 lines 182-189** — canonical 6-class `DebtClass` taxonomy. Implication: T1 sub-verdicts must NOT become a 7th class.
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A3.2 lines 290–292** — ownership declaration in `.cfdb/concepts/*.toml`. T1's input contract is the TOML file shape already consumed by `cfdb-concepts::ConceptOverrides::load`.
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A4.2** — live inventory + raid plan flow (#50 downstream). T1 JSON output shape must be consumable by `/operate-module`.
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A3.3 line 322** — *"cfdb does NOT emit the raid plan markdown. cfdb's responsibility ends at returning a structured infection inventory."* T1 returns JSON; `/operate-module` owns interpretation.
- **`CLAUDE.md` §3** — dogfood enforcement (cfdb violations + determinism + cross-dogfood). New T1 cypher MUST be byte-stable (ORDER BY required).
- **`CLAUDE.md` §2.5** — new-capability issues carry 4-row `Tests:` block.

No expanded quotes needed — package quotes were sufficient.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONCEPT INVENTORY — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**ConceptOverrides (TOML loader)**
  DOMAIN: crates/cfdb-concepts/src/lib.rs:77-108 — `pub struct ConceptOverrides { by_crate: BTreeMap<String, ContextMeta> }` with `lookup`, `declared_contexts`, `crate_assignments`.
  ENTRY loader: crates/cfdb-concepts/src/lib.rs:119-133 — `pub fn load_concept_overrides(workspace_root: &Path) -> Result<ConceptOverrides, LoadError>` scans `<workspace_root>/.cfdb/concepts/*.toml`.
  CONSUMER: crates/cfdb-extractor/src/lib.rs:55,103 — extractor calls `load_concept_overrides` at extract time.
  CONSUMER: crates/cfdb-petgraph/src/enrich/concepts.rs:48,61 — `enrich_concepts` calls `load_concept_overrides` at Phase D.

**ContextMeta (per-concept TOML payload)**
  DOMAIN: crates/cfdb-concepts/src/lib.rs:52-60 — `pub struct ContextMeta { name: String, canonical_crate: Option<String>, owning_rfc: Option<String> }`. These three props are exactly what T1's three sub-verdicts ask about.

**ConceptFile (TOML on-disk shape — private)**
  INTERNAL: crates/cfdb-concepts/src/lib.rs:62-72 — `struct ConceptFile { name, crates: Vec<String>, canonical_crate: Option<String>, owning_rfc: Option<String> }`. Private — not exposed; `load_concept_overrides` is the only reader.

**:Context graph node (TOML data materialized on extract)**
  EMITTED: crates/cfdb-extractor/src/lib.rs:122,206 — `emit_context_node(&mut emitter, name, meta)` emits `Label::CONTEXT` nodes with props `{name, canonical_crate?, owning_rfc?}`.
  SCHEMA: crates/cfdb-core/src/schema/describe.rs:357-368 — descriptor lists `canonical_crate`, `name`, `owning_rfc` as Context attrs (Provenance::Extractor).
  LABEL: crates/cfdb-core/src/schema/labels.rs — `pub const CONTEXT`.
  LIVE CHECK: `cfdb query --db .cfdb/db --keyspace cfdb 'MATCH (c:Context) RETURN c.name, c.canonical_crate, c.owning_rfc'` returns one row: `{name: "cfdb", canonical_crate: "cfdb-core", owning_rfc: "RFC-029"}`.

**:Concept graph node (enrich_concepts output)**
  EMITTED: crates/cfdb-petgraph/src/enrich/concepts.rs:151-170 — `build_concept_nodes` emits nodes with ONLY `name` + `assigned_by` props. No `canonical_crate`, no `owning_rfc`.
  SCHEMA: crates/cfdb-core/src/schema/describe.rs:333-355 — `concept_node_descriptor` attrs: `assigned_by` (enum: doc/rule/llm/manual), `name` (string). Provenance::EnrichConcepts.
  LABEL: crates/cfdb-core/src/schema/labels.rs:44 — `pub const CONCEPT`.
  LIVE CHECK: on a BARE extract (no `cfdb enrich-concepts` step), `MATCH (c:Concept)` returns zero rows — the evaluator warns "unknown node label: Concept, did you mean `Context`?". Materializes only after `cfdb enrich-concepts --db .cfdb/db --keyspace cfdb --workspace .` is run.

**:Crate graph node**
  LABEL: crates/cfdb-core/src/schema/labels.rs:18 — `pub const CRATE`.
  EMITTED: by extractor for every workspace package. LIVE CHECK confirms `:Crate { name: "cfdb-core" }`, etc. are present.

**:RfcDoc graph node**
  EMITTED: crates/cfdb-petgraph/src/enrich/rfc_docs.rs — `enrich_rfc_docs` scans `docs/**/*.md` + `.concept-graph/*.md` and emits `:RfcDoc { path, title }` nodes + `(:Item)-[:REFERENCED_BY]->(:RfcDoc)` edges.
  LABEL: crates/cfdb-core/src/schema/labels.rs:52 — `pub const RFC_DOC`.
  SEMANTIC: `:RfcDoc.path` is a workspace-relative filesystem path (e.g. `docs/RFC-cfdb.md`). It is NOT an RFC-tag like `"RFC-029"`. `:Context.owning_rfc` on the cfdb keyspace is the string `"RFC-029"`, which has no direct file match — the repo uses `docs/RFC-cfdb.md` for RFC-029.

**Trigger / check verb**
  NOT FOUND — no `cfdb check` subcommand, no `--trigger` flag, no `TriggerId` type, no `Trigger::T1` constant.
  Searches exhausted: `rg "fn check|Check\s*\{|check_trigger|--trigger|Trigger::|TriggerId"` → no matches. `rg "Command::Check"` → no matches. Issue #101 is the first use.

**violations CLI verb (existing — T1 reference plumbing)**
  ENTRY: crates/cfdb-cli/src/main.rs:394-416 — `Command::Violations { db, keyspace, rule: PathBuf, no_fail: bool, count_only: bool }`.
  DISPATCH: crates/cfdb-cli/src/main.rs:497-508 — runs `violations(...)`, exits 1 if `rows_found > 0 && !no_fail`.
  IMPL: crates/cfdb-cli/src/commands.rs:368-406 — `pub fn violations(db, keyspace, rule, count_only) -> Result<usize>`. Reads the cypher file, `parse()`, `execute()`, prints JSON (or row count with `--count-only`).

**scope CLI verb (existing — classifier orchestrator)**
  ENTRY: crates/cfdb-cli/src/scope.rs — composes 6 classifier cypher rules keyed by `DebtClass`, populates `ScopeInventory::findings_by_class`, writes JSON.
  Classifier rules: `examples/queries/classifier-*.cypher` (6 files). Each RETURNS rows projected into `Finding { qname, name, kind, crate_name, file, line, bounded_context }`.
  NOT a T1 consumer — scope is per-context inventory; T1 is per-repo editorial drift.

**DebtClass (frozen taxonomy)**
  DOMAIN: crates/cfdb-query/src/inventory.rs:21-79 — `pub enum DebtClass { DuplicatedFeature, ContextHomonym, UnfinishedRefactor, RandomScattering, CanonicalBypass, Unwired }` + `FromStr` + `variants()`.
  Serde: `snake_case`. 6 variants, no room for T1 sub-verdicts at this layer.

**Finding struct (scope inventory row)**
  DOMAIN: crates/cfdb-query/src/inventory.rs:104-119 — `pub struct Finding { qname, name, kind, crate_name, file, line, bounded_context }`. Pure data — no `pattern`, no `class`, no `evidence` columns.

**ScopeInventory (§A3.3 JSON envelope)**
  DOMAIN: crates/cfdb-query/src/inventory.rs:155-186 — envelope `{ context, keyspace_sha, findings_by_class: BTreeMap<DebtClass, Vec<Finding>>, canonical_candidates, reachability_map, loc_per_crate, warnings }`. `scope` verb's output.
  NOT a T1 container — T1 output is separate signal per Forbidden moves.

**list_items_matching helper (query composer)**
  DOMAIN: crates/cfdb-query/src/list_items.rs — `pub fn list_items_matching(name_pattern, kinds, group_by_context) -> Query`. Used by `cfdb scope` to inventory items.

**arch-ban-*.cypher (cross-dogfood ban rules)**
  LOCATION: `examples/queries/arch-ban-f64-in-domain.cypher`, `arch-ban-reqwest-client-new.cypher`, `arch-ban-utc-now.cypher`.
  DISPATCH: `ci/cross-dogfood.sh:86` — `for rule in "$REPO_ROOT"/examples/queries/arch-ban-*.cypher; do cfdb violations --rule $rule; done`.
  Semantic: companion-tree-focused architectural bans. T1 is NOT a companion-tree ban; it's a self-declarations-vs-self-code assertion.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GLOBAL RESOLUTION CENSUS — scope concepts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GLOBAL RESOLUTION CENSUS — ConceptOverrides / TOML loader
  Resolution points found: 1  [OK]
    1. crates/cfdb-concepts/src/lib.rs:119 — `pub fn load_concept_overrides` (CANONICAL — exported from cfdb-concepts crate since #103)

GLOBAL RESOLUTION CENSUS — ContextMeta struct
  Definitions found: 1  [OK]
    1. crates/cfdb-concepts/src/lib.rs:56 — `pub struct ContextMeta` (CANONICAL)

GLOBAL RESOLUTION CENSUS — :Concept node emission
  Resolution points found: 1  [OK]
    1. crates/cfdb-petgraph/src/enrich/concepts.rs:158-170 — `build_one_concept_node` (CANONICAL — only site that writes CONCEPT label nodes)

GLOBAL RESOLUTION CENSUS — :Context node emission
  Resolution points found: 1  [OK]
    1. crates/cfdb-extractor/src/lib.rs:206-230 — `emit_context_node` (CANONICAL — only site that writes CONTEXT label nodes)

GLOBAL RESOLUTION CENSUS — violations verb dispatch
  Resolution points found: 1  [OK]
    1. crates/cfdb-cli/src/main.rs:497-508 → crates/cfdb-cli/src/commands.rs:368-406 — single file-to-query-to-JSON pipeline (CANONICAL for exit-1-on-findings semantics)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXISTING RESOLUTION POINTS — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`audit-split-brain --workspace . --full-workspace` output (filtered to T1 scope files):

  Zero findings in `examples/queries/`, `crates/cfdb-cli/`, `crates/cfdb-query/`, `crates/cfdb-concepts/`, `crates/cfdb-petgraph/src/enrich/`, `.cfdb/concepts/`.

  Workspace-wide findings (NOT in T1 scope — do not touch):
  - `Visibility` `FromStr` delegate-bypass at `crates/cfdb-extractor/src/item_visitor.rs:674` (marked `delegates: true`, pre-existing).
  - Duplicate `LoadError` enum at `crates/cfdb-concepts/src/lib.rs:210` + `tools/check-prelude-triggers/src/toml_io.rs:11` (distinct crates, distinct domains — acceptable).

No split-brain risks in T1 scope.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MCP/CLI BYPASS RISKS — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No domain type `FromStr` changes in T1 scope — MCP/CLI bypass scan N/A. (`DebtClass::from_str` already exists at `crates/cfdb-query/src/inventory.rs:65-79` and must stay frozen per Forbidden moves.)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PARAM CENSUS — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

No resolver/factory/consumer files in scope — Param Census N/A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALL CHAINS — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Chain 1: User runs `cfdb violations --rule <file>.cypher --db <db> --keyspace <ks>` (existing — T1 reference plumbing)
  ENTRY  → crates/cfdb-cli/src/main.rs:394-416         Command::Violations (clap)
  DISPATCH → crates/cfdb-cli/src/main.rs:497-508       dispatch_core -> violations(...) call + exit-1-on-rows
  IMPL   → crates/cfdb-cli/src/commands.rs:368-406     violations(db, keyspace, rule, count_only)
  PARSE  → cfdb_query::parse (parses cypher string)
  LOAD   → crates/cfdb-cli/src/compose.rs              load_store(db, keyspace)
  EXEC   → cfdb_petgraph::PetgraphStore::execute(ks, parsed)
  OUT    → stdout JSON (or row count with --count-only); stderr "violations: N (rule: path)"
  EXIT   → 1 if rows > 0 && !no_fail; else 0

Chain 2: `cfdb extract` populates `:Context` nodes with TOML props (existing — T1 input contract for MISSING_CANONICAL_CRATE / STALE_RFC_REFERENCE)
  ENTRY  → crates/cfdb-cli/src/main.rs:Command::Extract
  EXT    → crates/cfdb-extractor/src/lib.rs:100-124    walks packages, accumulates `contexts_seen` BTreeMap (includes `overrides.declared_contexts()` so TOML-declared contexts are always emitted even if no workspace crate is in them)
  EMIT   → crates/cfdb-extractor/src/lib.rs:122,206    emit_context_node — writes `:Context { name, canonical_crate?, owning_rfc? }` per unique context

Chain 3: `cfdb enrich-concepts` materializes `:Concept` nodes (existing — T1 input contract for CONCEPT_UNWIRED)
  ENTRY  → crates/cfdb-cli/src/main.rs:Command::EnrichConcepts
  DISPATCH → dispatch_enrich
  IMPL   → crates/cfdb-petgraph/src/enrich/concepts.rs:60-116  run(state, workspace_root)
  LOAD   → cfdb_concepts::load_concept_overrides(workspace_root)
  INDEX  → build_item_index — walks `:Item` nodes once, groups by `crate` prop
  EMIT   → one `:Concept { name, assigned_by="manual" }` per unique context name
  EDGES  → `(:Item)-[:LABELED_AS]->(:Concept)` for items in listed crates; `(:Item)-[:CANONICAL_FOR]->(:Concept)` for items in `canonical_crate`

Chain 4: cfdb's own self-dogfood (existing — T1's target test in AC-5)
  STEP 1 → cfdb extract --workspace . --db .cfdb/db --keyspace cfdb
  STEP 2 → cfdb enrich-concepts --db .cfdb/db --keyspace cfdb --workspace .
  STEP 3 → for rule in examples/queries/arch-ban-*.cypher; do cfdb violations --rule "$rule" ...; done  (existing dogfood pattern in CLAUDE.md §6)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DATA FLOW TRACE — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T1 input flow (what T1 queries read):

`canonical_crate` / `owning_rfc` props
  ENTRY:     .cfdb/concepts/<ctx>.toml                    — declared on disk (e.g. cfdb.toml: `canonical_crate = "cfdb-core"`, `owning_rfc = "RFC-029"`)
  PARSE:     cfdb-concepts/src/lib.rs:173                  — `ConceptFile` (private) → `ContextMeta` (pub)
  MATERIALISE: cfdb-extractor/src/lib.rs:206                — `emit_context_node` writes props onto `:Context` graph node
  [CONVERGE] Extract-time is the single resolution point for `:Context.canonical_crate` / `:Context.owning_rfc`.
  QUERY READ: cypher `MATCH (c:Context) WHERE c.canonical_crate IS NOT NULL` — the natural input for T1 MISSING_CANONICAL_CRATE and STALE_RFC_REFERENCE.

`:Concept.name`
  ENTRY:     .cfdb/concepts/<ctx>.toml `name` field        — same TOML source
  PARSE:     cfdb-concepts/src/lib.rs:178                  — `ContextMeta.name`
  MATERIALISE: cfdb-petgraph/src/enrich/concepts.rs:158-170  — `build_one_concept_node` writes `:Concept { name, assigned_by="manual" }`
  [FORK]     ⚠️ `enrich_concepts` pass is OPTIONAL — keyspaces without the pass have `:Context` but no `:Concept`.
  QUERY READ: T1's CONCEPT_UNWIRED semantics needs either `:Concept` (requires enrich_concepts) or can reach the same information via `:Context` + TOML-re-read (no enrichment needed).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CALLER CENSUS — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`load_concept_overrides` — 2 production callers:
  1. crates/cfdb-extractor/src/lib.rs:102 — extract entry (reads TOML at extract time)
  2. crates/cfdb-petgraph/src/enrich/concepts.rs:61 — enrich_concepts entry (re-reads TOML at Phase D)

  OK — both are legitimate TOML readers at different pipeline stages. Any T1 consumer that needs the original TOML shape (rather than materialized graph nodes) has this canonical loader available.

`emit_context_node` — 1 production caller:
  1. crates/cfdb-extractor/src/lib.rs:122 — single emission site.

`violations()` fn — 1 production caller:
  1. crates/cfdb-cli/src/main.rs:504 — single dispatch site in `dispatch_core`.

No T1-adjacent functions have SPLIT-RISK fan-out — all reach single canonical sites.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECORATOR CHAINS — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

N/A — cfdb is a pure-library + CLI codebase; no hexagonal decorator chains. Verb dispatch is flat clap match in `main.rs`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNERSHIP MAP — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verb: `violations` (CLI)
  Called from:
    1. crates/cfdb-cli/src/main.rs:504 — dispatch_core (only caller)
  Canonical owner: dispatch_core + commands::violations
  Fan-out: 1 (expected: 1)

Verb: `scope` (CLI)
  Called from:
    1. crates/cfdb-cli/src/main.rs:538 — dispatch_typed (only caller)
  Canonical owner: dispatch_typed + scope::scope
  Fan-out: 1 (expected: 1)

Concept-TOML loader: `cfdb_concepts::load_concept_overrides`
  Called from:
    1. crates/cfdb-extractor/src/lib.rs:102 — extract-time
    2. crates/cfdb-petgraph/src/enrich/concepts.rs:61 — enrich-concepts Phase D
  Canonical owner: cfdb_concepts (pub fn)
  Fan-out: 2 (expected: ≥1 — both callers are legitimate pipeline stages that read TOML)

Cypher-rule dispatch
  Template A: `cfdb violations --rule <file>` — file-based, open contract
  Template B: `cfdb scope --context <name>` — hardcoded 6 classifier rules via `include_str!`, closed contract
  Canonical owners: two distinct patterns live side-by-side; a new `cfdb check --trigger T1` verb has a shape closer to Template B (closed, versioned trigger registry) but the underlying exec plumbing (`parse → execute → rows → exit`) is identical to Template A.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCOPE CLASSIFICATION — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Existing files touched by T1's scope (from traces above):
  ENTRY (CLI):   crates/cfdb-cli/src/main.rs:72-430       — clap Command enum; new verb lands here
  ENTRY (disp):  crates/cfdb-cli/src/main.rs:444-470      — run() match; new verb's dispatch arm lands here (or extends an existing helper)
  IMPL (CLI):    crates/cfdb-cli/src/commands.rs:368-406  — `violations` fn; the plumbing `cfdb check --trigger T1` reuses
  CYPHER:        examples/queries/                        — new `.cypher` rule file lands here (arch-ban-*/classifier-* siblings)
  LIB (reader):  crates/cfdb-concepts/src/lib.rs:119      — `load_concept_overrides` reference reader (if T1 reads TOML directly rather than querying `:Context`)
  TEST:          crates/cfdb-cli/tests/                   — new integration test lands here (arch_ban_utc_now.rs is the template)

Layers containing existing code relevant to T1: CLI (entry/dispatch/impl) + LIB (TOML reader) + CYPHER RULE FILE.

Gate sequence (derived from layers — NOT from intuition):
  Gate 1 (BDD):      N/A — cfdb is library+CLI, no BDD harness; AC-4 + AC-5 integration tests satisfy Gate 1 intent via `crates/cfdb-cli/tests/*.rs`
  Gate 2 (Contract):  N/A — no new port/trait/interface; T1 is a CLI verb + cypher file
  Gate 3 (Domain):    REQUIRED if T1 adds pure-function logic in cfdb-concepts or cfdb-query (unit tests)
  Gate 4 (Reality):   REQUIRED — integration test at `crates/cfdb-cli/tests/trigger_t1.rs` (or similar) against a real on-disk fixture workspace + real cfdb store

Note on `/work-issue-lib`: per CLAUDE.md §4 routing, cfdb is a "pure-library Rust project" and `/work-issue-lib` is the prescribed pipeline. The 4-gate sequence above is the standard rubric; the library pipeline may collapse Gates 1+2 into Gate 4 (integration) + Gate 3 (unit).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXPERT VERDICTS — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### Ambiguity A1 — `:Concept` vs `:Context` as T1's input node

**Observation (fact):**
- Issue body AC-1 reads "for each `:Concept c`: If `c.canonical_crate` is set AND no `:Crate{name = c.canonical_crate}` exists → MISSING_CANONICAL_CRATE". It indexes `c.canonical_crate` and `c.owning_rfc` on a `:Concept` node.
- Concrete graph inspection (`cfdb query --db .cfdb/db --keyspace cfdb 'MATCH (c:Concept) RETURN c.name, c.assigned_by'`) returns zero rows on a fresh extract and warns "unknown node label: Concept, did you mean `Context`?". `:Concept` nodes materialize only after `cfdb enrich-concepts` runs.
- Schema descriptor at `crates/cfdb-core/src/schema/describe.rs:333-355` declares `:Concept` carries only `name` + `assigned_by` props. `canonical_crate` and `owning_rfc` live on `:Context` nodes (same file, 357-368).
- `:Context` nodes are emitted at extract-time (no enrich pass required) with exactly the three props T1's sub-verdicts test.
- Schema tests (`crates/cfdb-extractor/tests/self_workspace.rs:176-216`) confirm `:Context { cfdb }` carries `canonical_crate="cfdb-core"` + `owning_rfc="RFC-029"` on cfdb's own tree.

**Divergence verdict (fact, not prescription):**
- The issue AC's "`:Concept c`" wording does not match the graph shape emitted by `enrich_concepts` — `:Concept` lacks the props the AC indexes into.
- The same semantic content is available on `:Context` nodes without any enrichment pass.
- Prescription must choose one of: (a) query `:Context` directly, treating the AC's `:Concept` wording as a label-conflation; (b) extend `enrich_concepts` to copy `canonical_crate` + `owning_rfc` onto `:Concept` nodes (schema change — descriptor extension, no version bump required for additive attrs); (c) have T1 re-read TOML via `cfdb_concepts::load_concept_overrides`. The decision belongs in `/prescribe`.

### Ambiguity A2 — `STALE_RFC_REFERENCE` file-matching strategy

**Observation (fact):**
- `:Context.owning_rfc` on cfdb's keyspace is the string `"RFC-029"`. The actual RFC file is `docs/RFC-cfdb.md` (v0.1 locked) and `docs/RFC-cfdb-v0.2-addendum-draft.md` (addendum). There is no `docs/RFC-029*.md` file.
- `:RfcDoc.path` nodes (from `enrich_rfc_docs`) are filesystem paths like `docs/RFC-cfdb-v0.2-addendum-draft.md`, not RFC tags.
- `git grep -c "RFC-029" docs/` shows the tag IS present in the addendum body, but as free-text, not as a filename.
- Self-dogfood AC-5 expects "zero findings" on cfdb's own tree — so STALE_RFC_REFERENCE's matching strategy MUST accept `owning_rfc="RFC-029"` as "present" given the actual filesystem shape (no RFC-029.md but multiple docs mention RFC-029).

**Divergence verdict (fact, not prescription):**
- Naïve filename matching (`owning_rfc == :RfcDoc.path.stem`) would fire on cfdb's self-dogfood → contradicts AC-5.
- Two honest matching strategies exist: (1) string-contains on `:RfcDoc.path` OR `:RfcDoc.title`; (2) text-scan of `docs/` for the tag, not via the graph at all. Each has different determinism semantics.
- Prescription must pick a strategy and document it in a query comment so future maintainers know why the matching is not a simple filename compare.

### Ambiguity A3 — enrichment dependency and CLI UX

**Observation (fact):**
- If T1 queries `:Concept`, the keyspace must have `enrich-concepts` run before `cfdb check --trigger T1` produces meaningful output.
- If T1 queries `:Context` directly, no enrich pass is required — just `cfdb extract`.
- `cfdb scope` handles this dependency by emitting per-class "missing enrichment" warnings rather than hard-failing (`crates/cfdb-cli/src/scope.rs` — see `attach_scope_warnings`).
- `cfdb violations --rule <file>` has no such warning path — it just returns zero rows silently if the required label is absent.

**Divergence verdict (fact, not prescription):**
- Both UX patterns exist in the codebase. Which pattern T1 adopts depends on whether prescription picks `:Context` (pass-free) or `:Concept` (requires enrich).
- The scope.rs warning pattern is richer but heavier; the violations.rs pattern is simpler.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DECISION ARCHAEOLOGY — Issue #101
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Concept: `:Concept` node schema restriction to `name` + `assigned_by`
  Issue AC claim: "for each `:Concept c`: If `c.canonical_crate` is set …" — assumes `canonical_crate` prop on `:Concept`.
  Current state: `:Concept` carries `name` + `assigned_by` only (`crates/cfdb-petgraph/src/enrich/concepts.rs:158-170`; schema `crates/cfdb-core/src/schema/describe.rs:333-355`).
  Introduced by: `933d24f` (`feat(cfdb-petgraph,cfdb-concepts,cfdb-cli): enrich_concepts — :Concept nodes + LABELED_AS/CANONICAL_FOR edges (#109)`).
  Rationale (from #109 body): *"Node emission: `:Concept { name: String, assigned_by: String }` — one node per `.cfdb/concepts/<name>.toml` file; `assigned_by = "manual"` for TOML-declared concepts."* — canonical_crate was intentionally omitted from `:Concept` because `:Context` already carries it and `enrich_concepts` was scoped to minimal emission.
  RFC support: `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.2 — original 5-pass table called for `:Concept` materialisation; council round 1 synthesis §43-F added it as the 6th pass (DDD Q4). Neither document prescribed `canonical_crate` on `:Concept`.
  Addendum: §A2.2 (6 passes not 5) — concept of minimal emission is internal to the enrichment module.
  Parent briefing: yes — freshness package Forbidden moves lists "Do NOT add trigger-specific fields to `ScopeInventory`" and "Do NOT duplicate the `cfdb violations` cypher dispatch" but does NOT explicitly forbid extending `:Concept` props. Schema-attr extension is additive and has precedent (e.g., `:Item.bounded_context` added via #108).
  Divergence verdict:
    - Issue AC and current schema disagree on where `canonical_crate` lives (`:Concept` vs `:Context`).
    - Current schema is consistent with #109 (6th-pass scope) and with the `:Context` node descriptor (Provenance::Extractor).
    - Issue AC was authored 2026-04-19, predates #109 merge (2026-04-20) — the AC was written before `:Concept` emission shape was finalized.
    - Prescription has enough facts to pick between "query `:Context` directly" (A1 option a) vs "extend `:Concept` emission" (A1 option b) without re-reading the issue body in isolation.

Concept: T1 CLI surface — verb name vs subflag
  Issue AC claim: "New CLI verb or subflag `cfdb check --trigger T1`" — flexible shape in AC-2.
  Current state: no `check` verb, no `--trigger` flag anywhere in `cfdb-cli`.
  Introduced by: N/A — never implemented.
  Rationale: issue body explicitly allows "verb or subflag".
  RFC support: N/A — RFC-029 / addendum do not prescribe a specific CLI surface for triggers.
  Divergence verdict: no divergence — the AC is loose on CLI shape by design. Prescription picks verb vs subflag. The only constraint from Forbidden moves is "do not duplicate violations dispatch plumbing" — both verb and subflag can satisfy that via shared composition.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
