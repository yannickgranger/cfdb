//! Empty stub crate named `clap` so the fixture workspace's crate graph
//! carries a `clap` dependency for the RFC-049 §3.1 manifest gate. It
//! holds no framework code — `cli_fx`'s clap-derive detection stays
//! attribute-textual against its own local stand-in traits.
