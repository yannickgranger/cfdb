//! Empty stub crate named `axum` so the fixture workspace's crate graph
//! carries an `axum` dependency for the RFC-049 §3.1 manifest gate. It
//! holds no framework code — `http_fx`'s route detection stays syntactic
//! against its own local stand-in `Router` / `App` types.
