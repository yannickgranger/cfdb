[package]
name = "http_fx"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
# Stub `axum` (empty crate) — satisfies the RFC-049 §3.1 manifest gate
# so the http_route detector runs on this member. Detection stays
# syntactic (method-name + literal path + handler resolution).
axum = { path = "../axum" }
