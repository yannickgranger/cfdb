# Transformation table — Issue #151 (drift god-file split)

**Invariants (enforced by integration agent, NOT by sub-agents):**

1. **Determinism.** Post-split `cfdb extract` run twice produces byte-identical SHA256. Baseline (pre-split, for reference only): `73a6026a5d5ab922ebe0445ea8beb90ca957d19e4621a223611c388efa58e856` at `.baseline/facts.sha256`. **Note: pre vs post SHAs differ by design** — splits add new `:File`/`:Module` nodes — the invariant is determinism, not cross-version equality (#128 correction).
2. **Public API unchanged.** No `pub` / `pub(crate)` item renamed. Every existing import path across the workspace continues to resolve.
3. **Test counts preserved.** `cargo test -p cfdb-cli` and `cargo test -p cfdb-hir-extractor` pass at the same counts as on develop @ `6e04d1a`.
4. **Every resulting file ≤500 LOC.** `quality-architecture --workspace .` reports 0 god-files workspace-wide after integration.
5. **`TriggerId` delegation preserved.** `main_parse.rs:29`'s `parse_trigger_id` delegates to `<cfdb_cli::check::TriggerId as FromStr>::from_str` — canonical FromStr must stay at that qname post-split.

## Work units — 4 solo agents, 4 worktrees

Each sub-agent runs in its own worktree branched off `origin/develop @ 6e04d1a`. Sub-agents **DO NOT run `cargo`** — text-only edits, single commit per unit. Integration agent cherry-picks + runs `cargo check --workspace` once.

| Unit | Shape | Worktree | Crate | File(s) | Current LOC |
|---|---|---|---|---|---|
| U1 | Solo | `.claude/worktrees/151-sub-check` | cfdb-cli | `src/check.rs` | 639 |
| U2 | Solo | `.claude/worktrees/151-sub-commands` | cfdb-cli | `src/commands.rs` | 904 |
| U3 | Solo | `.claude/worktrees/151-sub-scope` | cfdb-cli | `src/scope.rs` | 589 |
| U4 | Solo | `.claude/worktrees/151-sub-entry-point` | cfdb-hir-extractor | `src/entry_point_emitter.rs` | 784 |

## U1 — `crates/cfdb-cli/src/check.rs` (639 → 4 files)

**Structure today:** consts (T1/T3 Cypher embeds, ~30 LOC) + `pub enum TriggerId` + `pub struct UnknownTriggerId` + Display/FromStr impls (~70 LOC, lines 54–160) + `pub fn check` entry (~10 LOC, 162) + `struct Finding` + `struct ContextRow` + `struct T3Row` types (~100 LOC) + `fn run_trigger_t1` + T1 helpers (`fetch_contexts`, `fetch_scalar_set`, `fetch_rfc_haystack`, `scalar_str_owned`) (~200 LOC, 227–389) + `fn run_trigger_t3` + helpers (~180 LOC, 467–572) + `#[cfg(test)] mod tests` (~66 LOC, 573–639).

**Target layout (2018-edition: `check.rs` is the module entry; `check/` holds submodules):**

| Path | Contents | LOC est. |
|---|---|---|
| `crates/cfdb-cli/src/check.rs` | module doc + const Cypher embeds + `pub enum TriggerId` + `pub struct UnknownTriggerId` + FromStr/Display impls + `pub fn check` entry-point dispatcher (match on trigger, delegates to t1::run / t3::run) + Finding + ContextRow + T3Row type defs + `mod t1; mod t3; #[cfg(test)] mod tests;` | ~250 |
| `crates/cfdb-cli/src/check/t1.rs` | `pub(super) fn run` + T1-specific helpers (`fetch_contexts`, `fetch_scalar_set`, `fetch_rfc_haystack`, `scalar_str_owned`) — move exactly, no behaviour change | ~200 |
| `crates/cfdb-cli/src/check/t3.rs` | `pub(super) fn run` + T3-specific helpers | ~180 |
| `crates/cfdb-cli/src/check/tests.rs` | existing `#[cfg(test)] mod tests` body verbatim | ~70 |

**Public API preserved:** `cfdb_cli::check::{check, TriggerId, UnknownTriggerId}` resolve as before (these are `pub` in the current lib.rs re-export).

**Delegation invariant:** `impl FromStr for TriggerId` stays in `check.rs` top level, NOT in a submodule. Preserves `cfdb_cli::check::TriggerId::from_str` qname for `main_parse.rs:29`.

## U2 — `crates/cfdb-cli/src/commands.rs` (904 → 6 files)

**Structure today:** LIST_CALLERS_CYPHER const + `pub fn extract` + `extract_at_path`/`extract_at_rev`/`extract_at_url_rev` + URL helpers (`prepare_cache_dir`, `clone_and_checkout`, `short_rev`, `workspace_basename`, `parse_url_at_sha`, `is_url_at_sha`, `url_has_scheme`, `cache_dir_for`, `cache_base_dir`, `url_hash_hex16`) + `struct GitWorktree` + `extract_hir` (cfg-gated) (~410 LOC, 16–434) + `pub fn query` + `bind_json_params` + `bind_single_param` (~70 LOC, 435–535) + `pub fn list_callers` (~45 LOC, 536–580) + `pub fn violations` + `pub(crate) fn run_cypher_rule` + `pub(crate) fn parse_and_execute` (~100 LOC, 580–695) + `pub fn dump` + `pub fn list_keyspaces` + `pub fn export` (~40 LOC) + `#[cfg(test)] mod tests` (~210 LOC, 695–904).

**Target layout:**

| Path | Contents | LOC est. |
|---|---|---|
| `crates/cfdb-cli/src/commands.rs` | module doc + `pub use` re-exports of every pub fn currently at the `commands::` surface + `mod extract; mod query; mod rules; mod aux; #[cfg(test)] mod tests;` | ~80 |
| `crates/cfdb-cli/src/commands/extract.rs` | `pub(super) fn extract` + all `extract_at_*` + every URL/rev/cache helper + `struct GitWorktree` + both cfg-gated `extract_hir` definitions + `pub fn keyspace_path` (co-located for proximity to extract outputs) | ~420 |
| `crates/cfdb-cli/src/commands/query.rs` | `pub(super) fn query` + `bind_json_params` + `bind_single_param` + LIST_CALLERS_CYPHER const + `pub(super) fn list_callers` | ~130 |
| `crates/cfdb-cli/src/commands/rules.rs` | `pub(super) fn violations` + `pub(crate) fn run_cypher_rule` + `pub(crate) fn parse_and_execute` (keep `pub(crate)` — consumed by sibling `check_predicate.rs`) | ~100 |
| `crates/cfdb-cli/src/commands/aux.rs` | `pub(super) fn dump` + `pub(super) fn list_keyspaces` + `pub(super) fn export` | ~65 |
| `crates/cfdb-cli/src/commands/tests.rs` | existing `#[cfg(test)] mod tests` body | ~220 |

**Public-path invariant:** `cfdb_cli::{extract, query, list_callers, violations, dump, list_keyspaces, export, keyspace_path}` — all currently re-exported at `crates/cfdb-cli/src/lib.rs:23-26` via `pub use commands::{...};`. The `commands.rs` module-level `pub use` lines must re-export the same set from the new submodules so the crate-root re-export works unchanged.

**`pub(crate) fn run_cypher_rule` / `pub(crate) fn parse_and_execute`:** these are consumed by `check_predicate.rs` and `check.rs`. Must remain accessible at `crate::commands::{run_cypher_rule, parse_and_execute}` OR `crate::commands::rules::{...}`. Easiest: `pub(crate) use` them at the `commands.rs` entry after the `mod rules;` declaration.

## U3 — `crates/cfdb-cli/src/scope.rs` (589 → 3 files)

**Structure today:** classifier Cypher const embeds (~30 LOC, 22–60) + `pub fn scope` entry (~35 LOC, 67–102) + `validate_context` + `build_scope_inventory` + `populate_findings_by_class` + `classifier_rules` + `run_classifier_rule` + `query_findings_in_context` + `query_canonical_candidates` + `attach_scope_warnings` + `emit_scope_output` + `resolve_keyspace_name` + `query_known_contexts` + `crates_for_context` + row extractors (`scalar_str`, `finding_from_row`, `row_list_str`, `canonical_candidate_from_row`) + `class_empty_bucket_note` (~520 LOC, 67–589). **No inline tests** — scope coverage is in `crates/cfdb-cli/tests/scope.rs`.

**Target layout:**

| Path | Contents | LOC est. |
|---|---|---|
| `crates/cfdb-cli/src/scope.rs` | module doc + classifier const embeds + `pub fn scope` entry + `build_scope_inventory` + `populate_findings_by_class` + `attach_scope_warnings` + `emit_scope_output` + `resolve_keyspace_name` + `class_empty_bucket_note` + `mod classifier; mod helpers;` | ~230 |
| `crates/cfdb-cli/src/scope/classifier.rs` | `pub(super) fn classifier_rules` + `pub(super) fn run_classifier_rule` + `pub(super) fn query_findings_in_context` + `pub(super) fn query_canonical_candidates` | ~180 |
| `crates/cfdb-cli/src/scope/helpers.rs` | `pub(super) fn validate_context` + `pub(super) fn query_known_contexts` + `pub(super) fn crates_for_context` + row extractors (`scalar_str`, `finding_from_row`, `row_list_str`, `canonical_candidate_from_row`) | ~220 |

**Public API:** `pub fn scope` stays at `cfdb_cli::scope`. No other pub items in this file.

## U4 — `crates/cfdb-hir-extractor/src/entry_point_emitter.rs` (784 → 3 files)

**Structure today:** HTTP_ROUTE_METHOD_NAMES const (~15 LOC, 51–64) + `pub fn extract_entry_points` + `extract_entry_points_attached` (~45 LOC, 64–109) + `fn scan_file` (main dispatcher — reads each file's AST and branches on item kind, ~80 LOC, 109–190) + HTTP route classification (`classify_http_route_method_call` + `extract_path_and_handler` + `is_url_path` + `receiver_resource_path` + `call_ends_in` + `string_literal_value` + `resolve_handler_qname` + `resolve_handler_from_method_chain` + `resolve_path_to_fn_qname` + `emit_http_route`) (~280 LOC, 190–470) + `has_clap_derive` + `has_tool_attr` (~30 LOC) + name/qname resolution (`struct_name_and_qname`, `enum_name_and_qname`, `fn_name_and_qname`) (~50 LOC) + `try_emit_cron_job` + `try_emit_websocket` (~70 LOC) + `struct HandlerTarget` + `resolve_handler_arg` + `enclosing_fn_name_and_qname` + `build_item_qname` (~110 LOC, 622–784). **No inline tests** — AC-level coverage in sibling integration tests.

**Target layout:**

| Path | Contents | LOC est. |
|---|---|---|
| `crates/cfdb-hir-extractor/src/entry_point_emitter.rs` | module doc + HTTP_ROUTE_METHOD_NAMES const + `pub fn extract_entry_points` + `extract_entry_points_attached` + `fn scan_file` + `has_clap_derive` + `has_tool_attr` + name/qname helpers (`struct_name_and_qname`, `enum_name_and_qname`, `fn_name_and_qname`, `enclosing_fn_name_and_qname`, `build_item_qname`) + `struct HandlerTarget` + `resolve_handler_arg` + `mod http_route; mod other_kinds;` | ~280 |
| `crates/cfdb-hir-extractor/src/entry_point_emitter/http_route.rs` | `pub(super) fn classify_http_route_method_call` + `pub(super) fn extract_path_and_handler` + `fn is_url_path` + `fn receiver_resource_path` + `fn call_ends_in` + `fn string_literal_value` + `pub(super) fn resolve_handler_qname` + `fn resolve_handler_from_method_chain` + `fn resolve_path_to_fn_qname` + `pub(super) fn emit_http_route` | ~290 |
| `crates/cfdb-hir-extractor/src/entry_point_emitter/other_kinds.rs` | `pub(super) fn try_emit_cron_job` + `pub(super) fn try_emit_websocket` | ~80 |

**Public API:** `pub fn extract_entry_points` stays at `cfdb_hir_extractor::entry_point_emitter::extract_entry_points`. Sole pub item in this file.

## Integration protocol (main agent)

1. Each sub-agent commits to its branch (`work/151-sub-<name>`).
2. Main agent cherry-picks all 4 commits into integration worktree (`work/151-drift-god-files`). Different files → no conflicts expected.
3. `cargo check --workspace --all-targets --all-features` — ONE run.
4. If compile errors, fix localized issues (import-path drift most likely).
5. `cargo test --workspace --all-features` — ONE run. Assert test counts.
6. `cargo build --release -p cfdb-cli --bin cfdb && cfdb extract --workspace . --db .cfdb/db-post --keyspace cfdb` — new canonical dump + SHA.
7. Re-run extract twice to verify byte-identical post-split SHAs (determinism).
8. `quality-architecture --workspace .` — assert 0 god-files workspace-wide.
9. Graph-specs anti-drift — both passes must exit 0 (no pub type changes).
10. `/ship 151 agency:yg/cfdb --workspace <integration>`.

## Sub-agent contract (every work unit obeys)

- READ `.context/151.md` + `.transform/151.md` in full before editing.
- EDIT only the files listed for your unit — no touching other units' scope.
- NEVER run `cargo check / build / test / clippy / fmt` — the integration agent compiles once for all units.
- MOVE-ONLY surgery. Cut `fn` / `struct` / `enum` / `impl` / `const` blocks, paste into sibling files, raise visibility to `pub(super)` only where the split requires it. No renames. No body changes. No behaviour changes.
- Each resulting file MUST be <500 LOC.
- `use` statements: each new file gets only what it needs; the parent gains `use ...::{...};` for cross-module references.
- SINGLE COMMIT per unit. Message format `refactor(<crate>): split <file>.rs into <N> files (#151)`.
