---
issue: 239
issue_state: open
issue_created_at: 2026-04-24T07:32:19Z
refreshed_at: 2026-04-24T16:40:00Z
refreshed_sha: 346eab1
verdict: current
repo: yg/cfdb
cfdb_keyspace_sha: 346eab16305e
triggers_json_path: .triggers/239.json
triggers_fired: []
---

# Context package — Issue #239

## Issue summary
Add `render_type_inner` to `crates/cfdb-extractor/src/type_render.rs` that unwraps a closed list of standard-library wrapper types (`Vec<T>`, `Option<T>`, `Arc<T>`, `Rc<T>`, `Box<T>`, `Result<T, E>`, `Pin<P>`, `Cell<T>`, `RefCell<T>`) and returns the inner type name(s) for RETURNS/TYPE_OF resolution in the post-walk passes. Currently `render_type_string` collapses `Vec<Foo>` → `"Vec"`, suppressing same-crate RETURNS/TYPE_OF edges for wrapped types. Measurable delta on qbot-core: RETURNS count ≥2×, TYPE_OF count ≥1.5×.

## AC freshness
- 🟢 OPEN — Add `render_type_inner` helper to `crates/cfdb-extractor/src/type_render.rs`. Current file has `render_type` at line 11 and `render_type_string` at line 65; no inner-unwrapping helper exists on develop.
- 🟢 OPEN — Wire `render_type_inner` into RETURNS resolution at `resolve_deferred_returns` (`crates/cfdb-extractor/src/lib.rs:300-347`). Today that function does a single exact-match lookup in `emitted_item_qnames` + a unique-last-segment fallback; no wrapper unwrapping is attempted.
- 🟢 OPEN — Wire `render_type_inner` into TYPE_OF resolution at `resolve_deferred_type_of` (`crates/cfdb-extractor/src/lib.rs:371-…`). Same shape as RETURNS — same exact/last-segment/no-unwrap codepath today.
- 🟢 OPEN — Closed wrapper list covers exactly: `Vec<T>`, `Option<T>`, `Arc<T>`, `Rc<T>`, `Box<T>`, `Result<T,E>` (both arms), `Pin<P>`, `Cell<T>`, `RefCell<T>`.
- 🟢 OPEN — Nested wrappers unwrap recursively with bounded depth 3; depth-4+ silently drops.
- 🟢 OPEN — Unit tests (a)-(f) per issue body: `Vec<Foo>` → one RETURNS; `Result<Ok,Err>` → two RETURNS; `Vec<Option<Arc<Foo>>>` at depth 3; depth-4 drop; trait-constrained generic drops; user-defined `MyBox<Foo>` drops.
- 🟢 OPEN — Self-dogfood: extract cfdb; assert RETURNS ≥150, TYPE_OF ≥300.
- 🟢 OPEN — Target-dogfood on qbot-core: report before/after RETURNS + TYPE_OF counts + flag any unrecognized wrapper class.

## Commits since issue was filed (scope: crates/cfdb-extractor/src/type_render.rs crates/cfdb-extractor/src/lib.rs docs/RFC-037-schema-producer-alignment.md)
- No commits on `origin/develop` have touched the scope files since the issue was filed at 2026-04-24T07:32:19Z. Current develop tip `346eab1` matches the commit the issue was filed against. Scope is clean.

## Relevant PRs
- **Merged:**
  - #228 (`feat(rfc-037): delete SUPERTRAIT + RECEIVES_ARG; SchemaVersion v0.3.0`) — final producer-wave slice; closed RFC-037 epoch. Relevant because this issue is the §6 non-goal disposition of that RFC.
  - #226 (`feat(rfc-037): REGISTERS_PARAM 3-paths + TYPE_OF producer`) — landed `resolve_deferred_type_of` — one of the two call sites this issue extends.
  - #224 (`feat(rfc-037): qname canonical helpers + RETURNS + :Field attrs`) — landed `resolve_deferred_returns` + `emitted_item_qnames` — the other call site + the resolution index.
  - #243 (`chore(release): v0.3.0`) + #244 (develop→main) + #245 (backmerge) — v0.3.0 released; SchemaVersion unchanged by this issue, so no release-coordination impact.
- **Open:**
  - #247 (`fix(cfdb-petgraph): bind edge.var in build_path_binding` — closes #242). **Orthogonal scope** — edge-pattern MATCH evaluator in `crates/cfdb-petgraph/src/eval/pattern.rs`; does not touch `cfdb-extractor`. No collision.

## Sibling open issues
- **#238** (`chore(docs): RFC-037 closeout — mark producer wave shipped + §6 non-goals disposition`) — **downstream consumer**: #238 explicitly names #239 as the shipped disposition of §6 non-goal #1. Closing #239 feeds #238's closeout table.
- **#240** (`chore(ci): backfill + enforce Closes #N footer`) — unrelated (CI hygiene).
- **#242** (`fix(cfdb-petgraph): edge-pattern MATCH returns 0 rows`) — unrelated (query-side bug); handled by open PR #247.
- **#246** (`fix: drain 18 pre-existing clones-in-loops from cfdb-petgraph doughnut`) — unrelated (mechanical cleanup).
- **#247** — (PR, not issue).

## Doctrinal layer
- `docs/RFC-037-schema-producer-alignment.md` §2 line 41 (Out-of-scope): "`render_type_inner` / generic-unwrapping — current `type_render.rs:14-21` strips `Vec<T>`/`Option<T>`/`Result<T,E>` to the outer wrapper. TYPE_OF / RETURNS edges on wrapped same-crate types silently do not emit. Documented limitation in §6; follow-up RFC may refine." Implication: the RFC explicitly anticipates this work as a follow-up; "may refine [via RFC]" is permissive, not mandatory.
- `docs/RFC-037-schema-producer-alignment.md` §3.2 line 98 (RETURNS limitation): "`render_type_string` at `crates/cfdb-extractor/src/type_render.rs:11-58` strips generic arguments: `Vec<MyType>` renders as `"Vec"`. RETURNS on wrapper-wrapped same-crate types silently does not emit. … A follow-up `render_type_inner` that unwraps `Vec<T>` → `T`, `Option<T>` → `T`, `Result<T,E>` → `T` is noted in §6 non-goals." Implication: the shipped behavior of #216 (RETURNS) and #220 (TYPE_OF) explicitly deferred this precision win; the infrastructure for it (`emitted_item_qnames`, post-walk pass, last-segment fallback) is already live.
- `docs/RFC-037-schema-producer-alignment.md` §6 line 258 (Non-goals): "`render_type_inner` generic unwrapping … The HSB Jaccard signal (#204) has a recall loss proportional to wrapper frequency in target workspaces; acceptable for v0.3.0." Implication: downstream HSB (#204) is the consumer whose recall improves when this ships.
- `CLAUDE.md` §1 (repo-local, work-type table): "Bug fix (wrong behavior on existing verb) | Issue → `/work-issue-lib` → dogfood-gated PR". This fits: RETURNS/TYPE_OF are existing edges whose emission is silently incomplete; fixing the incompleteness does not add new fact types, verbs, schema fields, or flags. No RFC amendment strictly required; `/work-issue-lib` path is canonical.
- `CLAUDE.md` §2.5 (repo-local, Tests template): Issue body already carries the 4-row `Tests:` block (Unit / Self dogfood / Cross dogfood / Target dogfood) per template. Implementer delivers against what is prescribed.
- `CLAUDE.md` §3 (repo-local, dogfood gates): no SchemaVersion bump expected (issue explicitly says "SchemaVersion: unchanged"); **no lockstep graph-specs-rust PR required**.
- `CLAUDE.md` §5 (repo-local, schema discipline): edge counts change; wire shape does not. This is a non-breaking precision improvement.

## Split-brain pre-scan (scope-filtered)
No split-brain risks in cfdb-extractor scope. `audit-split-brain --full-workspace` reports 0 findings matching `cfdb-extractor`. Safe to extend `type_render.rs` without widening an existing split.

## Session handoffs (relevant)
- `state:a0-session:2026-04-23-cfdb-rfc-037-in-flight` (the parent epoch that produced this backlog): documents that `render_type_inner` is one of three remaining follow-ups after PR #228. Already-shipped: #227 (HIR impl-target qname). Still-open: #239 (this issue) + #238 (closeout docs).
- Two 2026-04-24 sessions (`agentry-cutoff-unlock-postmortem`, `agentry-dogfood-bootstrap`) are on a different repo (yg/agentry); not relevant to cfdb #239.

## Forbidden moves
- **Do not extend the wrapper list beyond the 9 enumerated types.** The closed list is `Vec`, `Option`, `Arc`, `Rc`, `Box`, `Result`, `Pin`, `Cell`, `RefCell`. If prescription wants to add (e.g.) `Weak<T>`, `Mutex<T>`, `RwLock<T>`, `UnsafeCell<T>`, STOP and file an RFC amendment. Rationale: the issue explicitly makes this a closed list; silent widening undermines the RFC §6 rationale.
- **Do not handle trait-constrained generics (`fn<T: Trait>() -> T`).** Explicit non-goal — `T` is unresolvable without monomorphization. Rationale: RFC §2 line 40 defers cross-crate + generic resolution to HIR; this issue is syn-side only.
- **Do not handle user-defined wrappers (`MyBox<T>`).** Explicit non-goal — requires an RFC addendum + config. Rationale: closed list is the only stable surface.
- **Do not handle tuple types (`(Foo, Bar)`) or associated types (`<T as Trait>::Output`).** Both explicit non-goals; HIR territory.
- **Do not bump `cfdb_core::SchemaVersion`.** Wire shape of RETURNS/TYPE_OF edges is unchanged; only emission counts go up. Rationale: repo CLAUDE.md §3 — no lockstep graph-specs PR needed when wire shape is stable. Issue body is explicit on this.
- **Do not exceed bounded depth 3 in nested unwrapping.** Depth-4+ MUST silently drop. Rationale: bounded depth is a normative acceptance criterion; exceeding it would mask deeply-nested pathological cases and grow the fact graph uncontrollably.
- **Do not reintroduce `type_qname` as a field property.** #217 removed `type_qname` from `:Field` attribute surface per RFC §3.5; the TYPE_OF edge IS the typed link now. If new code writes a `type_qname` field prop, it is a split-brain with the shipped `:Field` shape. Rationale: regression of a ratified schema change.
- **Do not re-implement the post-walk resolution scaffolding.** `emitted_item_qnames` (`crates/cfdb-extractor/src/lib.rs:438`), `deferred_returns` (line 443), `deferred_type_of` (line 454), the last-segment fallback index (lines 307-320), and the `BTreeSet` (not `HashSet`) choice — all shipped on develop. New code extends these; do NOT parallel-structure new deferred queues.
- **Do not bypass the existing `render_type_string` fast path.** The call pattern is: try `render_type_string` first (fast/precise), fall back to `render_type_inner` only on miss. The issue body prescribes this order explicitly.
- **Do not touch the HIR side.** #227 (shipped as #241) owns HIR qname canonicalization. `render_type_inner` is a syn-side concern in `cfdb-extractor`, not `cfdb-hir-extractor`.

## Parent corrections
_none yet — append-only, see correction protocol below_

## Correction protocol
Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#239): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.
