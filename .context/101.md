---
issue: 101
issue_state: open
issue_created_at: 2026-04-19T23:38:46Z
refreshed_at: 2026-04-21T00:00:00Z
refreshed_sha: fe8b8901a373
verdict: contested
repo: agency:yg/cfdb
cfdb_keyspace_sha: fe8b8901a373
---

# Context package — Issue #101

## Issue summary

Feat: Trigger T1 — new `cfdb check --trigger T1` CLI surface running a query that detects concepts declared in `.cfdb/concepts/<ctx>.toml` that have no matching `:Item` / `:Crate` in the extracted graph. Three sub-verdicts: `CONCEPT_UNWIRED`, `MISSING_CANONICAL_CRATE`, `STALE_RFC_REFERENCE`. Exit 1 on any finding.

## AC freshness

- ✅ **(implicit) Blocker #109** — AC claims "Blocked-by: #43 (enrichment framework — needs `enrich_concepts` so the `:Concept` nodes are materialised)" — shipped via PR #121 (merge `933d24f`). `:Concept` nodes + `LABELED_AS` + `CANONICAL_FOR` edges materialise from `.cfdb/concepts/*.toml`.
- 🟢 **AC-1** — `examples/queries/t1-concept-unwired.cypher` implementing three subclasses — no such file on develop.
- 🔀 **AC-2 (partially shipped differently)** — "new CLI verb or subflag `cfdb check --trigger T1` that runs the query + emits `:Finding` nodes in the configured store + prints JSON to stdout". The generic `cfdb violations --rule <cypher> [--count-only]` verb ALREADY exists (`crates/cfdb-cli/src/commands.rs:368-406`) and does exactly this (runs cypher, returns row count, exits non-zero on findings, prints JSON). The `cfdb check --trigger T1` verb IS a new surface — prescription must decide whether to land it as sugar over `violations` or as a distinct verb with its own dispatch. The "emits `:Finding` nodes in the configured store" phrasing is aspirational — no code path today writes graph nodes on query execution; `Finding` is a pure-data Rust struct (`crates/cfdb-query/src/inventory.rs:110-119`), not a graph label. Queries RETURN rows, they do not CREATE :Finding graph nodes.
- 🟢 **AC-3** — exit code 1 iff findings emitted — aligns with existing `violations` wrapping pattern.
- 🟢 **AC-4** — synthetic `.cfdb/concepts/<ctx>.toml` fixture + integration test asserting exactly 2 findings — no such fixture.
- 🟢 **AC-5** — self-dogfood proof (run against cfdb's own tree → zero findings). `.cfdb/concepts/cfdb.toml` exists (1 concept declared), graph currently carries :Concept nodes per #109 — baseline achievable.
- 🟢 **AC-6** — `/operate-module` (#50) routing integration — #50 is still OPEN, so this AC is forward-compatible wiring only; prescription must note that #50 consumption is a downstream contract.
- 🔀 **AC-7 (shipped differently)** — AC says "`DebtClass` extension (if needed) lands with a PR body justification; otherwise map T1 findings onto `UnfinishedRefactor` + specific class discriminator prop". The 6-class `DebtClass` enum is FROZEN in `crates/cfdb-query/src/inventory.rs:21-30` — `Ord`/`PartialOrd` derives + addendum §A2.1 note that these are the canonical 6. Adding a 7th variant requires an RFC amendment. T1's three sub-verdicts (`CONCEPT_UNWIRED` / `MISSING_CANONICAL_CRATE` / `STALE_RFC_REFERENCE`) are all editorial-drift shapes that are UPSTREAM of the classifier, not a new classifier output — prescription must NOT add a 7th `DebtClass`.

**Verdict derivation:** AC-1/3/4/5/6 are 🟢 OPEN. AC-2 is 🔀 SHIPPED DIFFERENTLY (generic `violations` verb already covers the row/exit/JSON plumbing). AC-7 is 🔀 SHIPPED DIFFERENTLY (taxonomy frozen; do not extend). No AC is 🔄 IN-FLIGHT. → `verdict: contested` — reduced scope spelled out below.

## Commits since issue was filed (scope: examples/queries/ crates/cfdb-cli/ crates/cfdb-core/src/query/ crates/cfdb-query/ .cfdb/concepts/ docs/RFC-cfdb-v0.2-addendum-draft.md)

- `2f214bd` style(cfdb): cargo fmt after #48 classifier wiring
- `dc1654b` feat(cfdb): :Finding classifier Cypher + 6-class taxonomy wiring (#48)
- `4a3d807` feat(cfdb): signature_divergent UDF + fn signature emission (#47)
- `5128f46` feat(cfdb): canonical-bypass Pattern C — 4 verdicts, generalized (#45)
- `53c042a` feat(cfdb): vertical-split-brain.cypher (Pattern B) + scar tests (#44)
- `dcd7b91` fix(#127): register check-prelude-triggers in .cfdb/concepts/cfdb.toml
- `d942f09` style: move tests mod to end of commands.rs (clippy items_after_test_module)
- `8901ca9` feat(cfdb-cli): cfdb extract --rev <sha> — extract against arbitrary git revisions (#37)
- `933d24f` feat(cfdb-petgraph,cfdb-concepts,cfdb-cli): enrich_concepts — :Concept nodes + LABELED_AS/CANONICAL_FOR edges (#109)
- `d239e57` feat(cfdb-petgraph,cfdb-cli): enrich_bounded_context re-enrichment + v0.2-9 ≥95% gate (#108)
- `c42ff67` feat(cfdb-petgraph,cfdb-core,cfdb-cli): enrich_rfc_docs real impl + SchemaVersion V0_2_3 (#107)
- `242ef1e` feat(cfdb-petgraph): enrich_git_history real impl — git2 behind git-enrich feature (#105)

## Relevant PRs

- **Merged (Tier-4 classifier + enrichment siblings):** #136 (#48 classifier), #135 (#47 signature_divergent), #134 (#45 Pattern C), #133 (#44 Pattern B), #121 (#109 enrich_concepts — UNBLOCKS #101), #119 (#108 enrich_bounded_context)
- **Open:** none

## Sibling open issues

- #102 — Trigger T3 concept-name-in-≥2-crates (PAIR WITH #101 per user intent)
- #100 — `.cfdb/published-language-crates.toml` marker loader
- #95 — v0.2-phase1 umbrella (parent)
- #50 — `/operate-module` skill (downstream consumer of T1 findings)
- #51 — RFC-029 addendum merge into parent RFC
- #49 — `cfdb-query-dsl`
- #34 — EPIC v0.2 (parent)

## Doctrinal layer

- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.1 lines 182-189** — canonical 6-class `DebtClass` taxonomy (DuplicatedFeature, ContextHomonym, UnfinishedRefactor, RandomScattering, CanonicalBypass, Unwired). Verbatim: *"Note on the fix strategy column: the values above are abstract action names … NOT concrete Claude skill names."* Implication: adding a 7th class requires an explicit RFC amendment — T1 sub-verdicts must fit inside the existing 6 (or be routed as a separate trigger-level signal outside `DebtClass`).
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A3.2 lines 290–292** — ownership declaration in `.cfdb/concepts/*.toml`. Referenced from the issue body (required prior reading). Implication: T1's input contract is the TOML file shape already consumed by `cfdb-concepts::ConceptOverrides::load(workspace_root)` and materialised as `:Concept` nodes by `enrich_concepts`.
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A4.2** — live inventory + raid plan flow (#50 downstream). Implication: T1 JSON output shape must be consumable by `/operate-module` — this locks the output contract to the `ScopeInventory` vocabulary already shipped in `cfdb-query::inventory`.
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A3.3 line 322** — *"cfdb does NOT emit the raid plan markdown. cfdb's responsibility ends at returning a structured infection inventory."* Implication: T1 returns JSON; `/operate-module` owns interpretation. Do not bake trigger routing into cfdb.
- **`CLAUDE.md` §3** — dogfood enforcement: every PR runs `cfdb violations` against `.cfdb/queries/*.cypher` + determinism + cross-dogfood. Implication: the new T1 cypher MUST be byte-stable across runs (`ORDER BY` clauses required per determinism CI).
- **`CLAUDE.md` §2.5** — new-capability issues carry 4-row `Tests:` block (Unit + Self dogfood + Cross dogfood + Target dogfood). Implication: prescription must satisfy all 4 rows or document `Tests: none` rationale per row.

## Split-brain pre-scan (scope-filtered)

`audit-split-brain --workspace . --full-workspace` reports:
- Zero findings in `examples/queries/`, `crates/cfdb-cli/`, `crates/cfdb-query/`, `.cfdb/concepts/`.
- Pre-existing workspace findings (not in scope for #101): `Visibility` `FromStr` delegate-bypass in `crates/cfdb-extractor/src/item_visitor.rs:674`; duplicate `LoadError` enum in `crates/cfdb-concepts/src/lib.rs:210` + `tools/check-prelude-triggers/src/toml_io.rs:11`. These are out-of-scope — do not touch under T1.

**No split-brain risks in T1 scope.**

## Session handoffs (relevant)

- **`state:a0-session:2026-04-20-cfdb-epic-43-complete`** — EPIC #43 fully shipped. PR #121 (#109 enrich_concepts) closed the blocker for T1. Confirmed next work: "Tier 4: #44 Pattern B, #45 Pattern C, #46 partial, #47 signature_divergent UDF; Tier 5: #48 Finding classifier; Other open: #101 T1 concept-unwired, #102 T3 concept-multi-crate (#101/#102 unblocked by #109)." Follow-up noted: **"walk EPIC #34 tier lowest-first" + "no backlog inflation — fix-in-place preferred over filing issues for ambient debt"**.
- **`state:a0-session:2026-04-20-cfdb-backlog-audit`** — filed #101 + #102 as child issues. Lessons: **"Moving pub(crate) items across crates requires promoting to pub — verify with architecture_dep_rule.rs whitelist update, .cfdb/concepts/cfdb.toml crates list update, docs/RFC-cfdb.md references"** (applies if T1 requires new public helpers in cfdb-concepts or cfdb-query).

## Scope override (parent decision)

### IN (actionable this PR)
- AC-1: Land `examples/queries/t1-concept-unwired.cypher` implementing the three sub-verdicts (`CONCEPT_UNWIRED`, `MISSING_CANONICAL_CRATE`, `STALE_RFC_REFERENCE`) against the enriched graph (`:Concept` + `:Item` + `:Crate`).
- AC-3: Exit code = 1 iff findings, 0 when clean (consistent with `cfdb violations` pattern).
- AC-4: Synthetic fixture + integration test asserting exactly 2 findings against a crafted `.cfdb/concepts/<ctx>.toml`.
- AC-5: Self-dogfood proof — run T1 against cfdb's own tree; paste result in PR body (expect zero findings).
- **New CLI surface** `cfdb check --trigger T1` — thin composition over the existing `violations` plumbing, but with a dedicated verb so prescription/consumer skills can bind to `--trigger <ID>` as a stable contract rather than a cypher file path.

### OUT (already shipped differently — DO NOT re-implement)
- AC-2 "emit `:Finding` nodes in the configured store" — no graph-node emission path exists; queries return rows. Prescription must read "emit `:Finding` nodes" as "return rows matching the `Finding` projection shape (`qname, name, kind, crate, file, line, bounded_context`)" — which is what every classifier query on develop already does. Do not add a :Finding graph label.
- Generic cypher-rule dispatch + exit-code wiring — already in `cfdb violations` (`crates/cfdb-cli/src/commands.rs:368-406`). The new `check --trigger T1` verb MUST reuse this plumbing, not duplicate it.

### DEFERRED (route to another issue)
- AC-6 `/operate-module` consumption — gated on #50 merging. This PR lands the T1 signal + JSON shape; #50 consumes it. No wire-up between them lands here.
- AC-7 `DebtClass` extension — frozen taxonomy. T1's three sub-verdicts (`CONCEPT_UNWIRED` / `MISSING_CANONICAL_CRATE` / `STALE_RFC_REFERENCE`) are upstream-of-classifier signals; they live as query output columns, NOT as new `DebtClass` variants. If `/operate-module` (#50) needs to classify them, it does so by mapping T1 rows onto an existing class (addendum §A2.3 routing table is external to the finding). Any taxonomy-extension proposal is a separate RFC amendment.

### UNCLEAR
_(none — the scope is reconcilable without user input; prescription has enough doctrine to decide REUSE vs CREATE per AC.)_

## Forbidden moves

- **Do NOT add a 7th `DebtClass` variant.** Taxonomy is frozen at 6 per addendum §A2.1. T1 sub-verdicts are upstream signals; their values live as query output columns, not as enum variants.
- **Do NOT add a `:Finding` graph node label or any query-execution-writes-to-store mechanism.** Queries return rows. `Finding` is a Rust struct aggregating row projections (`crates/cfdb-query/src/inventory.rs:110-119`).
- **Do NOT duplicate the `cfdb violations` cypher dispatch + exit-code plumbing.** The new `check --trigger T1` verb MUST compose over the existing `violations` codepath or share a common helper; duplicating the dispatch is split-brain.
- **Do NOT bypass `cfdb-concepts::ConceptOverrides::load`** for reading `.cfdb/concepts/*.toml`. The canonical loader is already public in the extracted `cfdb-concepts` crate (per PR #103).
- **Do NOT bake `/operate-module` routing into the cypher or CLI.** cfdb emits structured output; routing is consumer-skill territory (§A3.3).
- **Do NOT skip the determinism `ORDER BY` clause** in the new cypher. CI's `ci/determinism-check.sh` sha256-compares two extract+query runs; missing `ORDER BY` is a regression.
- **Do NOT add trigger-specific fields to `ScopeInventory`** — the `cfdb scope` envelope is §A3.3's contract for `/operate-module`. T1 output is a separate signal reached via `cfdb check`, not a new column on `scope`.
- **Do NOT file follow-up issues for ambient debt surfaced during this work** — fix in place if ≤15 min, per `2026-04-20-cfdb-epic-43-complete` session guidance ("no backlog inflation").
- **Cross-dogfood (`ci/cross-dogfood.sh`) must stay green against graph-specs-rust at the pinned SHA.** T1 is additive; no SchemaVersion bump is needed. If the new cypher is added under `.cfdb/queries/` (ban-rule dir) rather than `examples/queries/` (informational dir), lockstep becomes mandatory — prescription decides the path.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>
