---
issue: 148
issue_state: open
issue_created_at: 2026-04-21T14:02:28Z
refreshed_at: 2026-04-21T20:00:00Z
refreshed_sha: c71b91d
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: c71b91dde774
---

# Context package — Issue #148

## Issue summary

Slice 4 of RFC-034. Ships the CI-level dogfood + determinism proofs for the predicate library:

- `crates/cfdb-cli/tests/predicate_library_dogfood.rs` — integration test that iterates every `.cfdb/predicates/*.cypher` against cfdb's own keyspace with a fixed param set, asserts fixed lower-bound row counts + sorted determinism.
- `ci/predicate-determinism.sh` — builds cfdb, extracts cfdb, runs each predicate twice, asserts byte-identical stdout per predicate (§4.1).
- `.gitea/workflows/ci.yml` — new step wiring `ci/predicate-determinism.sh`. The Rust integration test runs in the existing `cargo test --workspace --all-features` step; no new CI step is needed for it.

## AC freshness

- 🟢 AC-integration-test — `crates/cfdb-cli/tests/predicate_library_dogfood.rs` absent on develop @ `c71b91d`. A sibling `check_predicate_dogfood.rs` from #147 covers two seeds individually; this new test covers ALL seeds uniformly.
- 🟢 AC-determinism-script — `ci/predicate-determinism.sh` absent. `ci/determinism-check.sh` is the shape precedent (different scope — extract not predicate).
- 🟢 AC-ci-step — no existing step in `.gitea/workflows/ci.yml` references predicate-determinism.
- 🟢 AC-blocked-by — #147 merged (PR #154 at `c71b91d`). `cfdb check-predicate` verb is live; the integration test consumes the library API; the script shells out to the binary.

## Commits since issue was filed (scope: crates/cfdb-cli/tests/ ci/ .gitea/workflows/ci.yml .cfdb/predicates/)

- `c71b91d` — Merge PR #154 (#147 Slice 3 — check-predicate verb)
- PRs #152, #153, #154 merges are the prerequisites.

## Relevant PRs

- **Merged:** #152 (Slice 1), #153 (Slice 2), #154 (Slice 3), #150 (RFC parent).
- **Open:** none.

## Sibling open issues

- #149 Slice 5 — docs guide; blocked-by #148.
- #151 — drift god-files follow-up; orthogonal.
- #95, #51, #34 — umbrellas.

## Doctrinal layer

- `docs/RFC-034-query-dsl.md` §7 Slice 4 — scope locked: integration test iterating every seed + `ci/predicate-determinism.sh` + CI step. "Wire both into `.gitea/workflows/ci.yml` as new steps (siblings of the existing `cfdb self-audit` step)."
- `docs/RFC-034-query-dsl.md` §4.1 — "Determinism ... `cfdb check-predicate --name X` on the same keyspace produces byte-identical stdout across runs." Implication: the script's assertion shape is "two runs → diff == ∅".
- `docs/RFC-034-query-dsl.md` §4.3 — "no baseline / ceiling / allowlist file for predicate violations. Every predicate is zero-tolerance against a hard threshold." Implication: the integration test uses loose `>=` assertions on row counts (for forward-compat as source grows); the determinism script has binary pass/fail.
- `council/49/RATIFIED.md` §9 — canonical owner of the predicate library is `.cfdb/predicates/`; canonical owner of the dogfood gate is the CI step we're adding.
- `ci/determinism-check.sh` — shape precedent for the new determinism script (two-run harness + sha256 comparison + exit 1 on mismatch, exit 2 on usage error).
- `crates/cfdb-cli/tests/check_predicate_dogfood.rs` (from #147) — shape precedent for the new integration test (extract cfdb workspace → persist to tempdir keyspace → call `check_predicate` library fn).

## Forbidden moves

- Do NOT add a `.predicate-determinism-baseline.toml` / allowlist file. §4.3 + §6.8.
- Do NOT skip a predicate in the library sweep. Every `.cfdb/predicates/*.cypher` runs in both the integration test and the determinism script; if a new predicate is added that can't produce a deterministic row set, it MUST be excluded in the SAME PR with explicit rationale in the loop.
- Do NOT hard-code row count equality in the integration test. Use `>= N` loose lower bounds — source grows.
- Do NOT shell out to `cfdb check-predicate` from the integration test. The test uses the library API (`cfdb_cli::check_predicate`) — same precedent as `check_predicate_dogfood.rs`. Only the bash script shells out.
- Do NOT change cfdb-core / cfdb-query / cfdb-petgraph / cfdb-concepts. Slice 4 is test + script + CI YAML only.
- Do NOT re-verify Slice 1/2/3 AC in this slice. Those slices shipped their own proofs.
- Do NOT bump SchemaVersion.
- Do NOT add a new Cypher construct or a new verb. This slice is witness-only.

## Parent corrections

_none yet_
