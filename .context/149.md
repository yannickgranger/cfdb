---
issue: 149
issue_state: open
issue_created_at: 2026-04-21T14:02:38Z
refreshed_at: 2026-04-21T20:40:00Z
refreshed_sha: 779ec6a
verdict: current
repo: agency:yg/cfdb
---

# Context package — Issue #149

## Issue summary

Slice 5 of RFC-034 — final docs slice. Ships `docs/query-dsl.md` with the canonical-examples gallery + param-resolver syntax + "how to add a predicate" runbook + the mandatory `Predicate`/`predicate` homonym note (ddd-specialist R1 ratification condition). Cross-updates `.cfdb/predicates/README.md` and the CLI verb inventory.

## AC freshness

- 🟢 AC-docs-file — `docs/query-dsl.md` absent on develop @ `779ec6a`. Verified: `ls docs/query-dsl.md` fails.
- 🟢 AC-homonym-note — ddd-specialist R1 ratification condition that this slice document the `cfdb_core::query::Predicate` (AST node) vs `.cfdb/predicates/*.cypher` (file) homonym. New section in the guide.
- 🟢 AC-cli-inventory — `crates/cfdb-cli/src/main.rs` module docstring lists 20 verbs across INGEST/RAW/TYPED/SNAPSHOT/SCHEMA/AUX groups. `cfdb check-predicate` is absent (this slice adds it); `cfdb check --trigger` is ALSO absent on develop (pre-existing docs drift from #101 that this PR boy-scouts — sub-15-minute fix per ~/.claude/CLAUDE.md §7 Fix-or-File rule).
- 🟢 AC-predicates-readme-xref — `.cfdb/predicates/README.md` does not reference `docs/query-dsl.md` today. New link + deferred-predicate cross-ref.

## Commits since issue was filed

- `779ec6a` — Merge PR #155 (#148 Slice 4 predicate-library dogfood + determinism CI)
- `c71b91d` — Merge PR #154 (#147 Slice 3 check-predicate verb)
- `fa3be53` — Merge PR #153 (#146 Slice 2 predicates seed library)
- `6497beb` — Merge PR #152 (#145 Slice 1 param_resolver)

All four prerequisites landed. Nothing else since issue filing touches `docs/` or `.cfdb/predicates/`.

## Relevant PRs

- **Merged:** #150 (RFC parent), #152, #153, #154, #155.
- **Open:** none.

## Sibling open issues

- #151 — drift god-files follow-up; orthogonal.
- #95, #51, #34 — umbrellas/blocked.

With #149 merged, #49 (parent EPIC of the Slice 1-5 chain) auto-closes via supersession and #49's five sub-issues all close. RFC-034 is complete.

## Doctrinal layer

- `docs/RFC-034-query-dsl.md` §7 Slice 5 — scope locked: `docs/query-dsl.md` with canonical examples / param-resolver syntax / runbook / homonym note / deferred-predicate note. Update RFC-cfdb.md §11 CLI inventory (real locus is `main.rs` module docstring — §11 is a consumer table). Update `.cfdb/predicates/README.md` to cross-ref.
- `council/49/RATIFIED.md` §5 (load-bearing decision 9) — "Homonym accepted. `Predicate` (AST node) vs `predicate` (file) — same bounded context, different layers. Documentation duty in Slice 5." This slice discharges the duty.
- `council/49/RATIFIED.md` §5 (load-bearing decision 5) — "Re-export predicate DEFERRED to a future RFC." The guide documents this as a non-goal so downstream readers know not to expect it from the current library.
- `CLAUDE.md` (project) §1 — "Docs, CI config, chore | Issue → direct PR → dogfood-gated PR". Implication: no `/discover` + `/prescribe` full pipeline; this is `/ship` fast-path eligible (all `.md` except main.rs docstring, which is inside a .rs file).
- `CLAUDE.md` (project) §2.5 — "Docs / CI / chore | No test required; the change is its own verification surface."

## Split-brain pre-scan (scope-filtered)

No new types, no new code. Scope is markdown + one module-docstring edit. No split-brain risk.

## Session handoffs (relevant)

- `a0-session:2026-04-21-763cbbbb-cfdb-oss-queue` — continues directly. RFC-034 is Slice 1-5 shipped as #145-#149; this is the final slice.

## Forbidden moves

- Do NOT add new behaviour. Every factual claim in `docs/query-dsl.md` must reflect code that shipped in #145/#146/#147/#148 — no forward-promises.
- Do NOT restore the deferred "re-export-without-adapter" predicate example. Per council/49/RATIFIED.md §5: it is Phase B / HIR future work. Document as non-goal only.
- Do NOT collapse `cfdb_core::query::Predicate` and the `.cfdb/predicates/*.cypher` concepts. Per RATIFIED §5 (load-bearing decision 9) + ddd-specialist verdict, they are a deliberate same-context-different-layer homonym; the guide documents the distinction in plain prose, not by cross-aliasing.
- Do NOT touch RFC-cfdb.md §6 (the ratified v0.1 API contract). The scope note in the issue body says "§11 CLI verb inventory" which actually lives in `crates/cfdb-cli/src/main.rs` module docstring — update THAT, not the §6 API table.
- Do NOT add a `--baseline`/`--allowlist` mention anywhere in the guide as "available" — they are non-goals per §4.3 + §6.8.
- Do NOT boy-scout unrelated docs. Adding `cfdb check --trigger` to the CLI inventory is in-scope (pre-existing drift of < 15 min); touching other RFC files is out of scope.
- Do NOT bump SchemaVersion.

## Parent corrections

_none yet_
