---
issue: 137
issue_state: open
issue_created_at: 2026-04-20T22:02:57Z
refreshed_at: 2026-04-21T14:00:00Z
refreshed_sha: 4864b37
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 4864b37df6ac
---

# Context package — Issue #137

## Issue summary

Extend the `graph-specs` anti-drift gate in `.gitea/workflows/ci.yml` to cover `tools/check-prelude-triggers/` — currently the gate only walks `crates/`, so renames of that tool's `pub` types (emitted in the `.triggers/<issue>.json` envelope consumed by `/freshness` Step 2g) would pass CI silently.

## AC freshness

- 🟢 AC1 — `graph-specs check --specs specs/concepts/ --code crates/ --code tools/` exits 0 on develop. **Reinterpretation needed:** `graph-specs check` (tested locally at `graph-specs 0.x`, `--help` shows `--code <CODE>` as a single arg) does NOT accept multiple `--code` values. The issue body's fallback clause ("If not, run the check twice and `|| exit 1`") applies; the shipped CI must run two passes. This is a mechanics refinement, not a scope reduction.
- 🟢 AC2 — Renaming any `pub` type in `tools/check-prelude-triggers/src/*.rs` fails the CI gate until the matching `specs/concepts/check-prelude-triggers.md` section is updated.
- 🟢 AC3 — `spikes/qa5-utc-now/` remains excluded — a deliberate `pub` rename there still passes CI. Confirmed baseline: `graph-specs check --specs specs/concepts/ --code .` on HEAD reports `FileStats`, `Subclass`, `Totals` (spikes-local types) as unspec'd, which is why `--code .` is NOT a valid shipping strategy.
- 🟢 AC4 — PR body names which types are now gated (reviewer sanity).

## Commits since issue was filed (scope: .gitea/workflows/ci.yml specs/concepts/ tools/check-prelude-triggers/ specs/README.md)

- `e41365e` — `docs(specs): add TriggerId + UnknownTriggerId spec entries for #101` (adds `cfdb-cli::TriggerId` spec; does NOT touch `tools/`)
- `929d108` — `feat(cfdb-concepts,cfdb-extractor): .cfdb/published-language-crates.toml loader + :Crate.published_language prop (#100)` (adds spec entries to `cfdb-concepts.md`; unrelated to tool gate widening)

Neither commit collides with #137. Both are pure additions to the spec set under the current `--code crates/` gate.

## Relevant PRs

- **Merged:** (all recent cfdb PRs already reflected in the commit log above)
- **Open:** none

## Sibling open issues

- #128 — `refactor(cfdb-*): split 8 pre-existing god-files` — orthogonal (crates only, not tools/)
- #95, #34, #51 — umbrellas/blocked, none touch tools/ or .gitea/workflows/ci.yml
- #49 — cfdb-query-dsl — orthogonal

## Doctrinal layer

- `docs/RFC-030-anti-drift-gate.md:141` — the RFC's canonical invocation is `graph-specs check --specs specs/concepts/ --code crates/`. Implication: the narrow `--code crates/` path is what the RFC originally ratified; onboarding `tools/` is a scoped amendment the PR body must explain.
- `docs/RFC-030-anti-drift-gate.md:532` — G2 gate: "graph-specs check ... exits 0 on the main branch". Implication: the widened invocation must still satisfy G2 on develop — zero violations across both passes, no ratchet file.
- `CLAUDE.md` (project) §3 — "Adding a new ban rule is an RFC-gated change." Implication: this issue is NOT adding a new ban rule; it is extending the SCOPE of the existing rule. Covered by the issue's own archaeology; no new RFC required.
- `CLAUDE.md` (project) §1 — "Docs, CI config, chore | Issue → direct PR → dogfood-gated PR". Implication: this issue is CI config + spec docs; the normal full-gate pipeline is NOT required. Discovery + prescription + ship are sufficient; BDD/TDD/reality gates do not apply (no Rust code changes).
- `CLAUDE.md` (project) §5 — "the recall crate holds cfdb to the rustdoc ground truth. Any RFC that adds a new fact type MUST extend the recall corpus." Implication: N/A — this issue adds no fact types.
- `tools/check-prelude-triggers/src/lib.rs:18-21` — `"Homonym note: the C* IDs here are RFC-034 mechanical pre-council triggers and are distinct from cfdb's internal Cypher-query triggers T1/T3 (cfdb issues #100/#101)."` Implication: `check-prelude-triggers::TriggerId` and `cfdb-cli::TriggerId` are homonyms BY DESIGN. The new spec file must describe only the `C*` enum; it must NOT merge or cross-reference the `T*` enum in `cfdb-cli.md`.
- `tools/check-prelude-triggers/src/toml_io.rs:3-4` — `"Pattern shape cloned from cfdb-concepts/src/lib.rs:165-220 (REUSE-of-shape per prescription D3; NO cfdb-concepts dependency per Forbidden move #8)."` Implication: `check-prelude-triggers::toml_io::LoadError` and `cfdb-concepts::LoadError` are a deliberate duplication. The new spec must describe the tool's `LoadError` on its own terms; it must NOT collapse the two into a single concept.

## Split-brain pre-scan (scope-filtered)

The scope touches `.gitea/workflows/ci.yml`, `specs/concepts/`, and the addition of a spec for `tools/check-prelude-triggers`. There are two pre-existing deliberate duplications in the tool's scope:

1. `check-prelude-triggers::TriggerId` (C1/C3/C7/C8/C9) ↔ `cfdb_cli::TriggerId` (T1/T3). Documented homonym; spec filing must not collapse.
2. `check-prelude-triggers::toml_io::LoadError` ↔ `cfdb_concepts::LoadError`. Documented duplication-by-design (Forbidden move #8); spec filing must describe as an independent concept.

The `audit-split-brain` run reported no NEW risks — only the two deliberate duplications above, which the tool source already marks.

## Session handoffs (relevant)

- `a0-session:2026-04-21-cfdb-triggers-t1-t3-shipped` — lesson: "DDD insight: same type name across different bounded contexts is ALLOWED when the concepts are independent (different change vectors). cfdb_cli::check::TriggerId (editorial-drift T1/T3) vs check_prelude_triggers::trigger_id::TriggerId (RFC-034 mechanical C1..C9) — two bounded contexts, two independent enums, no split-brain. audit-split-brain flags it but marks `delegates: true` on the clap wrapper, confirming the delegation is legitimate." This is directly load-bearing for the new spec file — the two `TriggerId` concepts must be spec'd as independent.

## Scope override (parent decision)

verdict=current — no override. The only refinement is AC1's mechanics: `graph-specs check` does NOT support multiple `--code` args, so the two-pass fallback is the shipping form. Two valid implementations satisfy the acceptance criteria:

- Option A: disjoint spec subtrees — `specs/concepts/` (crates) + `specs/tools/` (tools) + two CI passes.
- Option B: disjoint spec subtrees under one root — `specs/concepts/crates/` + `specs/concepts/tools/` + two CI passes.
- Option C: a single `--code .` pass — REJECTED because it picks up `spikes/qa5-utc-now/` (confirmed: three types leak through) and the issue forbids that.

Prescription picks between A and B.

## Forbidden moves

- Do NOT restore the spec file `specs/concepts/qa5-utc-now-spike.md` that commit `15f7b7a` added and `ff00b4a` reverted. `spikes/qa5-utc-now/` is legitimately experimental and stays excluded.
- Do NOT add a `cfdb-concepts` dependency to `tools/check-prelude-triggers/Cargo.toml`. The `LoadError` duplication is intentional (Forbidden move #8 in the tool's own prescription).
- Do NOT merge, alias, or cross-reference the `check-prelude-triggers::TriggerId` and `cfdb_cli::TriggerId` concepts. They are a deliberate DDD homonym across bounded contexts.
- Do NOT collapse `check-prelude-triggers::toml_io::LoadError` with `cfdb_concepts::LoadError` in the spec set. They are independent concepts (separate change vectors, separate owners).
- Do NOT use `--code .` as the shipping CI invocation. It picks up `spikes/qa5-utc-now/` types (`FileStats`, `Subclass`, `Totals`) and would re-introduce the boy-scout trap that ff00b4a rolled back.
- Do NOT onboard additional `tools/` entries in this PR. Only `check-prelude-triggers` is in scope; future tools need their own follow-up.
- Do NOT alter the fact-graph / wire format / schema version. This is CI + docs only; no `cfdb-core::SchemaVersion` touch.
- Do NOT introduce a baseline/ceiling/allowlist file for violations. Zero-tolerance; any pre-existing spikes/ leakage must be excluded by structure, not waived by a baseline.
- Do NOT run BDD/TDD gates for this issue — it is pure CI + docs (project CLAUDE.md §1 routing). The proof is the dogfood run, not a RED→GREEN test cycle.

## Parent corrections

### Correction @ 2026-04-21T14:20:00Z
**Commit:** (this commit)
**Reason:** Empirical — during local dogfood verification, `graph-specs check --specs specs/tools/ --code tools/` reported `missing in code: SCHEMA_VERSION (specs/tools/check-prelude-triggers.md:11)`. graph-specs' extractor reads **types** (struct/enum) from Rust code, not `const` items. A spec H2 section whose title is a `const` name will always trip the reverse-direction check.
**Scope:** Prescription D3 — retract the `## SCHEMA_VERSION` H2 section requirement. The sentinel is instead described inline inside the `## PreludeTriggerReport` section body (it is a frozen envelope field, not an independently-owned concept).
**Body:** The tool's gated pub surface for the anti-drift gate is 4 types, not 5: `PreludeTriggerReport`, `TriggerId`, `TriggerOutcome`, `LoadError`. `SCHEMA_VERSION` is mentioned as a field on `PreludeTriggerReport`. The PR body's "types now gated" enumeration MUST reflect 4 entries. This correction supersedes the dogfood-gate checklist item in `.prescriptions/137.md` that demanded the 5th H2 section.

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#137): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.

Past corrections are never edited or deleted. If a correction was wrong, append a new correction that retracts it. `git log .context/137.md` is the audit trail.
