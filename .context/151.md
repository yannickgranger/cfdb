---
issue: 151
issue_state: open
issue_created_at: 2026-04-21T15:27:15Z
refreshed_at: 2026-04-21T21:00:00Z
refreshed_sha: 6e04d1a
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 6e04d1a2076c
---

# Context package — Issue #151

## Issue summary

Split the 4 drift god-files that appeared on develop AFTER #128 filing: `crates/cfdb-cli/src/{check.rs 639, commands.rs 904, scope.rs 589}` + `crates/cfdb-hir-extractor/src/entry_point_emitter.rs 784`. Mechanical layout-only refactor mirroring the #128 contract: no behaviour change, no `pub` item rename, no wire-format touch. Same pattern as PR #144.

## AC freshness

- 🟢 All 4 listed files still over 500-LOC threshold on develop @ `6e04d1a` (`wc -l` verified).
- 🟢 `quality-architecture --workspace .` reports exactly these 4 god-files + zero others. Post-split target: 0 god-files workspace-wide.

## Commits since issue was filed

None touch the 4 scope files since #151 was filed (2026-04-21T15:27:15Z) — only docs/CI PRs (#150 → #156) landed.

## Relevant PRs

- **Merged (predecessor pattern):** #144 (#128 8 god-file splits) — direct precedent. Fan-out + single integration compile + scope-faithful.
- **Open:** none in scope.

## Sibling open issues

- #95, #51, #34 — all umbrellas/blocked; none touch the 4 scope files.

## Doctrinal layer

- `~/.claude/CLAUDE.md` §7 Fix-or-File — each split is a >15-min module-carve exercise. Tracker-filed per the rule.
- `~/.claude/CLAUDE.md` §6.8 — no metric ratchets. No `.god-files-allowlist.toml`; the only admissible fix is to split.
- `CLAUDE.md` (project) §2.5 — work type "Mechanical refactor": no new tests required; the existing suite must pass byte-identically (the invariant the refactor preserves).
- `CLAUDE.md` (project) §4 — "Rename / move / dedup / file split → `/fix-mechanical`; N parallel mechanical refactors → `/sweep-epic`". 4 parallel files → sweep-epic pattern.
- **Correction lesson from #128's `.context/128.md`:** "fact graph byte-identical pre/post" is NOT a valid invariant for file splits — new `:File`/`:Module` nodes are inevitable. The authoritative invariants are (a) DETERMINISM (two post-split extracts same SHA), (b) behaviour preservation (all tests pass at same counts), (c) god-files = 0 post-split. This correction carries forward to #151.
- `docs/RFC-cfdb.md` §12.1 (G1 determinism) — still holds.

## Split-brain pre-scan (scope-filtered)

Two pre-existing deliberate duplications that MUST be preserved:
1. `TriggerId` homonym (cfdb-cli `check.rs:90` T1/T3 vs check-prelude-triggers C1/…/C9) — documented in check.rs doc comment; must stay distinct.
2. `LoadError` documented clones (cfdb-concepts / check-prelude-triggers / check.rs wraps none — not in scope).

`check.rs` contains the canonical `impl FromStr for TriggerId` at line 123. `main_parse.rs:29` is the delegation wrapper (`delegates: true`). The split MUST keep the canonical FromStr at `cfdb_cli::check::TriggerId::from_str` — i.e. the pub impl stays in the post-split module hierarchy at the same qname.

No new split-brain risks. Visibility: every `pub` item in all 4 files keeps its original public path.

## Session handoffs (relevant)

- `a0-session:2026-04-21-763cbbbb-cfdb-oss-queue` — #128 was the direct precedent. Load-bearing lessons:
  - Fan-out text-only sub-agents + main integrates once. Zero compile errors on first try when discipline held.
  - Determinism (not byte-identical) is the refactor invariant.
  - #128 faithful-scope approach: list 8 in the issue body; the 4 drifting files got their own issue (THIS issue).

## Forbidden moves

- Do NOT behaviourally change any of the 4 files. Move-only.
- Do NOT rename any `pub` type / `pub fn` / `pub(crate)` item. All four files expose load-bearing public surfaces (`check`, `TriggerId`, `UnknownTriggerId`, `query`, `extract`, `list_callers`, `violations`, `scope`, `extract_entry_points`, etc.). Preserve every symbol name at its original module path.
- Do NOT collapse the `TriggerId` homonym with `check_prelude_triggers::TriggerId`. Keep them distinct.
- Do NOT break the `parse_trigger_id` delegation at `crates/cfdb-cli/src/main_parse.rs:29` — it must still call `<TriggerId as FromStr>::from_str` resolved against `crates/cfdb-cli/src/check*` after the split.
- Do NOT introduce a `.god-files-allowlist.toml` or any ceiling/baseline file (~/.claude/CLAUDE.md §6.8).
- Do NOT change `cfdb-core::SchemaVersion` or any fact-graph vocabulary. Layout-only surgery.
- Do NOT edit `#[cfg(feature = "...")]` gates (`hir` feature boundaries on `extract_hir` / `entry_point_emitter`). The cfg surface is load-bearing for default-build cold-compile cost (RFC-032 §3).
- Do NOT touch `check-prelude-triggers`, `cfdb-core`, `cfdb-petgraph`, `cfdb-extractor`, `cfdb-query`, `cfdb-concepts`. All edits stay within `cfdb-cli` + `cfdb-hir-extractor`.
- Do NOT split a file unless every resulting module is <500 LOC. The check is the `quality-architecture` threshold.
- Do NOT pursue RFC-034 follow-ups here — #151 is mechanical only. The re-export predicate / HIR Phase B / UDF mechanism deferrals from RFC-034 stay deferred.
- Do NOT run `cargo` from sub-agent worktrees. Text-only edits; the integration worktree does the single compile.

## Parent corrections

_none yet_
