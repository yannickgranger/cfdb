---
issue: 48
issue_state: open
issue_created_at: 2026-04-19T11:00:11Z
refreshed_at: 2026-04-20T17:00:00Z
refreshed_sha: 8547d85
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 8547d85d1519
---

# Context package — Issue #48

## Issue summary

Tier-5 keystone: wire the 6-class `DebtClass` taxonomy into `cfdb scope` by
authoring per-class classifier `.cypher` rules + a `.cfdb/skill-routing.toml`
policy file. Replaces the per-class `class_v01_unavailable_note` warnings in
`cfdb-cli/src/scope.rs` with real classifier output sourced from the
enrichment passes shipped in #43. `:Finding` keeps the DIP-clean shape — no
`fix_skill` field.

## AC freshness

- 🟢 OPEN — **`examples/queries/classifier.cypher` or per-class queries.** No classifier rules on develop under `examples/queries/classifier-*.cypher`. Prior session wrote 6 per-class files uncommitted in this worktree (decision permitted by issue body "or per-class queries — decide at discovery").
- 🟢 OPEN — **`:Finding {id, class, context, qnames[], evidence, confidence}` populator wired against existing Finding struct.** Issue body scopes this to "the EXISTING `Finding` struct" — confirmed at `crates/cfdb-query/src/inventory.rs:110` on develop the struct has the v0.1 column set (qname/name/kind/crate_name/file/line/bounded_context). Rich-schema fields (id/evidence/confidence) are NOT on develop and are deferred per §A2.2 → v0.3 per prior-session docs/cfdb-classifier.md line 166-169.
- 🟢 OPEN — **`.cfdb/skill-routing.toml` seed file mapping class → skill.** Absent on develop; prior session seeded all 6 classes uncommitted.
- 🟢 OPEN — **Architecture test `:Finding` contains no skill-related fields.** No such test on develop; prior session wrote `crates/cfdb-query/tests/finding_no_skill_field.rs` uncommitted.
- 🟢 OPEN — **Wire each class to its rule.** All 6 per-class rules + `populate_findings_by_class` orchestrator uncommitted in prior session's scope.rs diff (+208/-31).

**All AC items are 🟢 OPEN on develop (verdict=current).** The prior session's uncommitted implementation in this worktree is a proposed resolution, not existing state; it is reviewed/reaffirmed in `/discover` + `/prescribe`, not assumed correct.

## Commits since issue was filed (scope: classifier + scope.rs + cfdb-query + examples/queries + .cfdb/)

- `4a3d807` feat(cfdb): signature_divergent UDF + fn signature emission (#47) — SHIPPED blocker input for Context Homonym class
- `358b54f` Merge PR #134 / `5128f46` — canonical-bypass Pattern C (#45) — SHIPPED blocker input for Canonical Bypass class (classifier can also reuse the concept-specific ledger-canonical-bypass rule)
- `53c042a` feat(cfdb): vertical-split-brain.cypher (Pattern B) + scar tests (#44) — SHIPPED blocker input for Random Scattering class
- `dcd7b91` fix(#127): register check-prelude-triggers in .cfdb/concepts/cfdb.toml — out-of-scope but present
- `72ce3e7` feat(cfdb-extractor,cfdb-core): Slice #42 — impl-block `:Items` + IMPLEMENTS edges + V0_2_2
- `9b5e93d` refactor(cfdb-*): drain 53 pre-existing quality-metrics violations → 0 (closes #111)
- `81ea6ab` refactor(cfdb-concepts): extract shared bounded-context resolver crate (#3)
- (many more — see `git log origin/develop --since=2026-04-19T11:00:11Z` for full list; every enrichment pass #105/#107/#108/#109/#110 and HIR emission #84/#89/#92/#93/#97/#98/#124/#125/#126 shipped)

**Key takeaway:** every declared blocker (#43, #44, #45, #47) is merged. #46 is still technically open but its foundation (crate-prefix heuristic + `.cfdb/concepts/*.toml` override + enrich_bounded_context pass) shipped via PR #119 (Slice 43-E) — the remaining "v0.2-9 ≥95% accuracy gate" line item was met at 100%/100% in that PR per session state `2026-04-20-cfdb-epic-43-complete`.

## Relevant PRs

- **Merged blockers (all enrichment signals populated on develop):**
  - #112 Slice 43-A — EnrichBackend rename/additive
  - #113 Slice 43-C — `#[deprecated]` fact extraction
  - #117 Slice 43-B — `enrich_git_history` + git2 feature
  - #118 Slice 43-D — `enrich_rfc_docs` + V0_2_3
  - #119 Slice 43-E — `enrich_bounded_context` + v0.2-9 gate (100%/100%)
  - #121 Slice 43-F — `enrich_concepts` (:Concept + CANONICAL_FOR)
  - #122 Slice 43-G — `enrich_reachability` (BFS + reachable_from_entry)
  - #133 Pattern B `vertical-split-brain.cypher`
  - #134 Pattern C `canonical-bypass.cypher` (4-verdict generalized)
  - #135 `signature_divergent` UDF + fn signature emission
- **Open:** none.
- **Closed (not merged):** #120 (RFC-034 skills; routed elsewhere), #116 (concept-registry WIP).

## Sibling open issues

- #128 refactor(cfdb-*): split 8 pre-existing god-files (>500 lines) — orthogonal debt
- #102 Trigger T3 (concept-name-in-≥2-crates query)
- #101 Trigger T1 (concept-declared-in-toml-but-missing-in-code)
- #100 `.cfdb/published-language-crates.toml` marker loader
- #96 `cfdb extract --rev <url>@<sha>` — Option W
- #95 v0.2-phase1 subset for qbot-core drift-lock
- #51 RFC-029 addendum merge
- #50 feat(operate-module): skill spec + raid plan template — **consumes** #48 output
- #49 feat(cfdb-query-dsl): grammar + parser + execution engine — orthogonal
- #46 enrich_bounded_context heuristic + .cfdb/concepts/*.toml override mechanism — **functionally subsumed by #108/PR #119**; should be closed as "subsumed" or narrowed further
- #34 EPIC parent — v0.2 implementation umbrella (this is the parent of #48)

## Doctrinal layer

- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.1** (DRAFT — SECOND-PASS COUNCIL APPROVED GREEN 2026-04-14). Defines the 6-class taxonomy (Duplicated Feature / Context Homonym / Unfinished Refactor / Random Scattering / Canonical Bypass / Unwired). Fix strategy column holds *abstract* actions; concrete skill mapping lives in `SkillRoutingTable` (council BLOCK-1 / solid-architect) — the classifier MUST NOT embed `fix_skill` in `:Finding`.
  - Implication: every classifier rule emits only structural columns; skill routing is external TOML consumed by downstream orchestrators (`/operate-module`, `/boy-scout --from-inventory`).
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.2** (council BLOCK-1 clean-arch). Classifier is a TWO-STAGE pipeline: enrichment passes (shipped via #43 sub-slices) THEN a Cypher query joining the enriched graph. Per-class `.cypher` rules are permitted if the single `classifier.cypher` UNION form is parser-gap-blocked.
  - Implication: per-class file decomposition is RFC-sanctioned (Cypher parser has no `UNION`/`CASE WHEN` in v0.1); single-rule form is a v0.3 consolidation.
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.2 Invariant I6.** "Stage-2 classifier (issue #48) MUST NOT be deployed until `enrich_bounded_context` (slice 43-E) hits the v0.2-9 ≥95% accuracy gate." Gate met at 100%/100% by PR #119 per session memory `2026-04-20-cfdb-epic-43-complete`.
  - Implication: I6 is cleared. #48 may proceed.
- **`docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.3 (implied from council BLOCK-1 fix).** `SkillRoutingTable` owns class → skill mapping external to the graph. Council-ratified guardrails: ContextHomonym NEVER `/sweep-epic`; UnfinishedRefactor → `/sweep-epic --mode=port` (Q14 vote); RandomScattering → `/boy-scout` (NOT parallelized); Unwired → `/boy-scout` delete (orchestrator routes `TODO(#N)`-tagged items to issue owner).
  - Implication: `.cfdb/skill-routing.toml` content is mostly pre-decided; the contribution is schema design + seed file, not policy invention.
- **`CLAUDE.md` (repo-local) §2.5 Tests template.** New-capability issues MUST carry four rows: Unit / Self dogfood / Cross dogfood / Target dogfood.
  - Implication: #48 PR needs all four surfaces. Self-dogfood = classifier-taxonomy fixture test (prior session wrote `crates/cfdb-cli/tests/classifier_taxonomy.rs` uncommitted); Cross-dogfood = `ci/cross-dogfood.sh` exit 0 on graph-specs-rust at pinned SHA (adding classifier rules does NOT bump SchemaVersion — pure query-layer addition); Target-dogfood = manual measurement on qbot-core, report bucket sizes in PR body.
- **`CLAUDE.md` (repo-local) §3 Dogfood enforcement.** Adding NEW ban rules under `.cfdb/queries/` is RFC-gated; classifier rules land under `examples/queries/` (non-ban-rule directory) — no RFC needed for the rule files themselves. `SchemaVersion` stays at V0_2_3 (current) because no new node/edge/attr is written by the classifier; it only queries.

## Split-brain pre-scan (scope-filtered)

Ran `audit-split-brain --workspace . --full-workspace --format json` on the worktree (includes prior session's uncommitted work):

- `Visibility` FromStr bypass at `crates/cfdb-extractor/src/item_visitor.rs:674` (`parse_syn_visibility`) — **pre-existing, `delegates: true`.** Per global `CLAUDE.md` §11 lesson: `fromstr_bypasses` with `delegates:true` are false positives. Not in #48 scope. NOT to be touched.
- Duplicate type def `LoadError` across `crates/cfdb-concepts/src/lib.rs:210` and `tools/check-prelude-triggers/src/toml_io.rs:11` — **pre-existing, out of #48 scope.** Note: prior session's `cfdb-query/src/skill_routing.rs` defines `SkillRoutingLoadError` with a DIFFERENT name, so the classifier work does NOT compound this split-brain.
- No new overlaps surfaced by the classifier implementation. No `match_arm_overlaps`, no `const_table_overlaps`, no `split_resolutions` in scope.

## Session handoffs (relevant)

- **`state:a0-session:2026-04-20-cfdb-epic-43-complete`** — EPIC #43 closed, all 6 enrichment signals populated on develop. Lists `#48 Finding classifier (all 6 enrichment signals now populated)` as the Tier-5 next step. Lessons carried forward:
  - `feedback:cfdb:no-sha-dance` — lockstep graph-specs PRs pin ONCE at PR HEAD. (Not relevant here — no SchemaVersion bump.)
  - `feedback:cfdb:chronological-priority` — walk EPIC #34 tiers lowest-first. (#48 is Tier 5; remaining Tier 4 #46 is functionally subsumed.)
  - `feedback:cfdb:no-backlog-inflation` — fix-in-place preferred over filing for ambient debt.
- **`state:a0-session:2026-04-20-cfdb-backlog-audit`** — Sprint 1 complete + Sprint 2 architect decomposition of #43 done. Confirms `cfdb-concepts` is the shared bounded-context resolver (PR #103); #48 imports via `cfdb_query::inventory`.

## Forbidden moves

- **DO NOT add `fix_skill` / `recommended_skill` / any skill-mapping column to `:Finding`.** Council BLOCK-1 + solid-architect DIP verdict. Pinned by architecture test `crates/cfdb-query/tests/finding_no_skill_field.rs`.
- **DO NOT route `ContextHomonym` to `/sweep-epic` anywhere** — in `.cfdb/skill-routing.toml`, in docs, in code. Mechanical dedup on a homonym destroys bounded-context isolation (DDD Q3). Route is `/operate-module` with `council_required = true`.
- **DO NOT bump `cfdb-core::SchemaVersion`.** The classifier only queries the graph; no new nodes, edges, or attrs are materialized. Any PR that proposes a bump is a bug — triggers a graph-specs lockstep obligation unnecessarily.
- **DO NOT re-implement `compute_bounded_context` / `WELL_KNOWN_PREFIXES` / `.cfdb/concepts/*.toml` loading.** Canonical resolver is `cfdb-concepts` crate (PR #103 / issue #3). Classifier consumes `:Item.bounded_context` downstream via the enriched graph — no direct TOML parsing in the classifier path.
- **DO NOT add a per-class "degradation" silence mechanism that hides empty buckets.** Per `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.2, empty buckets MUST carry a per-class warning naming the missing input (HIR / concepts / reachability). The `class_empty_bucket_note` function in the prior session's scope.rs diff is the correct shape; do not regress it to a silent skip.
- **DO NOT reintroduce `class_v01_unavailable_note` in its original form.** The v0.1 warning set ("classifier unavailable for class X") is the thing being REPLACED. The new warnings are about *empty buckets* (zero findings) not *unavailable classifiers* (no rule).
- **DO NOT add new ban rules under `.cfdb/queries/*.cypher` in this PR.** Per CLAUDE.md §3, new ban rules are RFC-gated. Classifier rules land under `examples/queries/classifier-*.cypher` — these are query examples, not dogfood gates.
- **DO NOT parallelize `/sweep-epic` for `RandomScattering`.** Council-ratified: `random_scattering` routes to `/boy-scout` (extract helper inline during adjacent work). Do not override in the TOML.
- **DO NOT use `git push` or create PRs outside `/ship`.** Global rule; absolute.
- **DO NOT implement the RFC-§A2.2 rich `:Finding` schema (id / evidence[] / confidence / qnames[]) in this PR.** Issue body AC-2 scopes populator against the EXISTING `Finding` struct (v0.1 column set already on develop at `crates/cfdb-query/src/inventory.rs:110`). Rich schema is v0.3+ per the prior session's `docs/cfdb-classifier.md` §"Follow-ups deferred to v0.3+". Splitting this constraint protects the ship boundary.
- **DO NOT edit or "refresh" `.proofs/` files by hand.** Global §6 rule 7 — tool-owned artifacts. Gate agents write them; we do not.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#48): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.

Past corrections are never edited or deleted. If a correction was wrong, append a new correction that retracts it. `git log .context/48.md` is the audit trail.
