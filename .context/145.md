---
issue: 145
issue_state: open
issue_created_at: 2026-04-21T14:01:36Z
refreshed_at: 2026-04-21T16:00:00Z
refreshed_sha: 0845a32
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 0845a32b3d72
---

# Context package — Issue #145

## Issue summary

Slice 1 of RFC-034. Add `crates/cfdb-cli/src/param_resolver.rs` with `ParamResolveError` enum + `resolve_param` + `resolve_params` `pub(crate)` fns supporting 4 param forms (`context:` / `regex:` / `literal:` / `list:`). Add `cfdb-concepts` as a new direct dep on `cfdb-cli`. Invariant §4.6: delegate `.cfdb/concepts/*.toml` reading to `cfdb_concepts::load_concept_overrides` — NO inline TOML parser.

## AC freshness

- 🟢 AC-add-module — `crates/cfdb-cli/src/param_resolver.rs` does not exist on develop (`ls crates/cfdb-cli/src/param_resolver.rs` → not found).
- 🟢 AC-add-dep — `cfdb-concepts` is NOT currently listed in `crates/cfdb-cli/Cargo.toml` (verified `grep cfdb-concepts crates/cfdb-cli/Cargo.toml` returns nothing on develop @ 0845a32). Adding it is new.
- 🟢 AC-4-forms — no existing resolver covers these 4 forms; there is no `ParamResolveError` anywhere on develop (only appears in RFC-034 + council artifacts; no code definition). Searches exhausted: `rg "ParamResolveError|param_resolver" --type=rs` returns zero hits in `crates/` or `tools/`.
- 🟢 AC-delegates-to-concepts — `cfdb_concepts::load_concept_overrides` is the canonical loader and is already `pub fn` at `crates/cfdb-concepts/src/lib.rs:216`. No alternative loader exists.
- 🟢 AC-test-surface — prescribed Tests: block is present in the issue body; no conflicting tests exist.

## Commits since issue was filed (scope: crates/cfdb-cli/src/ crates/cfdb-cli/Cargo.toml crates/cfdb-concepts/src/)

None. Issue was filed 2026-04-21T14:01:36Z as part of RFC-034 decomposition; no commits have landed between filing and `refreshed_sha=0845a32` (0845a32 is the RFC-034 merge itself, which added no Rust code).

## Relevant PRs

- **Merged:** PR #150 (RFC-034 — the parent RFC, docs+council only, no Rust)
- **Open:** none for #145 yet

## Sibling open issues

- #146 — Slice 2 (`.cfdb/predicates/` seeds + schema-ref static check); parallel-shippable with this, no file overlap
- #147 — Slice 3 (`check-predicate` verb); blocked by #145 + #146
- #148 — Slice 4 (dogfood CI); blocked by #147
- #149 — Slice 5 (user-guide docs); blocked by #148
- #151 — drift-god-files follow-up from #128 (orthogonal; cfdb-cli scope overlap but on separate files — check.rs, commands.rs, scope.rs)

## Doctrinal layer

- `docs/RFC-034-query-dsl.md` §3.1 (revised per R1 C1) — param_resolver public API placed in `cfdb-cli`, NOT `cfdb-query`. CRP tie-break — solid-architect's R1 verdict. Implication: no file under `crates/cfdb-query/src/` is created by this slice; the module lives in `crates/cfdb-cli/src/param_resolver.rs`.
- `docs/RFC-034-query-dsl.md` §4.6 (invariant) — "The param resolver MUST read `.cfdb/concepts/*.toml` via `cfdb_concepts::load_concept_overrides` — the canonical loader shipped in #3. A second inline TOML parser in `cfdb-query` is a forbidden move." Implication: no `toml::from_str` call in the new module; all TOML reading goes through `cfdb_concepts`.
- `docs/RFC-034-query-dsl.md` §4.5 (hermeticity invariant) — "the param resolver reads ONLY `.cfdb/concepts/*.toml` ... does NOT shell out, does NOT make network calls, does NOT consult environment variables beyond the workspace root path." Implication: `resolve_param` signature is `(workspace_root: &Path, cli_arg: &str)` — two inputs, no env reads.
- `council/49/RATIFIED.md` §9 (canonical ownership) — "Param resolver — owning crate/dir: `cfdb-cli` (R1 C1 relocation) — canonical authority: `crates/cfdb-cli/src/param_resolver.rs` — single module, `pub(crate)` surface." Implication: visibility is `pub(crate)` (not `pub`), and the verb handler in a sibling module (`check_predicate.rs` — Slice 3) consumes it via `use crate::param_resolver::*`.
- `CLAUDE.md` (project) §2.5 — work type "New capability" requires "Dogfood-against-cfdb assertion AND unit tests for extracted pure functions AND `cfdb-recall` corpus extension when the change adds a new fact kind". Implication: this slice adds no fact kind (cfdb-recall corpus unchanged); unit tests + self-dogfood test mandatory.
- `CLAUDE.md` (project) §3 — dogfood gates. Implication: none of the dogfood gates (self-hosted ban rules, extractor recall, determinism, cross-dogfood) need modification; the new module affects neither the fact graph nor wire format.
- `~/.claude/CLAUDE.md` §6.8 (no metric ratchets) — Implication: no `.param-resolver-allowlist.toml` or similar file.
- `docs/RFC-034-query-dsl.md` §6 (non-goals) — "no new cfdb-core vocabulary, no SchemaVersion bump, no new evaluator primitive, no UDF framework, no template composition system, no shell-grep escape hatch (Cypher regex covers path-match), no extension to inner-subquery WHERE grammar, no positive `EXISTS { }`, no schema vocabulary extension (e.g. RE_EXPORTS edge), no `.cfdb/queries/` extension." Implication: slice 1 touches none of these — the resolver is a pure CLI-arg → `Param` mapping.

## Split-brain pre-scan (scope-filtered)

Three scope-filtered hits from `audit-split-brain --full-workspace`, all documented DELIBERATE duplications (no NEW risk):

1. `TriggerId` FromStr bypass — `cfdb-cli::check::TriggerId` (T1/T3) canonical, `cfdb-cli::main_parse::parse_trigger_id` delegates via `from_str`. Marked `delegates: true`. Unrelated to param_resolver scope.
2. `LoadError` duplicate type def — `cfdb-concepts::LoadError` (the authority we will re-use) vs `check-prelude-triggers::toml_io::LoadError` (deliberate clone per `tools/check-prelude-triggers/src/toml_io.rs:3-4` — #127 Forbidden move #8: no cfdb-concepts dep). **`ParamResolveError`** MUST be a DISTINCT type name (not `LoadError`) to avoid adding a third definition — confirmed in RFC §3.1 and issue body.
3. `TriggerId` duplicate type def — `cfdb-cli::TriggerId` (T1/T3) vs `check-prelude-triggers::TriggerId` (C1/C3/C7/C8/C9). Documented DDD homonym; unrelated.

`ParamResolveError` is a NEW name with zero current definitions (`rg ParamResolveError` returns only RFC + council hits). No split risk introduced.

## Session handoffs (relevant)

- `a0-session:2026-04-21-763cbbbb-cfdb-oss-queue` (prior session, this author) — lessons: council caught real defects on RFC-034; `eval_call` dispatch table at `crates/cfdb-petgraph/src/eval/predicate.rs:111-121` is the documented extension point; delegation-to-canonical-loader is the invariant to protect. Follow-up: #145+#146 unblocked post-RFC-034 merge, ship in parallel. **Directly applicable to this slice.**
- `a0-session:2026-04-21-cfdb-triggers-t1-t3-shipped` (earlier session) — lesson re bounded-context homonym (not directly relevant — TriggerId, not ParamResolveError).

## Forbidden moves

- Do NOT place `param_resolver` in `crates/cfdb-query/`. Council R1 C1 relocated it to `cfdb-cli` per CRP tie-break (solid-architect verdict). Any file created under `crates/cfdb-query/src/` is a regression vs the ratified decision. The module lives ONLY at `crates/cfdb-cli/src/param_resolver.rs`.
- Do NOT implement an inline TOML parser inside `param_resolver.rs`. Invariant §4.6: delegate ALL `.cfdb/concepts/*.toml` reading to `cfdb_concepts::load_concept_overrides`. No `toml::from_str` call, no `toml::Value::try_into`, no bespoke `#[derive(Deserialize)]` for `ContextMeta`. If a context needs additional fields not in `cfdb_concepts`, extend `cfdb_concepts` in a sibling slice, not here.
- Do NOT read environment variables, shell out, or make network calls. Hermeticity invariant §4.5 — resolver takes `(workspace_root: &Path, cli_arg: &str)` and reads only `.cfdb/concepts/*.toml` via the canonical loader.
- Do NOT make `resolve_param` / `resolve_params` / `ParamResolveError` `pub` (public). They MUST be `pub(crate)` — RATIFIED §9 canonical ownership. Slice 3's `check_predicate.rs` consumes via `use crate::param_resolver::*`; integration tests in `crates/cfdb-cli/tests/` consume via `cfdb_cli::param_resolver::*` if the module is re-exported at crate root (decide at implementation time; do NOT raise to `pub`).
- Do NOT name the error type `LoadError`. Both `cfdb-concepts::LoadError` and `check-prelude-triggers::toml_io::LoadError` already exist; introducing a third `LoadError` in `cfdb-cli` would require an explicit "fourth bounded context" justification the RFC does not grant. Use `ParamResolveError` as specified.
- Do NOT add labels, edges, or attributes to `cfdb-core::schema`. No `SchemaVersion` bump. No new `:Param` node kind, no new edge. The resolver produces `cfdb_core::query::Param` values consumed by the evaluator — it does NOT extend the fact-graph vocabulary.
- Do NOT add a UDF registration. RFC §6 non-goals — UDF framework is a future RFC; param resolver is a specific loader, not a generic UDF mechanism.
- Do NOT extend the Cypher parser. RFC §2.2 — `cfdb-query` is UNCHANGED by this slice; the parser, builder, evaluator, and AST are all reused as-is.
- Do NOT introduce a `.param-resolver-baseline.toml` or allowlist file. `~/.claude/CLAUDE.md` §6.8 no-metric-ratchets.
- Do NOT change any `pub` API on existing crates. `cfdb-concepts::load_concept_overrides` is CONSUMED as-is; its signature stays frozen.
- Do NOT perform boy-scout refactors inside `cfdb-cli/src/` on unrelated files (e.g., the 3 drift god-files from #128 — check.rs, commands.rs, scope.rs). Those are out of scope; #151 tracks them separately. Staying narrow keeps the PR reviewable.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#145): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.

Past corrections are never edited or deleted. If a correction was wrong, append a new correction that retracts it. `git log .context/145.md` is the audit trail.
