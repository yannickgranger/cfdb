---
issue: 242
issue_state: open
issue_created_at: 2026-04-24T07:40:16Z
refreshed_at: 2026-04-24T08:30:00Z
refreshed_sha: 346eab1
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 346eab16305e
triggers_json_path: .triggers/242.json
triggers_fired: []
---

# Context package — Issue #242

## Issue summary

`cfdb-query` edge-pattern MATCH returns 0 rows despite the JSON keyspace provably containing the edges. Single-node MATCH works; bug is specifically in edge traversal / binding expansion. Blocks qbot-core #3736 (per-PR drift gate) plus any classifier rule that matches on edges.

## AC freshness

- 🟢 Diagnose + fix edge-traversal path in query engine — issue is <3h old, no fix commits anywhere
- 🟢 Unit: `MATCH (a)-[r]->(b)` on 2-node + 1-edge fixture returns 1 (currently returns 0) — still open
- 🟢 Unit: named-label `-[r:KIND]->`, typed endpoints `(a:A)-[r]->(b:B)`, chained `(a)-[r1]->(b)-[r2]->(c)` — still open
- 🟢 Self dogfood: classifier rule row count matches `jq` over raw edges — still open
- 🟢 Cross dogfood: `ci/cross-dogfood.sh` exit 0 — still open
- 🟢 Target dogfood: qbot-core Cypher `count(r)` == `jq` count for REGISTERS_PARAM / EXPOSES / CALLS / INVOKES_AT / HAS_PARAM — still open

**⚠️ Issue-body scope paths are drift.** Body cites `crates/cfdb-query/src/evaluator/`, `crates/cfdb-query/src/binding/`, `crates/cfdb-petgraph/src/store.rs` — **none of these exist**. Real code:

- `crates/cfdb-petgraph/src/eval/pattern.rs:317` — `traverse(src_idx, edge)` (BFS + cycle detection)
- `crates/cfdb-petgraph/src/eval/pattern.rs:352` — `collect_directed_edges(idx, edge, outgoing, incoming)` (iterates `state.graph.edges(idx)`)
- `crates/cfdb-petgraph/src/eval/pattern.rs:467` — `edge_label_matches(pattern, edge)` (label comparator)
- `crates/cfdb-petgraph/src/graph.rs` — edge ingest (`ingest_one_edge` with dangling-src drop — see session lessons)
- `crates/cfdb-petgraph/src/eval/mod.rs` — evaluator entry (`KeyspaceState::by_prop` etc from RFC-035)

Implementers honor the issue-body intent; the scope map is derived from the trace, per A0 CLAUDE.md §8b.

## Commits since issue was filed (scope: `crates/cfdb-petgraph/`, `crates/cfdb-query/`)

_None._ Issue is 2h50m old; develop tip is `346eab1` (v0.3.0 back-merge) which predates the issue.

## Relevant PRs

**Merged (scope-touching, reverse-chron):**

- #233 feat(#205): raid plan validation queries (2026-04-24) — Cypher rule files in `.cfdb/queries/`, not eval/
- #232 feat(#204): HSB multi-signal cluster query (2026-04-24) — queries file, not eval/
- #231 feat(#202): VSB multi-resolver detector (2026-04-24) — scar corpus + detector, uses eval edges
- #197 feat(#186): composition-root wiring for `.cfdb/indexes.toml` (pre-v0.3.0) — **RFC-035 slice 7**
- #194 test(#185): target-dogfood measurement against qbot-core — **RFC-035 slice 6 test**
- #193 feat(#185): cross-MATCH posting-list intersection — **RFC-035 slice 6 evaluator surgery**
- #192 feat(#184): `candidate_nodes` fast paths — **RFC-035 slice 5 evaluator surgery**
- #191 feat(#183): lazy rebuild on `persist::load` — **RFC-035 slice 4**

**RFC-035 is the prime suspect.** Slices 4–7 rewired the evaluator with posting-list intersection + composition-root index loading. The bug exists on develop tip post-all-RFC-035 slices; may be a regression from one of them or a pre-existing bug amplified when index fast-paths took over.

**Open:** none touching scope.

## Sibling open issues

- #238 RFC-037 closeout doc — unrelated to eval
- #239 `render_type_inner` Vec/Option unwrap — extractor-side, not eval
- #240 CI `Closes #N` enforcement — CI chore

No sibling collisions on this scope.

## Doctrinal layer

- **docs/RFC-cfdb.md §6 (API contract)** — "`query_raw(keyspace, cypher, params, sets?) -> {rows, warnings}` … warnings catch label/attr/edge typos and 'schema mismatch' vs 'empty result' — silent-empty was the #1 LLM-consumer failure mode." **Implication:** the failing queries are within spec; returning 0 for a schema-valid edge pattern is exactly the "silent empty" failure mode the RFC was architected to eliminate. A warning emitted for known-label edges returning 0 rows would have caught this earlier.
- **docs/RFC-cfdb.md §12.1 (Determinism G1)** — "Same workspace SHA + same schema version → byte-identical graph." **Implication:** G1 governs extract determinism, not query correctness. Passing G1 does not prove the evaluator sees edges that the canonical JSONL dump contains. Bug is below G1's ceiling.
- **specs/concepts/cfdb-petgraph.md** — any new pub type in this crate requires a spec entry (user memory `feedback_graph_specs_pub_types.md`). Fix is read-side only so likely no new pub types, but if one is introduced, the spec must land in the same PR.
- **CLAUDE.md §2 rule 8 (no metric ratchets)** — applies if the fix is tempted to add a baseline/allowlist for queries known to return 0; forbidden. Fix the evaluator, not the threshold.

## Split-brain pre-scan (scope-filtered)

No findings in `crates/cfdb-petgraph/src/eval/` or `crates/cfdb-query/src/` from `audit-split-brain --full-workspace`. Net-new fix code should not introduce a second edge-traversal path alongside `traverse` / `collect_directed_edges`.

## Session handoffs (relevant)

- **`a0-session:2026-04-23-cfdb-212-213-shipped`** — lessons:
  - `cfdb-petgraph::graph::ingest_one_edge` drops dangling-src edges (`src/graph.rs:204`) silently. If the fix touches ingest, remember this drop can mask issues upstream.
  - Self-dogfood test in #213 asserts only "all buckets empty + warnings present" — the issue body notes this was *because* edges can't be matched in Cypher. The #213 test shape should get tightened to "N findings" after #242 fixes the read side.
- **`a0-session:2026-04-23-cfdb-rfc-037-in-flight`** — RFC-037 producer waves shipped REGISTERS_PARAM + TYPE_OF + RETURNS; these are the concrete edge types the issue targets. No overlap with eval code.

## Reproduction verified at refreshed_sha

Bug reproduces on this worktree's fresh extract (`346eab16305e`, cfdb-on-cfdb, 14911 nodes / 14770 edges):

| Query | Result | Ground truth (jq over `cfdb.json`) |
|---|---|---|
| `MATCH (i:Item) RETURN count(i)` | 1424 | (not computed; single-node path works) |
| `MATCH (a:Item)-[r:HAS_FIELD]->() RETURN count(r)` | **0** | **285** |
| `MATCH (a)-[r]->(b) RETURN count(r)` | **0** | ~14770 |
| `MATCH (a)-[r:INVOKES_AT]->(b) RETURN count(r)` | **0** | **12867** |

No `UnknownEdgeLabel` warning emitted (`pattern.rs:218`) — labels are recognized; the failure is downstream of label validation.

## Forbidden moves

- **Do not rewrite `pattern.rs::traverse` from scratch.** The BFS + cycle-detection structure is used by variable-length quantifier code (`var_length`). Rewrites risk breaking the var-length contract. Diagnose → minimal fix.
- **Do not touch `ingest_one_edge`'s dangling-src drop (`graph.rs:204`)** without an RFC amendment — this drop is intentional per the RFC-037 wave (lesson from session 2026-04-23-rfc-037-in-flight).
- **Do not add a baseline / allowlist / ceiling for edge-count assertions.** CLAUDE.md §2 rule 8 forbids metric ratchets; fix the evaluator.
- **Do not reintroduce the issue-body's stale path citations.** Real scope lives in `crates/cfdb-petgraph/src/eval/` and `crates/cfdb-petgraph/src/graph.rs`. `crates/cfdb-query/src/` holds builders/parser, not the evaluator.
- **Do not skip the `UnknownEdgeLabel` warning audit.** If the bug is below label validation, the fix should consider emitting a second warning class ("edge label known but 0 rows") to honor RFC §6 silent-empty-elimination intent.
- **Do not touch `.cfdb/indexes.toml` or the fast-path intersection from #185 without reading RFC-035 first.** The RFC-035 wave is the prime suspect for regression; mutating its knobs without understanding is a rollback-risk.

## Parent corrections

### Correction @ 2026-04-24T13:05:00Z

**Commit:** (this commit)
**Reason:** Probe-first investigation (prescription Ambiguity A) completed. Root cause narrowed from "3 hypotheses across ingest + NodeIndex + evaluator" to a single bug site. Downstream implementer must act on this, not the prescription's probe-first branch logic.
**Scope:** /work-issue-lib (implementer) + any future /prescribe refresh.
**Body:**

The probe ran in-crate against two fixtures (programmatic + persist-roundtrip 2-node-1-edge) and on cfdb's own keyspace. Observed facts:

- **Ingest path works correctly.** `graph.node_count() == file.nodes.len()` and `graph.edge_count() == file.edges.len()` in both fixtures. `id_to_idx` is identical pre/post-roundtrip. `edge_labels` is populated. `ingest_warnings` is empty. Hypotheses (A)=ingest-drop and (C)=NodeIndex-desync are **ruled out**.
- **Petgraph edge traversal works correctly.** `state.graph.edges(idx).count() == 1` for the src node in the programmatic fixture. Outgoing and incoming edges both enumerate as expected.
- **The bug is in `build_path_binding` at `crates/cfdb-petgraph/src/eval/pattern.rs:260-281`.** The function inserts `pp.from.var` → `Binding::NodeRef(src_idx)` and `pp.to.var` → `Binding::NodeRef(dst_idx)`, but **never inserts `pp.edge.var`**. The edge variable `r` in `MATCH (a)-[r]->(b)` is architecturally unrepresentable: `Binding` enum at `eval/mod.rs:66` has only `NodeRef(NodeIndex)` / `Value(RowValue)` / `Null` — no `EdgeRef` variant.

Symptom confirmation against cfdb's own keyspace (14897 nodes / 14770 edges):

| Query | Result |
|---|---|
| `MATCH (a:Item)-[r:HAS_FIELD]->(b) RETURN count(a)` | **285** ✓ (matches jq ground truth) |
| `MATCH (a:Item)-[r:HAS_FIELD]->(b) RETURN count(b)` | **285** ✓ |
| `MATCH (a:Item)-[r:HAS_FIELD]->(b) RETURN a.qname` | real qnames ✓ |
| `MATCH (a:Item)-[:HAS_FIELD]->(b) RETURN count(a)` | **285** ✓ (anonymous edge) |
| `MATCH (a:Item)-[r:HAS_FIELD]->(b) RETURN count(r)` | **0** ❌ (`r` unbound) |

Scope-sizing evidence (shipped-code census):

- **31 anonymous edge patterns `-[:LABEL]->`** across all `.cypher` files in `./examples/queries/`, `./.cfdb/queries/`, `./.cfdb/predicates/`.
- **0 named edge patterns `-[var:LABEL]->`** in any shipped rule.
- **0 edge-property accesses (`r.prop`, `r.label`, `r.kind`)** anywhere in the repo.

Impact: the bug blocks named-edge queries that project/aggregate on the edge variable — specifically the qbot-core #3736 PR drift gate's intended audit shape per the issue body. It does **not** block any shipped classifier, detector, raid, or violation rule (all anonymous). This reconciles the #213 self-dogfood "empty buckets + warnings present" observation with the current finding: those buckets are empty for legitimate no-findings reasons (cfdb-on-cfdb has no `is_deprecated` items), not because of this bug.

**Revised prescription guidance for the implementer:**

- Ambiguity A is resolved. No probe-first branching on ingest vs evaluator needed — the fix lives in the evaluator.
- Prescription EXTEND #1 (`PetgraphStore::execute` surfacing `ingest_warnings`) is **not required** for this bug fix. Ingest warnings aren't involved. If kept, scope it as a follow-up observability issue.
- Prescription EXTEND #2 (`EmptyResult` message refinement) is **not triggered** — `count()` always produces one row, so `rows.is_empty()` is false and `EmptyResult` never fires in the reproduction. Defer to follow-up.
- The minimal fix is **Path 1** (scope-verified against shipped-code census): introduce a way to bind an edge variable in `Binding`, insert it in `build_path_binding`, ensure `count(r)` works. Property access on edges (`r.label`, `r.kind`) is **deferred** — zero shipped consumers; follow-up issue if/when a real consumer appears.
- The regression test in `crates/cfdb-petgraph/src/eval/edge_match_regression_tests.rs` must cover: `count(r)` on both `-[r]->` and `-[r:LABEL]->` against a programmatic 2-node-1-edge fixture, AND against a persist-roundtrip fixture. Asserts: 1 pre-fix would fail; post-fix returns 1.

Not a new forbidden move — this correction ADDS findings; it does not retract anything from the context package's existing forbidden moves list.

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#242): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.
