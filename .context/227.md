---
issue: 227
issue_state: open
issue_created_at: 2026-04-23T13:08:51Z
refreshed_at: 2026-04-24T00:00:00Z
refreshed_sha: 508b164
verdict: current
repo: yg/cfdb
cfdb_keyspace_sha: 508b16463c4a
triggers_json_path: .triggers/227.json
triggers_fired: []
---

# Context package — Issue #227

## Issue summary
HIR extractor's `fn_name_and_qname` omits the impl target for impl methods, producing `mod::method` instead of the syn-side canonical `mod::ImplTarget::method`. This divergence causes REGISTERS_PARAM dst ids emitted by HIR side to mismatch :Param node ids emitted by syn side; ingest silently drops the edge.

## AC freshness
- 🟢 OPEN — Inspect `def.container(sema.db)` for `ItemContainer::Impl` detection. (Current code at `crates/cfdb-hir-extractor/src/entry_point_emitter.rs:410-418` uses only module + fn name.)
- 🟢 OPEN — Resolve impl self-type via HIR, normalize via `cfdb_core::qname::normalize_impl_target`. (Normalizer exists on develop at `crates/cfdb-core/src/qname.rs:145`; HIR side does not currently call it.)
- 🟢 OPEN — Return `"{module}::{impl_target}::{method_name}"` shape matching `method_qname`.
- 🟢 OPEN — Unit test: `impl Foo { #[tool] fn bar(&self, x: i32) }` fixture asserting qname `"crate::Foo::bar"` and REGISTERS_PARAM dst matches syn's `param_node_id("crate::Foo::bar", 1)`.
- 🟢 OPEN — Target dogfood on qbot-core: assert REGISTERS_PARAM edges from :EntryPoint{kind:mcp_tool} > 0 (current 0, expected ~80+).

## Commits since issue was filed (scope: crates/cfdb-hir-extractor/ crates/cfdb-core/src/qname.rs)
- No commits on develop have touched `crates/cfdb-hir-extractor/` or `crates/cfdb-core/src/qname.rs` since the issue was filed 2026-04-23. Current HEAD is the same commit the issue was filed against (`508b164`).

## Relevant PRs
- **Merged:** #226 (REGISTERS_PARAM 3-paths + TYPE_OF producer) — predecessor PR that shipped the producer but surfaced this limitation in its body ("Known limitation").
- **Merged:** #224 (qname canonical helpers + RETURNS + :Field attrs) — landed `normalize_impl_target` + `method_qname` in `cfdb-core::qname`.
- **Open:** none.

## Sibling open issues
- #216 — RETURNS producer (syn-level, post-walk resolution) — **unrelated scope** (cfdb-extractor, not HIR).
- #217 — :Field attribute alignment — **unrelated scope** (syn emitter).
- #220 — TYPE_OF producer — **unrelated scope** (post-walk resolution, not HIR qname).

## Doctrinal layer
- `docs/RFC-037-schema-producer-alignment.md` §3.1 — "REGISTERS_PARAM target widening (Option A)". Names `cfdb-hir-extractor` as the emitter for clap `#[derive(Parser)]` / `Subcommand` paths but the MCP `#[tool]` fn path ships via `cfdb-extractor` (syn-side). The HIR fix here closes the emitter-seam gap for impl-method MCP tools where the HIR side is the `:EntryPoint` detector but the syn side is the `:Param` emitter. Implication: qname canon on both sides MUST agree.
- `crates/cfdb-hir-extractor/src/entry_point_emitter.rs:337-346` — inline "Known limitation (follow-up)" doc block names this exact issue verbatim; the fix is the removal of that block + implementing the extension.
- `CLAUDE.md` §2.5 (repo-local) — tests: unit + self-dogfood + cross-dogfood + target-dogfood. The issue body already prescribes four rows per the Tests: template.
- `CLAUDE.md` §5 (repo-local) — SchemaVersion bump NOT required: this fix does not change wire format, only corrects a qname construction bug on one producer side. No lockstep graph-specs PR needed.

## Split-brain pre-scan (scope-filtered)
No split-brain risks in scope (cfdb-hir-extractor, cfdb-core/qname). Single finding was in `cfdb-petgraph/src/enrich/...` tests — out of scope for this issue.

## Session handoffs (relevant)
None — no prior session recorded state for #227.

## Forbidden moves
- **Do not re-implement `normalize_impl_target`.** The canonical version lives at `crates/cfdb-core/src/qname.rs:145`. Call it; do not copy.
- **Do not re-implement `method_qname` formula.** It exists at `crates/cfdb-core/src/qname.rs:61`. The returned qname shape MUST match what `method_qname(stack, target, method_name)` produces — route through it or recreate the exact same output (prefer routing).
- **Do not bump SchemaVersion.** This is a producer bug fix, not a wire contract change. No graph-specs lockstep.
- **Do not delete the known-limitation doc block silently.** Remove it AS PART OF this PR with a note that the limitation is closed; do not leave stale docs.
- **Do not widen scope to include the clap `#[derive(Parser)]` or `Subcommand` paths.** Those are separate emitters in `entry_point_emitter.rs:395-408` (struct/enum) that already use their type name correctly — they are NOT impl methods. This issue is narrowly about `fn_name_and_qname` for fns inside `impl` blocks.

## Parent corrections
_none yet — append-only, see correction protocol below_

## Correction protocol
Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#227): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.
