---
issue: 100
issue_state: open
issue_created_at: 2026-04-19T23:38:16Z
refreshed_at: 2026-04-21T00:00:00Z
refreshed_sha: fe8b890
verdict: current
repo: yg/cfdb
cfdb_keyspace_sha: fe8b8901a373
---

# Context package — Issue #100

## Issue summary

`feat(cfdb): .cfdb/published-language-crates.toml marker loader + :Crate.published_language prop` — land a new TOML config file declaring which crates publish a "Published Language" (DDD pattern) consumed across bounded contexts, its loader in `cfdb-concepts`, and a `:Crate.published_language: bool` property materialised during extraction/enrichment. Feeds §A2.1 class 2 Context Homonym classifier distinction between homonyms and Published Language.

## AC freshness

- **AC-1** (new module `published_language.rs` in `cfdb-concepts` with `PublishedLanguageCrates` + `load_published_language_crates`): 🟢 OPEN. No `published_language` item exists in any crate source (grep confirms — 3 hits are all prior-session `.context`/`.prescriptions` artifacts, not source).
- **AC-2** (`is_published_language` / `owning_context` / `allowed_consumers` public API): 🟢 OPEN. No such API.
- **AC-3** (missing file → empty map, non-error): 🟢 OPEN. Pattern to mirror from `load_concept_overrides` at `crates/cfdb-concepts/src/lib.rs:119` (also returns empty on missing dir).
- **AC-4** (malformed TOML → `LoadError::Toml`): 🟢 OPEN. Existing `LoadError` enum at `crates/cfdb-concepts/src/lib.rs:210` with `Io` + `Toml` variants — reusable verbatim.
- **AC-5** (deterministic `BTreeMap` for reverse lookup): 🟢 OPEN. Pattern from `ConceptOverrides::crate_assignments` at `crates/cfdb-concepts/src/lib.rs:105`.
- **AC-6** (`:Crate` nodes get `published_language: bool` prop during enrichment): 🟢 OPEN. Wiring location to be decided at prescription — extract-time (`crates/cfdb-extractor/src/lib.rs`) vs. adjacent to `enrich_bounded_context` (PR #119 pattern) vs. new enrich pass. Issue body: "wired into #46's pass or adjacent — coordinate at discovery". #46 was closed as obsolete (absorbed by #108/#109), so "adjacent" resolves to either extract-time or a new enrich pass.
- **AC-7** (≥6-row unit test fixture): 🟢 OPEN. Pattern from existing `load_concept_overrides` tests at `crates/cfdb-concepts/src/lib.rs::tests` (13 tests).

## Commits since issue was filed (scope: `crates/cfdb-concepts/`, `crates/cfdb-extractor/src/context.rs`, `crates/cfdb-extractor/src/lib.rs`, `crates/cfdb-extractor/src/file_walker.rs`, `crates/cfdb-core/src/fact.rs`, `.cfdb/concepts/`)

- `dcd7b91` fix(#127): register check-prelude-triggers in .cfdb/concepts/cfdb.toml
- `933d24f` feat(cfdb-petgraph,cfdb-concepts,cfdb-cli): enrich_concepts — :Concept nodes + LABELED_AS/CANONICAL_FOR edges (#109) — touches `cfdb-concepts` for the `crate_assignments` exposure.
- `9b5e93d` refactor(cfdb-*): drain 53 pre-existing quality-metrics violations → 0 (closes #111)
- `81ea6ab` refactor(cfdb-concepts): extract shared bounded-context resolver crate (#3) — the PR that created `cfdb-concepts` as a shared crate; makes #100's AC-12 question (which crate owns the loader) concrete: **it goes in `cfdb-concepts`**.

None of these touch a `published_language` code path; the issue is genuinely greenfield within the existing `cfdb-concepts` template shape.

## Relevant PRs

- **Merged:**
  - #103 — `refactor(cfdb-concepts): extract shared bounded-context resolver crate (#3)` — canonical home for concept-file loaders. This is where `load_published_language_crates` lives.
  - #119 — `feat(cfdb): Slice 43-E — enrich_bounded_context re-enrichment + v0.2-9 ≥95% gate (#108)` — the adjacent re-enrichment pattern that AC-6 may follow.
  - #121 — `feat(cfdb): Slice 43-F — enrich_concepts (:Concept + LABELED_AS + CANONICAL_FOR) (#109)` — the newest enrich pass. Adds `ConceptOverrides::crate_assignments`.
- **Open:** none touching the scope.

## Sibling open issues

- #34 (EPIC) — v0.2 tracker.
- #95 — parent umbrella (qbot-core #4047 Phase 2 drift-lock subset) — explicitly lists #100 as a deliverable.
- #101 / #102 (Triggers T1/T3) — consume enriched graph; could optionally consume the new `:Crate.published_language` prop but do not block or collide.
- #138 (shipped moments ago in the sibling #96 pipeline) — unrelated, extract --rev URL flag.

## Doctrinal layer

- `docs/RFC-cfdb-v0.2-addendum-draft.md` §A2.1 rows 183–184 — class 2 Context Homonym requires distinguishing "same name in different bounded contexts with semantic divergence" from a Published Language (intentional cross-context consumer). **Implication:** `:Crate.published_language` is the signal the classifier uses to suppress false ContextHomonym positives.
- `docs/RFC-cfdb-v0.2-addendum-draft.md` §A3.2 lines 288–292 — bounded-context + override-mechanism pattern; new file follows the same TOML-discipline as `.cfdb/concepts/*.toml`. **Implication:** same filesystem convention, same loader template.
- `crates/cfdb-extractor/src/context.rs` — per-issue-body reference as the template shape. Note: `context.rs` is now a thin re-export; the canonical loader lives in `cfdb-concepts` (PR #103).
- cfdb `CLAUDE.md` §2 — RFC-first + architect-review for new capability. A short §A1.8 amendment to the addendum is appropriate alongside the implementation (mirror of the §A1.7 pattern used for #96).

## Split-brain pre-scan (scope-filtered)

- `fromstr_bypasses.Visibility` at `crates/cfdb-extractor/src/item_visitor.rs:674` — `delegates=true`, documented false positive, out of scope.
- `duplicate_type_defs.LoadError` — `cfdb-concepts::LoadError` vs `tools/check-prelude-triggers::toml_io::LoadError` — intentional per `tools/check-prelude-triggers/src/toml_io.rs:3-4` (Forbidden move #8 from #48 prescription); out of scope.

No split-brain risk in the `cfdb-concepts` lib path or `:Crate` emission path.

## Session handoffs (relevant)

No prior session handoffs reference #100 directly. The 2026-04-20 backlog audit did not touch it.

## Forbidden moves

- **Do not fork `LoadError`.** `crates/cfdb-concepts/src/lib.rs:210` is canonical. `load_published_language_crates` must return the SAME `LoadError` type (both `Io` and `Toml` variants). A new `PublishedLanguageLoadError` would create a parallel error vocabulary.
- **Do not re-implement TOML loading from scratch.** `collect_toml_entries` at `crates/cfdb-concepts/src/lib.rs:140` + the `fs::read_to_string` + `toml::from_str` pattern at line 168 is the canonical shape. Reuse the pattern (either verbatim if the file semantics are similar, or mirror the pattern for the single-file `.cfdb/published-language-crates.toml`).
- **Do not land the loader in `cfdb-core` or `cfdb-extractor`.** PR #103 (closing #3) pinned `cfdb-concepts` as the home for concept-file loaders. Anywhere else is a layer violation.
- **Do not bump `SchemaVersion`.** Per issue body "Does NOT bump `SchemaVersion` (additive prop on existing `:Crate` shape)" — the prop is additive on an already-declared node label. No lockstep `graph-specs-rust` fixture bump needed.
- **Do not create a second `.cfdb/*.toml` loader pattern.** The existing `.cfdb/concepts/<name>.toml` pattern loads a directory of per-context files; the new `.cfdb/published-language-crates.toml` is a single file. The loaders differ in that one structural detail — but the TOML parse + error surfacing + `BTreeMap` discipline must match exactly. Do not invent a new one.
- **Do not emit `:Finding`s from `load_published_language_crates`.** Per issue body "Does NOT emit `:Finding`s — that's #48 classifier reading the prop". Loader is pure data → typed API. No classifier logic.
- **Do not add `--cache-dir` / `--published-language-dir` / `--ignore-published-language` CLI flags.** Issue body does not mandate a CLI surface. File a follow-up if a real need emerges.
- **Do not validate consumer-import relationships at load time.** Per issue body "Does NOT validate that declared consumers actually import the crate (static check only — runtime check is #48 scope)". TOML parse is the only validation.
- **Do not ratchet.** cfdb `CLAUDE.md` §3 + global `~/.claude/CLAUDE.md` §6.8 — no baseline / ceiling / allowlist files.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#<issue>): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.

Past corrections are never edited or deleted. If a correction was wrong, append a new correction that retracts it. `git log .context/<issue>.md` is the audit trail.
