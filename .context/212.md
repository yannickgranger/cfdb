---
issue: 212
issue_state: open
issue_created_at: 2026-04-23T05:13:12Z
refreshed_at: 2026-04-23T00:00:00Z
refreshed_sha: aeb26d5
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 978d04872000
---

# Context package — Issue #212

## Issue summary
`feat(cfdb-cli): implement cfdb diff — real two-keyspace comparison (Phase B)`. Replace the Phase A stub at `crates/cfdb-cli/src/stubs.rs:155-188` with a real `{added, removed, changed}` envelope over two keyspaces. Upstream dep for qbot-core #3736 per-PR cfdb drift gate.

## AC freshness
- 🟢 **OPEN** — `cfdb diff --db <dir> --a X --b Y` emits valid JSON envelope. Stub at `crates/cfdb-cli/src/stubs.rs:155-188` emits placeholder shape with `warnings[]`; real envelope not implemented.
- 🟢 **OPEN** — identical keyspaces → empty sets, exit 0. Stub returns empty arrays but from no-op, not from equality computation.
- 🟢 **OPEN** — keyspace-diff deterministic across two runs. Not implemented.
- 🟢 **OPEN** — unit tests cover identical / added-only / removed-only / changed-only / mixed. No tests exist.
- 🟢 **OPEN** — help text stops saying "Phase A stub". Current help at `crates/cfdb-cli/src/main_command.rs:293` explicitly says it.

## Commits since issue was filed (scope: crates/cfdb-cli, crates/cfdb-core/src/store.rs, crates/cfdb-petgraph)
- `c734fc2` feat(#205): raid plan validation queries + bucket convention (RFC-036 §3.5) — unrelated to diff scope
- `1becd65` feat(#204): HSB multi-signal cluster query (RFC-036 §3.4 v2)
- `ff8785b` feat(#202): VSB multi-resolver detector + scar corpus (RFC-036 §3.2)
- `8f0f5df`, `52b7d3f`, `af2720c`, `a3922c4`, `2ca059f`, `c30e879` — #203 enrich-metrics work
- No commit since 2026-04-23T05:13:12Z touches `crates/cfdb-cli/src/stubs.rs` or diff-surface.

## Relevant PRs
- **Merged:** none touching the diff verb since issue created
- **Open:** none (0 open PRs on `agency:yg/cfdb`)

## Sibling open issues
- `#213` — `cfdb classify` verb (explicit downstream of this issue)
- `#216`/`#217`/`#220` — RFC-037 producer work (unrelated)
- `#227` — HIR fn_name_and_qname (unrelated)

## Doctrinal layer
- `docs/RFC-cfdb.md` §8 (architecture diagram, line 448) — `cfdb diff` listed as a first-class CLI verb alongside `extract`/`query`. No Phase-gating language for the diff verb itself.
- `docs/RFC-cfdb.md` §12.1 (determinism recipe, lines 653-671) — sorted-JSONL canonical dump is byte-stable across extracts. Implies diff should ride the same canonical sort to inherit determinism for AC3. **Implication:** diff over `StoreBackend::canonical_dump` output is the determinism-safe path; do not diff the in-memory graph directly.
- `crates/cfdb-core/src/store.rs:62-87` (StoreBackend trait) — primitives `list_keyspaces()`, `canonical_dump()`, `schema_version()` are already on the trait. No extension needed.
- `CLAUDE.md` §2.5 (cfdb project section) — new-capability issues must carry a `Tests:` block with four rows (Unit / Self dogfood / Cross dogfood / Target dogfood). **Issue body does NOT currently carry this block** — architects must fill it before implementation PR is mergeable.

## Split-brain pre-scan (scope-filtered)
No split-brain risks in `crates/cfdb-cli/` or `examples/queries/`. `audit-split-brain --full-workspace` returned `[]` for the scoped filter.

## Session handoffs (relevant)
- `2026-04-23-201-paused-for-gap-audit`:
  - Notes that #212 and #213 "filed by another instance for qbot-core-cross-linked work on cfdb diff + cfdb classify" — confirms these are the upstream block for qbot-core #3736.
  - Current session is paused at the #201 gap audit; this issue is NOT part of that gap.
- `2026-04-15-3729-cfdb-scope-shipped`:
  - The `cfdb scope --context` verb was shipped and already emits `findings_by_class`. Relevant to #213, not this issue.
  - Pattern for new cfdb verbs: `clap value_parser` delegating to domain `FromStr`; public types (e.g. `ScopeInventory`) in `cfdb-query` (or `cfdb-core`), not `cfdb-cli`.
  - **Implication for #212:** the `DiffEnvelope` JSON shape type should live in `cfdb-query` (or `cfdb-core`) so qbot-core #3736's gate script can deserialize it from the published crate.

## Forbidden moves
- **Do not leak snapshot-store semantics into the diff verb.** RFC §6 G5 snapshots ≠ keyspaces; `cfdb diff` is keyspace-to-keyspace. The issue-body phrasing "snapshot diff ships with the snapshot store in Phase B" is legacy — interpret as "real diff implementation", not "requires new snapshot store."
- **Do not diff the in-memory graph directly.** AC3 (determinism) requires diffing sorted canonical dumps (RFC §12.1) or equivalent sorted-JSONL output. In-memory iteration order is not byte-stable.
- **Do not place `DiffEnvelope` types in `cfdb-cli`.** They are consumed by qbot-core #3736's gate script and possibly by `cfdb classify` (#213). Keep them in `cfdb-query` or `cfdb-core` for downstream deserialization.
- **Do not extend `StoreBackend` trait** unless strictly necessary. `list_keyspaces`, `canonical_dump`, `schema_version` are already present. Adding new trait methods affects all impls (petgraph, potential HIR backend).
- **Do not skip the `Tests:` block.** Architects must fill in all four rows (Unit / Self dogfood / Cross dogfood / Target dogfood) in the issue body before an implementation PR can ship. `CLAUDE.md` §2.5 mandates this.
- **Do not change exit-0 semantics.** AC says "exit 0 regardless of diff size." The consumer gate in qbot-core #3736 decides pass/fail — cfdb diff is informational.

## Parent corrections

### Correction @ 2026-04-23T19:50:00Z
**Commit:** (pending — this commit)
**Reason:** Original freshness run treated the pass as audit-only ("are they blocked?") and deferred the Step 2f-cfdb cfdb-extract bootstrap. When the main agent then picked up #212 for real implementation work, `/discover` hard-blocked on the missing `cfdb_keyspace_sha` frontmatter field. Bootstrapped cfdb on this worktree (14173 nodes / 14036 edges, keyspace `cfdb` at `.cfdb/db/cfdb.json`) and added `cfdb_keyspace_sha: 978d04872000` to the frontmatter. No other package content affected.
**Scope:** Frontmatter + Step 2f-cfdb compliance for downstream `/discover` and `/prescribe`.
**Body:** `/discover` and `/prescribe` queries against cfdb MUST pin `--keyspace 978d04872000` (the HEAD SHA of `work/212-cfdb-diff` at the time of extraction, `978d048` — same SHA that carries the drift-fix cherry-pick `docs(cfdb-cli): drop stale snapshot/EPIC-#3622 framing`).

## Correction protocol
Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to>
    **Body:** <correction text>

Corrections are git commits (`context(#212): correction — <subject>`).
