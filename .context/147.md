---
issue: 147
issue_state: open
issue_created_at: 2026-04-21T14:02:12Z
refreshed_at: 2026-04-21T19:00:00Z
refreshed_sha: fa3be53
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: fa3be5352df3
---

# Context package — Issue #147

## Issue summary

Slice 3 of RFC-034. Add `crates/cfdb-cli/src/check_predicate.rs` + new `Command::CheckPredicate` variant + dispatch arm. The verb loads a `.cfdb/predicates/<name>.cypher` template, resolves `--param` CLI args via the Slice-1 resolver, executes against the pinned keyspace, emits the three-column `(qname, line, reason)` format — same exit contract as `cfdb violations` (exit non-zero iff `row_count > 0`).

## AC freshness

- 🟢 AC-check-predicate-fn — `crates/cfdb-cli/src/check_predicate.rs` absent on develop @ `fa3be53`. Confirmed `ls` fails.
- 🟢 AC-PredicateRunReport / AC-PredicateRow — `rg "PredicateRunReport\|PredicateRow" crates/` on develop returns zero production matches (only the RFC-034 / council docs). New types.
- 🟢 AC-Command-CheckPredicate — `grep -n "CheckPredicate" crates/cfdb-cli/src/main_command.rs` returns zero. New variant.
- 🟢 AC-dispatch-arm — no existing arm in `main_dispatch.rs`. New wiring.
- 🟢 AC-lib-re-export — no `pub use check_predicate::...` line in `crates/cfdb-cli/src/lib.rs`. New re-export.
- 🟢 AC-exit-non-zero-when-rows — contract mirrors existing `cfdb violations` at `commands.rs:580` (which returns `Result<usize, _>` count used by main_dispatch to set exit). Precedent + pattern already proven.
- 🟢 AC-blocked-by — #145 and #146 both merged on develop (#145 at `937ebc4`, #146 at `0d2295e`, merges `1b40…`/`fa3be53`). Both sibling slices green.

## Commits since issue was filed (scope: crates/cfdb-cli/src/ .cfdb/predicates/ crates/cfdb-cli/Cargo.toml crates/cfdb-cli/src/lib.rs)

- `fa3be53` — Merge PR #153 (#146 Slice 2 — predicates seed library + schema-ref static check)
- `0d2295e` — PR #153 tip: `feat(.cfdb,cfdb-query): predicate seed library + schema-ref static check (#146)` (landed `.cfdb/predicates/*.cypher` + the schema-ref integration test)
- `6497beb` — Merge PR #152 (#145 Slice 1 — cfdb-cli::param_resolver)
- `937ebc4` — PR #152 tip: `feat(cfdb-cli): param_resolver — TOML-backed --param forms (#145)` (landed the `pub(crate)` `param_resolver` module with `#![allow(dead_code)]`)
- `36ad02f` — `chore(cfdb-cli): Cargo.lock for cfdb-concepts + toml deps (#145)`

None of these conflict with #147; they are the explicit prerequisites this slice consumes.

## Relevant PRs

- **Merged:** #152 (#145 Slice 1), #153 (#146 Slice 2), #150 (RFC-034 parent).
- **Open:** none.

## Sibling open issues

- #148 Slice 4 (predicate-library dogfood CI) — blocked-by this issue (#147).
- #149 Slice 5 (user guide) — blocked-by #148.
- #151 (drift-god-files follow-up to #128) — orthogonal, same-crate scope (cfdb-cli) but different files; only `src/check_predicate.rs` (new) touches this slice's surface.

## Doctrinal layer

- `docs/RFC-034-query-dsl.md` §3.1 (post-R1 C1) — `check_predicate.rs` signature is `pub fn check_predicate(db, keyspace, workspace_root, name, cli_params) -> Result<PredicateRunReport, CfdbCliError>`; `PredicateRunReport { predicate_name, predicate_path, row_count, rows }`; `PredicateRow { qname, line, reason }` with rows sorted `(qname asc, line asc)` for determinism. Implication: field names + visibility (`pub`) are locked.
- `docs/RFC-034-query-dsl.md` §3.4 — CLI: `cfdb check-predicate --db <path> --keyspace <name> --name <predicate> [--param <name>:<form>:<value> ...] [--format text|json]`. Implication: clap derives on the new `Command::CheckPredicate` variant use exactly these flag names.
- `docs/RFC-034-query-dsl.md` §4.1 — Determinism invariant. Implication: `sort_by((qname, line))` on rows before serialization; `ORDER BY` in the predicate templates is a necessary but not sufficient condition — the verb ALSO sorts the QueryResult rows post-execute to guarantee `PredicateRunReport` ordering regardless of future evaluator changes.
- `docs/RFC-034-query-dsl.md` §4.3 — No-ratchet rule. `cfdb check-predicate` exits 0 iff row count is 0; no `--baseline`, no `--allowlist`. Implication: error surface is binary.
- `docs/RFC-034-query-dsl.md` §6 non-goals — verb handler MUST NOT introduce UDFs, modify evaluator, add `cfdb-query` dep on `cfdb-concepts` (both are `cfdb-cli` deps), or create a template composition mechanism (one file = one predicate).
- `council/49/RATIFIED.md` §9 — canonical owner of the verb is `cfdb-cli`; `pub` re-export surface at the crate root (`cfdb_cli::{check_predicate, PredicateRunReport, PredicateRow}`) is mandated so integration tests and future skill consumers can import them.
- `CLAUDE.md` (project) §2.5 — new-capability tests: unit + dogfood + cross-dogfood + target-dogfood (optional). This slice carries the dogfood expectation from the issue-body `Tests:` block.
- `crates/cfdb-cli/src/param_resolver.rs:31-36` — inline comment documents the `#![allow(dead_code)]` allow with the statement "Removing this allow is part of the #147 PR diff." Implication: this slice MUST remove the allow and the 6-line justification block, leaving the module lint-clean because it now has a real in-crate consumer (`check_predicate.rs`).
- `crates/cfdb-cli/src/commands.rs:580` — existing `violations()` signature + `run_cypher_rule()` + `parse_and_execute()` helpers. Implication: the verb's plumbing (parse → lint → execute → sort → print) reuses `parse_and_execute()` and mirrors `violations`'s three-column output + exit-code contract.
- `crates/cfdb-cli/src/commands.rs:536-560` — `list_callers` verb. Implication: the "load template → insert params → execute → print" sequence has an exact code precedent for templated verbs; follow that pattern.

## Split-brain pre-scan (scope-filtered)

- `TriggerId` homonym (cfdb-cli::check::TriggerId vs check-prelude-triggers::trigger_id::TriggerId) — documented DDD homonym; irrelevant to this slice.
- `LoadError` duplicate (cfdb-concepts::LoadError vs check-prelude-triggers::toml_io::LoadError + ParamResolveError handling) — #145 introduced `From<cfdb_concepts::LoadError>` on `ParamResolveError`. No new risk for this slice; `check_predicate` returns `CfdbCliError` which already wraps `ParamResolveError` via the existing error stack? Let /discover verify `CfdbCliError` variants before implementation — if a `From<ParamResolveError>` is missing, this slice will need to add one and that must flow through the prescription.

`check_predicate` / `PredicateRunReport` / `PredicateRow` have zero occurrences in Rust source on develop — new-name invariants are clean.

## Session handoffs (relevant)

- `a0-session:2026-04-21-763cbbbb-cfdb-oss-queue` — lessons: `parse_and_execute` is the canonical plumbing; existing `cfdb violations` + `cfdb list-callers` verbs are the shape precedents; pub(crate) surface of `param_resolver` means Slice 3 MUST `use crate::param_resolver::*`, not re-export from lib.rs. Direct continuation of the current session.

## Forbidden moves

- Do NOT leave `#![allow(dead_code)]` in `crates/cfdb-cli/src/param_resolver.rs`. The allow's comment at lines 31-36 explicitly states removal is part of THIS slice's diff. Removing it + confirming clippy stays green is the proof that Slice 3's consumer is real.
- Do NOT re-implement the CLI-arg parsing logic. The Slice-1 `resolve_params(workspace_root, &cli_params)` at `crates/cfdb-cli/src/param_resolver.rs:117` IS the canonical resolver. Call it directly (`use crate::param_resolver::resolve_params;`) — any local `fn parse_param_cli_arg` / `impl FromStr` / match-on-`:` elsewhere in `cfdb-cli` duplicates the Slice-1 bridge and violates the R1 C1 council decision + CRP tie-break.
- Do NOT re-implement the parse/execute plumbing. `parse_and_execute()` at `commands.rs:636` is the shared entry point for every cypher-rule verb (`violations`, `check --trigger`). Call it; do not fork it.
- Do NOT introduce a UDF. RFC §6 non-goals. The `eval_call` dispatch table at `crates/cfdb-petgraph/src/eval/predicate.rs:111-121` is the future extension point — adding a new arm is a separate RFC.
- Do NOT modify `cfdb-petgraph/src/eval/`. The evaluator is unchanged; the verb just composes existing parse + execute + sort + format.
- Do NOT add `cfdb-concepts` as a dep of `cfdb-query`. That prohibition was the R1 C1 relocation rationale. The dep edge stays `cfdb-cli → cfdb-concepts` (already present from #145).
- Do NOT add a template composition / include / macro mechanism in `.cfdb/predicates/`. RFC §6 non-goal. One file = one predicate.
- Do NOT add a `--baseline` / `--allowlist` / `--ceiling` flag. RFC §4.3 + `~/.claude/CLAUDE.md` §6.8 (no metric ratchets).
- Do NOT use the `cfdb violations` verb path for predicate dispatch. `violations` reads a raw `.cypher` rule file with NO param binding; `check-predicate` MUST resolve `--param` args via the Slice-1 resolver before execute. Keeping them as separate verbs preserves the RATIFIED §9 canonical-ownership invariant + the two verbs' different contracts.
- Do NOT break the homonym note on `ParamResolveError`. Conversion from `ParamResolveError` to `CfdbCliError` (if needed in the new verb) MUST NOT name a new `LoadError` variant on `CfdbCliError`; map via the existing `CfdbCliError::Usage(String)` escape hatch or a new dedicated variant.
- Do NOT touch the 3 drift god-files (check.rs / commands.rs / scope.rs) — those are #151's scope. Staying narrow keeps the PR reviewable.
- Do NOT bump `SchemaVersion`. RFC §6 non-goal.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run.
