---
issue: 236
issue_state: open
issue_created_at: 2026-04-23T22:37:02Z
refreshed_at: 2026-04-24T00:00:00Z
refreshed_sha: f284ccf
verdict: current
repo: yg/cfdb
cfdb_keyspace_sha: f284ccf74ab6
---

# Context package — Issue #236

## Issue summary

Add `--format sorted-jsonl` to `cfdb classify` (shipped with `--format json` only in #213). Output is one JSON object per `Finding` line, sorted by `(class, qname, rule)`, byte-stable. Envelope-level metadata shape (header line with `#` prefix vs sidecar) is TBD by prescription. No changes to `ClassifyEnvelope` struct — format is a serialization choice, not a new wire contract.

## AC freshness

- 🟢 AC1: `--format sorted-jsonl` emits newline-delimited JSON, one finding per line — not shipped; `emit_classify_output` at `crates/cfdb-cli/src/commands/classify.rs` (added in f43539e / #235) currently implements only `Format::Json`. Open.
- 🟢 AC2: Byte-stable output on identical inputs (determinism like `ci/determinism-check.sh`) — not asserted on classify output yet. Open.
- 🟢 AC3: Lines sorted by `(class, qname, rule)` lex — no sort contract exists on classify output today. Open.
- 🟢 AC4: `--format json` behavior unchanged — current behavior; this is a regression-lock, not a new change. Open (as a requirement, not yet proven by tests).
- 🟢 AC5: Envelope-level metadata shape consistent with `cfdb dump` conventions — TBD by prescription. `cfdb dump`'s canonical impl at `crates/cfdb-petgraph/src/canonical_dump.rs:16-44` emits nodes+edges only, no per-dump envelope header — a direct analogue to classify is NOT 1:1 (classify has `schema_version` / `inventory.context` / `diff_source` which dump does not). Design choice legitimately open.
- 🟢 AC6: No changes to `ClassifyEnvelope` struct in `cfdb-query` — regression lock. Open.

**Top-level verdict: `current`** — all ACs open; no collisions with in-flight PRs; no prior commits shipping this shape; no session-handoff warnings against the scope.

## Commits since issue was filed (scope: crates/cfdb-cli/ crates/cfdb-query/ crates/cfdb-petgraph/src/canonical_dump.rs docs/RFC-cfdb.md)

Issue filed 2026-04-23T22:37:02Z; scoped `git log origin/develop --since=2026-04-23` shows:

- `f43539e` — feat(cfdb): cfdb classify verb — debt-class routing of diff findings (#213). **This is the parent ship — #236 extends it.**
- `3529c73` — feat(cfdb): real cfdb diff — keyspace-to-keyspace delta over canonical sorted-JSONL (#212). **Establishes the `--format sorted-jsonl` pattern on `cfdb diff`; #236 mirrors it on classify.**
- `978d048` — docs(cfdb-cli): drop stale snapshot/EPIC-#3622 framing on cfdb diff stub (unrelated — stub doc scrub)
- RFC-037 producer backlog commits (af2720c, 2ca059f, a3922c4, 8f0f5df, 4588fba) — unrelated scope (producer wiring)
- `6cc5296` — docs(#51): merge RFC-029 v0.2 addendum into parent RFC-cfdb.md (predates issue; noted because §A2 / §A2.2 / §A2.3 lines 1178/1197/1268 now live in RFC-cfdb.md, not the deleted addendum draft)

No commits in scope post-issue-filing. Worktree branch is off `origin/develop` at `f284ccf` (post-#235 merge).

## Relevant PRs

- **Merged:**
  - PR #234 — `feat(cfdb-cli): implement cfdb diff — real two-keyspace comparison (Phase B) (#212)` — ships `--format json|sorted-jsonl` on `cfdb diff`
  - PR #235 — `feat(cfdb-cli): cfdb classify verb — route diff findings by debt class (#213)` — ships `cfdb classify --format json` (MVP; sorted-jsonl deferred to this issue)
- **Open:** (none)

## Sibling open issues

- #216 — 037-A: RETURNS producer (syn-level, post-walk resolution) — unrelated scope
- #217 — 037-E: `:Field` attribute alignment — 5 props per descriptor — unrelated scope
- #220 — 037-D: TYPE_OF producer — unrelated scope
- #227 — HIR `fn_name_and_qname` should include impl target for methods — unrelated scope

No sibling touches `classify` / `diff` / `canonical_dump`.

## Doctrinal layer

- `docs/RFC-cfdb.md` §12.1 (line 653-655) — **Canonical dump format contract.** Quote: *"`cfdb dump --db <path> --keyspace <name>` emits one JSON object per node and edge, sorted by `(node_label, qname)` then `(edge_label, src_qname, dst_qname)`. Numeric and string fields ordered alphabetically within each object. UTF-8, LF newlines, no trailing whitespace. Each emitted object carries a `kind: "node" | "edge"` discriminator so a stream-parser does not need positional context. (The original RFC draft named this verb `cfdb export --format=sorted-jsonl`; the shipped CLI uses `cfdb dump` — a one-name verb is cleaner than a verb+flag pair with a single legal flag value, and the format itself is unaffected. Reconciled in #3630.)"*
  Implication: sort key, charset, line discipline, and alphabetical-field-order rules are frozen by RFC. Classify's `sorted-jsonl` output MUST inherit them. The "one-name verb is cleaner" aside is NOT directive against `--format sorted-jsonl` on classify — it was historical reconciliation of `cfdb dump` specifically; `cfdb diff --format sorted-jsonl` is live on develop and establishes the precedent that output-verbs with multiple formats use `--format`.

- `docs/RFC-cfdb.md` §12 G1 (line 646, 708) — **Determinism contract.** Quote: *"Extraction is keyed by `(workspace SHA, schema major.minor)`. Same key → byte-identical **JSONL canonical dump**"* and *"Determinism CI check passes per the §12.1 recipe: same `(workspace SHA, schema major.minor)` → byte-identical sorted-jsonl dump across two consecutive extractions."*
  Implication: determinism is asserted BY DESIGN on sorted-jsonl output. Classify's sorted-jsonl MUST be byte-stable across runs — `ci/determinism-check.sh` pattern (two runs + sha256 compare) is the idiomatic test shape. AC2 aligns with RFC G1.

- `crates/cfdb-petgraph/src/canonical_dump.rs:16-44` — **Implementation pattern to mirror.** Format contract comment lines 18-44 document the BTreeMap-based envelope construction, stable `sort_by` (not `sort_unstable`), and id-fallback lookup for qname resolution. Any sorted-jsonl impl in classify MUST replicate these invariants.

- `CLAUDE.md` §1 (project-local) — **New capability path.** Quote: *"New capability (new verb, new fact type, new schema field, new `--flag`, new sub-backend) | RFC → architect review → issues → dogfood-gated PRs"*
  Implication: literal reading flags this issue as a `new --flag` addition requiring an RFC-first path. BUT — the `--format` flag already exists on `cfdb classify` (shipped in #213 with `Format::Json`), and `--format sorted-jsonl` is a new VALUE on an existing enum, mirroring the `cfdb diff --format json|sorted-jsonl` precedent shipped in #212 under RFC §12.1. This is extension-of-established-pattern, not a genuinely new capability. **Flagged for parent review in Step 4; not reclassified to `contested` because mechanical archaeology does not find a counter-example.**

- `CLAUDE.md` §2.5 (project-local) — **Mandatory Tests block per work type.** Quote: *"New capability (new verb, fact type, schema field) | Dogfood-against-cfdb assertion **AND** unit tests for extracted pure functions **AND** `cfdb-recall` corpus extension when the change adds a new fact kind"* — The issue body already carries a 4-row Tests: block per §2.5 template (Unit, Self dogfood, Cross dogfood N/A, Target dogfood N/A). No Tests: drift.

- `CLAUDE.md` §3 (project-local) — **Dogfood enforcement gates.** No new ban rule, no new fact type, no new schema field introduced → `SchemaVersion` stays. Cross-dogfood row on the issue is already "N/A — output-shape change only". Determinism gate applies to the new output (AC2 is the lock).

## Split-brain pre-scan (scope-filtered)

Scan filtered to `classify|diff|canonical_dump`:

- One match-arm overlap reported: `crates/cfdb-petgraph/src/enrich/concepts/tests.rs:184` ↔ `crates/cfdb-petgraph/src/enrich/rfc_docs/tests.rs:198` (shared literals `qname`, `item:{q}`, `ingest`, `name`, `test`, 55% overlap). **Unrelated to #236 scope** — both are test builders in the enrich/ subsystem, not classify/diff output formatting. No action for this issue.
- No match-arm, fromstr, or const-table splits in `commands/classify.rs`, `cfdb-query/src/classify.rs`, `cfdb-query/src/diff.rs`, or `canonical_dump.rs`.

Conclusion: **no split-brain risk in #236 scope.**

## Session handoffs (relevant)

- `state:a0-session:2026-04-23-cfdb-212-213-shipped` — parent session that shipped #212/#213 and filed #236. Lessons relevant to #236:
  - `QUALITY-SHIP DOES NOT ACCEPT --gate-profile`: drop the flag when `/ship` is invoked; use `quality-ship --workspace <ws> --base origin/develop` (2 flags only).
  - `graph-specs-check blocks /ship BEFORE quality-ship` — but #236 adds no new pub types (AC6 is regression lock on `ClassifyEnvelope`), so `specs/concepts/cfdb-query.md` needs no new entry.
  - `CANONICAL qname EXTRACTION` — sort-key semantics for sorted-jsonl follow the same node `{props.qname ?? id}` + edge `(src_qname, dst_qname)` pattern already used in `cfdb-petgraph/src/canonical_dump.rs:45-54`. Classify's sort key is different (`(class, qname, rule)`) but MUST use the same stable-sort + BTreeMap discipline.
  - `Follow-up noted:` *"When #235 merges: (2) File 'cfdb classify --format sorted-jsonl' as a small follow-up issue (deferred in #213 prescription)."* — that follow-up IS #236. This session is the follow-up being executed.

No other session handoffs touch classify/diff scope.

## Forbidden moves

1. **Do not mutate `ClassifyEnvelope` struct in `cfdb-query`.** Rationale: AC6 regression lock + #213 compositional-envelope lesson ("ClassifyEnvelope is a shipped wire contract"). The new format is a serialization choice, not a new wire field.
2. **Do not change `--format json` default output bytes.** Rationale: AC4 regression lock; existing integration tests (`classify_self_dogfood.rs`) assert on JSON shape. Adding a new format must not perturb the JSON branch.
3. **Do not use `HashMap`, `sort_unstable`, or any wall-clock-dependent serialization.** Rationale: RFC §12.1 G1 byte-determinism contract; `canonical_dump.rs:16-44` documents the invariants. Classify's sorted-jsonl output shares the same two-run byte-identical guarantee.
4. **Do not embed routing hints (`fix_skill` or equivalent) in finding rows.** Rationale: carried over from #213 — RFC-cfdb.md §A2.3 + line 919 council BLOCK-1 (solid-architect), enforced by `finding_no_skill_field` architecture test. Routing lives in `.cfdb/skill-routing.toml`. Serializing findings to sorted-jsonl must not leak routing columns.
5. **Do not add a new `pub struct` or `pub enum` in `cfdb-query` without a corresponding `specs/concepts/cfdb-query.md` entry.** Rationale: user-memory `feedback_graph_specs_pub_types.md` — `make graph-specs-check` blocks `/ship` on undeclared pub types. **This rule is trivially satisfied if AC6 holds (no struct changes).**
6. **Do not invoke `/ship` with `--gate-profile`.** Rationale: session-handoff lesson — the installed `quality-ship` binary rejects the flag; skill template is stale.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#236): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.

Past corrections are never edited or deleted. If a correction was wrong, append a new correction that retracts it. `git log .context/236.md` is the audit trail.

## Methodology note for parent (not a block)

Per `CLAUDE.md` §1 literal reading, a `new --flag` addition is on the "RFC → architect review → issues → PR" path. #236 is filed as a follow-up to #213's deferred-decision — no new RFC authored. Analysis:

- `--format` is a pre-existing flag on `cfdb classify` (shipped in #213). `sorted-jsonl` is a new VALUE on the existing `Format` enum, not a new flag.
- The pattern `--format json|sorted-jsonl` is live on `cfdb diff` (shipped in #212 under RFC §12.1). #236 mirrors the established precedent.
- The output shape itself (sorted JSONL) is RFC-ratified via §12.1 (lines 653-655) and implemented canonically in `crates/cfdb-petgraph/src/canonical_dump.rs`. No new design territory.
- AC6 locks `ClassifyEnvelope` → no wire contract change; AC5's metadata-shape choice is a small serialization design call, not architecture.

**Main agent judgment call:** proceed as extension-of-established-pattern (`/work-issue-lib` path), or reclassify as new-capability and pause for RFC. If the latter, append a correction stating that decision and close #236 with an RFC-authoring follow-up. If the former, no action — downstream skills proceed.
