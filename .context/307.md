---
issue: 307
issue_state: open
issue_created_at: 2026-04-26T00:10:13Z
refreshed_at: 2026-04-26T19:10:00Z
refreshed_sha: 9da9cae
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 9da9caeaceba
triggers_json_path: .triggers/307.json
triggers_fired: []
---

# Context package — Issue #307

## Issue summary
`EQUIVALENT_TO` edge label is declared in `cfdb-core::schema::describe::edges.rs:148` (`:Concept -[EQUIVALENT_TO]-> :Concept`, "Two Concepts are synonyms") but has **zero producers** anywhere in the codebase. Surfaced by RFC-037 §3.7 edge-liveness check on PR #305 CI. Issue body proposes three options (A: Phase B reservation via `Provenance::Reserved`; B: remove; C: implement). Recommendation = A.

## AC freshness
- 🟢 **AC-A1: Add `Provenance::Reserved` (or similar) variant to the schema describer enum** — open. No commit on develop has touched `crates/cfdb-core/src/schema/descriptors.rs::Provenance`.
- 🟢 **AC-A2: Mark EQUIVALENT_TO descriptor with the reserved provenance so edge-liveness check can suppress dormant warnings** — open. No commit on develop has touched the edge-liveness suppression logic.
- 🟢 **AC-A3: Document in describer that EQUIVALENT_TO is reserved for Phase B** — open. Current description is "Two Concepts are synonyms" (positive, not "reserved").

## Commits since issue was filed (scope: `crates/cfdb-core/`, `ci/edge-liveness.sh`)
- _(none touching scope)_ — only `a00fc99 feat(cfdb-core): add parse_or_default reader helper for :Context.source` (RFC-038 slice 4/4) lands between issue filing (2026-04-26 00:10Z) and now, and that touched `cfdb_core::context_source`, NOT the schema describer or edge-liveness scope. PR #309 closed/merged.

## Relevant PRs
- **Merged:**
  - #319 — `fix/317-foreign-item-emission → develop`: synthesise minimal `:Item` for referenced-but-not-walked qnames (open at refresh time; sibling work). Touches `cfdb-extractor` + `cfdb-core::qname` + `cfdb-cli` test, but does NOT touch `cfdb-core::schema::descriptors` or `ci/edge-liveness.sh`. No collision.
- **Open:** #319 (above — not blocking, distinct scope)

## Sibling open issues
- #317 (closed-as-merged in PR #319 above) — sibling RFC-037 §3.7 edge-liveness finding (IMPLEMENTS dropped at ingest); now resolved
- #298 — feat(cfdb): const-table overlap detector — distinct scope
- #297 — feat(cfdb-extractor): emit `:Param` nodes + `REGISTERS_PARAM` — distinct scope
- #279 — EPIC: cfdb v1.0 — gating (this issue contributes a small chunk)
- #266, #265, #264, #263 — multi-language roadmap — distinct scope

## Doctrinal layer

Source: `docs/RFC-037-schema-producer-alignment.md` §3.7 (edge-liveness check, lines 162-200)
> Edge-liveness check ships informational in v0.3.0; promoted to blocking in v0.4.0. Declaring an edge label without a producer becomes a CI violation.
> Implication: at v0.4.0 promotion EQUIVALENT_TO will hard-fail unless either a producer lands (Option C) or the dormant label is suppressed via a reservation discriminator (Option A). Option A is the cheap, non-RFC-gated path that issue #307 prescribes.

Source: issue #307 body, "Decision needed" section (verbatim)
> A — Phase B reservation, document explicitly. Mark EQUIVALENT_TO in the schema describer with a "Reserved — no producer in v0.x; see issue #N for Phase B implementation" note. Avoids removal churn if it's genuinely planned. Add `Provenance::Reserved` (or similar) to the describer enum so the edge-liveness check can suppress dormant warnings on reserved labels.
> Implication: this is the prescribed shape — variant + descriptor tag + edge-liveness suppression. The implementer fills in the wire-up; the design space is closed.

Source: `crates/cfdb-core/src/schema/descriptors.rs:24-61` — `Provenance` enum currently carries 6 variants (`Extractor`, `EnrichRfcDocs`, `EnrichMetrics`, `EnrichGitHistory`, `EnrichConcepts`, `EnrichReachability`). All describe value origin (extractor or enrich pass). Adding `Reserved` extends the vocabulary to "no producer yet, by design."
> Implication: the existing variant-introduction pattern (e.g. `EnrichReachability` was added in #43-A as a new tag for slice 43-G) is the precedent — single new variant, follow the same docstring shape.

Source: `crates/cfdb-core/src/schema/descriptors.rs:93-102` — `EdgeLabelDescriptor` currently lacks a `provenance` field; `AttributeDescriptor` has one.
> Implication: the issue's "mark EQUIVALENT_TO descriptor" needs a new `provenance: Provenance` field on `EdgeLabelDescriptor`. This is an additive non-breaking schema change (no SchemaVersion bump) — `#[serde(default)]` covers older keyspaces that don't carry the field.

Source: `ci/edge-liveness.sh:48-67` — hardcoded `EDGE_LABELS` array, `:103 exit 1` on any dormant.
> Implication: suppression must read the schema describer at runtime (via `cfdb schema-describe | jq`) to know which labels are reserved — hardcoding "EQUIVALENT_TO is reserved" in the bash script would split-brain with the schema. Use the JSON output as the single source of truth.

Source: `CLAUDE.md` §5 (project-local)
> Non-breaking additions (new node label, new edge label, new optional attribute) MAY keep the version.
> Implication: adding `Provenance::Reserved` + a defaulted `provenance` field on `EdgeLabelDescriptor` is non-breaking. No SchemaVersion bump. No graph-specs-rust lockstep.

Source: `CLAUDE.md` §1 (project-local)
> Bug fix (wrong behavior on existing verb) → Issue → `/work-issue-lib` → dogfood-gated PR.
> Implication: this is a bug-fix-shaped change (closing a contract drift between schema declaration and producer absence). Routes through `/work-issue-lib`, not RFC pipeline.

## Split-brain pre-scan (scope-filtered)
`audit-split-brain --full-workspace --format json` → 1 match-arm overlap (cfdb-petgraph test helpers, out of scope), 0 const-table or split-resolution findings in `cfdb-core/src/schema/`. No split-brain risk in scope.

## Session handoffs (relevant)

`a0-session:2026-04-26-cfdb-317-shipped`
- Follow-up suggested: "after #319 merges: re-run RFC-037 §3.7 edge-liveness — IMPLEMENTS no longer dormant; verify EQUIVALENT_TO is the only single-source dormant left to confirm #307's scope" — exactly this.
- Lesson: "Synthesised :Item must compute bounded_context via the SAME compute_bounded_context call the walk uses ... when adding a post-walk emission pass that mirrors walk-time emission, ALL extractor-time-derived props must use the canonical resolution helper." Not directly applicable here (no producer being added) but reinforces the single-source-of-truth principle.
- Lesson: "Graph stubs are not arrows" (memory feedback_stubs_not_arrows.md) — applies to the option B/C alternatives. Option A is the right shape; A is "tag the existing edge as reserved-by-design" (not "synthesise an EQUIVALENT_TO producer to satisfy the check").

`a0-session:2026-04-26-cfdb-pattern3-pattern5-pattern1-rfc038`
- Lesson: "RFC-037 §3.7 edge-liveness check on PR #305 surfaced 8 dormant edge labels: 6 explained by HIR-extractor + enrichment-only emission paths (CALLS, EXPOSES, REGISTERS_PARAM, LABELED_AS, CANONICAL_FOR, REFERENCED_BY), 2 real bugs (IMPLEMENTS edges dropped at ingest — see #306; EQUIVALENT_TO declared with no producer — see #307)." — confirms #307 scope is exactly EQUIVALENT_TO; the other 6 dormants are NOT in scope (they have HIR/enrich producers that simply weren't run on cfdb-self).

## Forbidden moves
- **Do NOT pick Option B (remove EQUIVALENT_TO).** Issue body explicitly recommends A: "there's a documented domain meaning ('TradeSide ≡ Direction') suggesting a real planned use case. Removing risks losing the reservation."
- **Do NOT pick Option C (implement EQUIVALENT_TO producer now).** That's RFC-gated capability addition — out of scope for this bug-fix issue.
- **Do NOT bump `cfdb_core::SchemaVersion`.** Adding `Provenance::Reserved` + a defaulted `provenance` field is non-breaking (CLAUDE.md §5). Bumping triggers an unwarranted graph-specs-rust lockstep.
- **Do NOT author an RFC.** Bug fix routes through `/work-issue-lib`, not RFC pipeline (CLAUDE.md §1).
- **Do NOT extend the suppression to the other 6 dormants** (CALLS, EXPOSES, REGISTERS_PARAM, LABELED_AS, CANONICAL_FOR, REFERENCED_BY). Those have real producers in `cfdb-hir-extractor` or enrichment passes — they're dormant on cfdb-self because cfdb is its own domain (no `:EntryPoint`-like calls, no concepts beyond cfdb itself, etc.). Tagging them Reserved would be wrong; the proper fix is to run the relevant enrich passes during the dogfood. Out of scope here.
- **Do NOT hardcode "EQUIVALENT_TO" in `ci/edge-liveness.sh` as a reserved label** — that creates a split-brain between the bash script and the schema descriptor. The script must read reserved labels from `cfdb schema-describe` JSON output (single source of truth = the descriptor).
- **Do NOT modify the EdgeLabel string constant** at `crates/cfdb-core/src/schema/labels.rs:104` (`pub const EQUIVALENT_TO: &'static str = "EQUIVALENT_TO";`) — the wire-form label name stays. Only the descriptor's `provenance` field changes.
- **Do NOT touch test-helper `build` duplication in `cfdb-petgraph/src/enrich/{concepts,rfc_docs}/tests.rs`** — out of scope (separate audit finding, same as #317).

## Parent corrections
_none yet — append-only, see correction protocol below_

## Correction protocol
Parent may append (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA>
    **Reason:** <why>
    **Scope:** <what area / which skill / which step>
    **Body:** <correction text>

Past corrections are never edited or deleted.
