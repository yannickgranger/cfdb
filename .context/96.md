---
issue: 96
issue_state: open
issue_created_at: 2026-04-19T21:05:14Z
refreshed_at: 2026-04-21T00:00:00Z
refreshed_sha: fe8b890
verdict: current
repo: yg/cfdb
cfdb_keyspace_sha: fe8b8901a373
---

# Context package — Issue #96

## Issue summary

`Option W: cfdb extract --rev <repo-url>@<sha>` — extend the existing `cfdb extract --rev <sha>` flag (shipped PR #123, same-repo only) to also accept a full remote URL, clone the repo at the given SHA into a tempdir, extract, and cache by `(repo_url, sha)`. Unblocks qbot-core ↔ qbot-strategies bilateral cross-repo drift-lock (tools-devops R2.2).

## AC freshness

- **AC-1** (`cfdb extract --rev <url>@<sha>` clones + extracts): 🟢 OPEN. Current `commands::extract_at_rev` in `crates/cfdb-cli/src/commands.rs:74` rejects `--rev` when `--workspace` is not a git repo root (`.git` presence check, line 90). URL parsing + clone-to-tempdir path does not exist.
- **AC-2** (public + private Gitea auth via existing tokens): 🟢 OPEN. No HTTP/git credential plumbing exists in the extract path today. `GitWorktree::add` shells out to `git worktree add --detach` which inherits env-level credentials for SSH, but there's no HTTPS token handling or `GITEA_TOKEN`/`~/.config/git-credentials` plumbing.
- **AC-3** (cache by `(repo_url, sha)`): 🟢 OPEN. No cache layer exists. `tempfile::tempdir()` is used for the same-repo worktree path (line 96) and is dropped after extraction.
- **AC-4** (tested against qbot-core + qbot-strategies sample rev pair): 🟢 OPEN. No integration test exists for the URL form. Same-repo form is tested in `crates/cfdb-cli/tests/extract_rev.rs` (282 lines, 5 tests) but the fixture is a tempdir-built 2-commit repo, not a remote URL.

**Explicit carve-out from PR #123:** PR #123 body states "Extraction against external repo URLs — that's #96 'Option W', orthogonal" — so the same-repo `--rev <sha>` flag is deliberately NOT a superset of #96.

## Commits since issue was filed (scope: `crates/cfdb-cli/src/commands.rs`, `crates/cfdb-cli/tests/extract_rev.rs`, `crates/cfdb-cli/src/main.rs`, `crates/cfdb-cli/src/compose.rs`, `crates/cfdb-cli/Cargo.toml`)

- `d942f09` style: move tests mod to end of commands.rs (clippy items_after_test_module)
- `8901ca9` feat(cfdb-cli): cfdb extract --rev <sha> — extract against arbitrary git revisions (#37) — **this is PR #123, the prerequisite same-repo flag**
- `933d24f` feat(cfdb-petgraph,cfdb-concepts,cfdb-cli): enrich_concepts — :Concept nodes + LABELED_AS/CANONICAL_FOR edges (#109)
- `d239e57` feat(cfdb-petgraph,cfdb-cli): enrich_bounded_context re-enrichment + v0.2-9 ≥95% gate (#108)
- `c42ff67` feat(cfdb-petgraph,cfdb-core,cfdb-cli): enrich_rfc_docs real impl + SchemaVersion V0_2_3 (#107)
- `b0ab45c` chore(cfdb-petgraph,cfdb-cli): cargo fmt for #105
- `242ef1e` feat(cfdb-petgraph): enrich_git_history real impl — git2 behind git-enrich feature (#105) — `git2` dep landed, behind `git-enrich` feature gate (out of scope for this issue's extract path)
- `9b5e93d` refactor(cfdb-*): drain 53 pre-existing quality-metrics violations → 0 (closes #111)
- `a6e1fdd` refactor(cfdb-cli): extract dispatch_enrich helper — claw back run() complexity from seven-arm bloom (#104)
- `bc7797a` feat(cfdb-core,cfdb-cli): Slice 43-A prereq — EnrichBackend rename/additions + schema reservations + RFC amendment (#104)
- `bcf31bb` feat(cfdb-hir-extractor,cfdb-cli): :EntryPoint + EXPOSES + cfdb-cli --features hir + SchemaVersion V0_2_0 (#86)

None of these touch the `--rev` URL code path; PR #123 is the closest-adjacent and it explicitly deferred URL extraction to this issue.

## Relevant PRs

- **Merged:**
  - #123 — `feat(cfdb-cli): cfdb extract --rev <sha> — extract against arbitrary git revisions (#37)` — prerequisite: provides `GitWorktree` RAII guard + `short_rev` keyspace-naming helper + `extract_at_rev` dispatcher. Option W extends this.
  - #117 — `feat(cfdb-petgraph,cfdb-cli): enrich_git_history real impl — git2 behind git-enrich feature (#105)` — introduced `git2` as an optional dep behind `git-enrich` feature. Out of scope for this issue's `extract` path, but relevant if URL clone needs HTTPS auth (potentially reuse `git2`'s credential callbacks rather than shelling `git clone`).
- **Open:** none touching the extract path.

## Sibling open issues

- #34 (EPIC) — v0.2 tracker.
- #95 — `v0.2-phase1 minimum subset for qbot-core #4046 drift-lock` — umbrella; explicitly lists #96 as one of its downstream deliverables.
- #137 — `feat(ci): extend graph-specs anti-drift gate to tools/` — unrelated CI hardening.
- #49, #100, #101, #102, #128 — other open v0.2 work; none touch this extract path.
- #51 — RFC-029 addendum merge; downstream of all v0.2 children.

## Doctrinal layer

- `docs/RFC-032-v02-extractor.md` line 129: `**Issue:** #37 (cfdb extract --rev)`. RFC-032 treats `--rev` as one capability; Option W is the URL extension not covered by the parent issue #37 (which shipped as PR #123 and explicitly carved out URLs).
- `docs/RFC-032-v02-extractor.md` line 131: `The --rev flag enables extraction against a...` (truncated context). **Implication:** the RFC endorses the flag; the URL variant is a natural extension within the same design frame.
- No explicit "Option W" section in the committed RFCs (the name comes from qbot-core council-4046 tools-devops R2.2, referenced by the issue body). Treat the issue body itself as the scope authority: URL@SHA parsing, clone, cache, auth.

## Split-brain pre-scan (scope-filtered)

- `fromstr_bypasses.Visibility` at `crates/cfdb-extractor/src/item_visitor.rs:674` — delegates=true; documented false positive, out of scope.
- `duplicate_type_defs.LoadError` (cfdb-concepts vs tools/check-prelude-triggers) — intentional per `tools/check-prelude-triggers/src/toml_io.rs:3-4` (Forbidden move #8 from #48 prescription); out of scope for this issue.

No split-brain risk in `cfdb-cli` extract path.

## Session handoffs (relevant)

No prior session handoffs reference issue #96 directly. Session `2026-04-20-cfdb-backlog-audit` does NOT mention #96 in its follow-ups.

## Forbidden moves

- **Do not re-implement `GitWorktree`.** The RAII guard at `crates/cfdb-cli/src/commands.rs:142-195` is the single tempdir-backed worktree manager; URL clone must layer above it, not alongside.
- **Do not fork `short_rev` keyspace-naming.** Current impl at `commands.rs:119-127` is the canonical short-rev → keyspace mapper. Reuse it for URL@SHA keyspace naming.
- **Do not add an unconditional `git2` dep to `cfdb-cli`.** `git2` is already a dep but behind the `git-enrich` feature (PR #105 / commit `242ef1e`). If URL clone needs a library path (vs. shelling `git clone`), preserve the feature-gating pattern — do not make `git2` unconditional.
- **Do not drop the subprocess-contract for same-repo `--rev`.** PR #123 body explicitly states "No git library dep added — subprocess is the contract" for the same-repo path. Option W can use subprocess `git clone` (inherits ambient credentials) or feature-gated `git2` for the URL path, but must not break the same-repo subprocess invariant.
- **Do not write cache files inside `target/` or `tempdir`.** AC-3 requires persistent cache across runs. Likely location: `~/.cache/cfdb/extract/<sha256(url)>/<sha>/` or configurable via `--cache-dir`. Subject to prescription.
- **Do not hand-forge `.cfdb/current.sha` or commit it.** Per council-cfdb-wiring, `.cfdb/current.sha` is per-workspace state, `.gitignore`'d already at line 4.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block:

    ### Correction @ <ISO timestamp>
    **Commit:** <SHA of this correction commit>
    **Reason:** <why>
    **Scope:** <what area this correction applies to — specific skill, specific step>
    **Body:** <correction text>

Corrections are git commits (`context(#<issue>): correction — <subject>`). Sub-agents re-read the file at start of every invocation and MUST cite any correction SHA they act on in their output.

Past corrections are never edited or deleted. If a correction was wrong, append a new correction that retracts it. `git log .context/<issue>.md` is the audit trail.
