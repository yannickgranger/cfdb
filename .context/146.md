---
issue: 146
issue_state: open
issue_created_at: 2026-04-21T14:02:50Z
refreshed_at: 2026-04-21T16:40:00Z
refreshed_sha: 0845a32
verdict: current
repo: agency:yg/cfdb
cfdb_keyspace_sha: 0845a32b3d72
---

# Context package — Issue #146

## Issue summary

Slice 2 of RFC-034. Create `.cfdb/predicates/` directory (sibling of `.cfdb/queries/` and `.cfdb/concepts/`), seed with three canonical `.cypher` files + `README.md` (runbook), add a `crates/cfdb-query/tests/predicate_schema_refs.rs` static check asserting every `:Label`/`[:EdgeLabel]` in every seed file resolves in `cfdb_core::schema::{Label,EdgeLabel}`.

## AC freshness

- 🟢 AC-directory — `.cfdb/predicates/` does not exist on develop (`ls .cfdb/` shows only `concepts/`, `cross-fixture.toml`, `skill-routing.toml`).
- 🟢 AC-seed-1 — `context-homonym-crate-in-multiple-contexts.cypher` absent (`rg "context-homonym" .cfdb/` returns zero hits).
- 🟢 AC-seed-2 — `fn-returns-type-in-crate-set.cypher` absent.
- 🟢 AC-seed-3 — `path-regex.cypher` absent.
- 🟢 AC-schema-check — `crates/cfdb-query/tests/predicate_schema_refs.rs` absent (`ls crates/cfdb-query/tests/` does not exist as a dir — cfdb-query has no `tests/` on develop).
- 🟢 AC-parse-clean — verified all three planned seeds parse cleanly via `cfdb query ... --input yaml` with zero ParseError (tested locally against keyspace `0845a32b3d72`; parser accepts each form, evaluator returns EmptyResult because `--input` param passthrough is Phase A / deferred — irrelevant to Slice 2 which is file-only).

## Commits since issue was filed

None. Issue filed 2026-04-21T14:02:50Z in the same minute as #145/#147/#148/#149 during RFC-034 decomposition. No commits touch `.cfdb/` or `crates/cfdb-query/tests/` between issue filing and `refreshed_sha=0845a32`.

## Relevant PRs

- **Merged:** #150 (RFC-034 parent — docs + council artifacts)
- **Open:** #152 (sibling Slice 1 `#145` — `cfdb-cli::param_resolver`; parallel-shippable, no file overlap with #146)

## Sibling open issues

- #145 (Slice 1, in flight at PR #152) — parallel-shippable; no file overlap
- #147 (Slice 3) — blocked-by #145 + #146
- #148 (Slice 4), #149 (Slice 5) — blocked downstream

## Doctrinal layer

- `docs/RFC-034-query-dsl.md` §3.5 — `.cfdb/predicates/` directory layout with 3 seeds + mandatory first-line param docs. Implication: seed filenames, required H-comment format, and the spec of "one file = one predicate, no composition" are authoritative.
- `docs/RFC-034-query-dsl.md` §3.3 (R1 C2) — table enumerates supported predicate shapes using ONLY real schema vocabulary (`:Crate`, `:Item`, `:File`, `[:IMPLEMENTS_FOR]`, `[:IN_CRATE]`, etc.). Implication: `RE_EXPORTS`, `DECLARED_IN` are FORBIDDEN — they do not exist in `cfdb_core::schema::EdgeLabel`.
- `docs/RFC-034-query-dsl.md` §6 non-goals (R1 C5) — "Not positive `EXISTS { }`" and "Not an extension to inner-subquery WHERE grammar" are explicit. Inner `NOT EXISTS { ... }` subqueries support only Compare predicates (not `IN`/`AND`/`OR`/`NOT`). Outer-binding references do NOT flow into inner scope (evaluator limitation documented at `examples/queries/t1-concept-unwired.cypher` first-line comment). Implication: seed #1 uses top-level MATCH with AND (no subquery); all three seeds use only top-level patterns.
- `docs/RFC-034-query-dsl.md` §4.6 — single authority: `cfdb_concepts::load_concept_overrides`. Implication: the new static check test reads schema labels from `cfdb_core::schema::{Label,EdgeLabel}` CONSTS; it does NOT read `.cfdb/concepts/*.toml`.
- `council/49/RATIFIED.md` §5 (load-bearing decision 4) — seed #1 reframed from the impossible `RE_EXPORTS`-using shape to a context-homonym detector using only `:Crate` + `IN $list`. Implication: seed body must match the ratified shape, not the original issue-body shape.
- `council/49/RATIFIED.md` §9 — canonical owner of `.cfdb/predicates/` is the directory itself (no code owner; file-only slice). Canonical owner of the static check is `cfdb-query` crate (NOT cfdb-core or cfdb-cli).
- `CLAUDE.md` (project) §3 — `.cfdb/queries/*.cypher` is for ban rules consumed by `cfdb violations`; this is separate. `.cfdb/predicates/*.cypher` is consumed by `cfdb check-predicate` (Slice 3). Implication: do NOT co-mingle the two directories.
- `~/.claude/CLAUDE.md` §6.8 — no metric ratchets. Implication: no `.predicates-allowlist.toml` or baseline file.

## Split-brain pre-scan (scope-filtered)

No new resolution points. The static check test imports from `cfdb_core::schema::{Label,EdgeLabel}` — canonical. No new `Label::variants()`-style helper being created (neither exists on develop; we inline the known-valid set in the test module).

## Session handoffs (relevant)

- `a0-session:2026-04-21-763cbbbb-cfdb-oss-queue` — applicable lessons: "council caught real defects"; the re-export edge issue (RE_EXPORTS) that killed the original seed #1; Zero-code-change fast path does NOT apply to this slice (adds a Rust test file + Cypher seeds — seeds are `.cypher` not in the inert list, and the test is Rust source).

## Forbidden moves

- Do NOT reference `RE_EXPORTS` or `DECLARED_IN` edges in any seed `.cypher` — these do not exist in `cfdb_core::schema::EdgeLabel`. Re-export tracking is HIR Phase B (per RFC-034 §6 deferred non-goals).
- Do NOT use positive `EXISTS { ... }` in any seed — the parser only supports `NOT EXISTS`. (Verified at `crates/cfdb-query/src/parser/predicate.rs:43-48`.)
- Do NOT use `IN` / `AND` / `OR` / `NOT` inside a `NOT EXISTS { ... }` inner WHERE — the parser is Compare-only in inner scope. (Verified at `crates/cfdb-query/src/parser/predicate.rs:131-139`.)
- Do NOT reference outer-bound variables inside a `NOT EXISTS` subquery — inner scope is fresh (cfdb-query v0.1 evaluator limitation; documented at `examples/queries/t1-concept-unwired.cypher` first-line comment).
- Do NOT place any seed under `.cfdb/queries/` — that directory is reserved for `cfdb violations` ban rules. Slice 2 creates the SIBLING `.cfdb/predicates/` directory.
- Do NOT add a `.predicates-allowlist.toml` or similar ratchet file. Every predicate is zero-tolerance vs hardcoded thresholds (`~/.claude/CLAUDE.md` §6.8).
- Do NOT write a new crate for the static check — the test lives at `crates/cfdb-query/tests/predicate_schema_refs.rs` (RFC-034 §7 Slice 2 target).
- Do NOT change `cfdb_core::schema::{Label,EdgeLabel}` public API. The test READS the existing constants; it does not extend them.
- Do NOT modify any existing predicate file's first-line comment format — the mandatory `// Params: ... Returns: (qname, line, reason)` shape defined in RFC §3.5 is fixed by this slice.
- Do NOT add implementation of the `cfdb check-predicate` verb — that is Slice 3 (#147). This slice is file-only + one test.
- Do NOT bump `SchemaVersion`. RFC-034 §6 non-goal.

## Parent corrections

_none yet — append-only, see correction protocol below_

## Correction protocol

Parent may append to this file (and ONLY append) as sub-agents run. Each correction is a timestamped block; sub-agents re-read this file at start of every invocation. Never edit committed corrections.
